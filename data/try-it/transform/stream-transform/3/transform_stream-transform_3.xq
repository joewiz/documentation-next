(: stream-transform streams output directly to the servlet response :)
(: It can only be used within an HTTP servlet context (e.g., controller.xq) :)
"transform:stream-transform#3 — like transform:transform#3 but streams the result directly to the HTTP response instead of returning it. Requires servlet context."
