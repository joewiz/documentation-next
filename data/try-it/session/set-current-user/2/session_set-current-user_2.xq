(: Change the user identity for the current session :)
"session:set-current-user($user-name as xs:string, $password as xs:string) as xs:boolean — changes the user identity for the current HTTP session; subsequent XQueries run as the new user"
