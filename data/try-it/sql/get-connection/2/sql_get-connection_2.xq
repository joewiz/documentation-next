(: Open a JDBC connection to a SQL database :)
"sql:get-connection($driver-classname as xs:string, $url as xs:string) as xs:long? — opens a connection using the JDBC driver and URL; auto-closed when the XQuery finishes"
