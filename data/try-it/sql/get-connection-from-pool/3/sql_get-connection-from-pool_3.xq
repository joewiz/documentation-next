(: Get a pool connection with credentials :)
"sql:get-connection-from-pool($pool-name as xs:string, $username as xs:string, $password as xs:string) as xs:long? — retrieves a pool connection with explicit credentials"
