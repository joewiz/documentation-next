(: Create a JNDI directory entry :)
"jndi:create($directory-context as xs:integer, $dn as xs:string, $attributes as element()) as empty-sequence() — creates an entry with the given Distinguished Name and attributes"
