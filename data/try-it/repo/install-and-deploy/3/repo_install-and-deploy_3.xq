(: Download, install, and deploy a specific version from a public repository :)
"repo:install-and-deploy($pkgName as xs:string, $version as xs:string?, $publicRepoURL as xs:string) as element() — downloads and installs a specific version of a package, resolving dependencies automatically"
