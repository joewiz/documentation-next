(: Install and deploy from database, resolving dependencies via public repo :)
"repo:install-and-deploy-from-db($path as xs:string, $publicRepoURL as xs:string) as element() — installs from a .xar in the database; dependencies are downloaded from the public repo"
