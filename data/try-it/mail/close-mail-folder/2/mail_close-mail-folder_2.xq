(: Close a mail folder :)
"mail:close-mail-folder($mail-folder-handle as xs:long, $expunge as xs:boolean) as empty-sequence() — closes the folder; if $expunge is true, permanently removes deleted messages"
