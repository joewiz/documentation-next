(: Create a filesystem store callback — DBA only :)
"compression:fs-store-entry3#1 — returns a function($path, $data-type, $data) for storing extracted entries to a filesystem directory. Requires DBA role."
