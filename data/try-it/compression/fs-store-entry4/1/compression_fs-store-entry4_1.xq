(: Create a filesystem store callback with params — DBA only :)
"compression:fs-store-entry4#1 — returns a function($path, $data-type, $data, $param) for storing extracted entries to a filesystem directory. Requires DBA role."
