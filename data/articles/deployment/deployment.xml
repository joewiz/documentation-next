<topic id="deployment">
    <title>Database Deployment</title>
    <prolog>
        <data name="date" value="1Q20"/>
        <data name="keyword" value="operations"/>
        <data name="keyword" value="java-development"/>
    </prolog>
    <body>
        <p>This article explains how to install eXist-db as a stand-alone or embedded server.</p>
        <section id="overview">
            <title>Overview</title>
        

        <p>There are <i>three</i> ways to deploy the eXist-db database:</p>

        <dl>
                <dlentry>
                    <dt>In a Servlet Context</dt>
                    <dd>
                    <p>The database is deployed as part of a web application. eXist will happily
                        live together with other servlets. This is the default mode established when
                        the database is deployed according to instructions provided in the <xref href="basic-installation" scope="external">Basic Installation Guide</xref>.</p>
                    <p>In this mode all resources used by eXist-db will have paths relative to
                        the web application's current context. For example, eXist will store all its
                        database files in the <ph>WEB-INF/data</ph> directory of the web
                        application (unless specifically configured otherwise).</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt>Stand-alone Server Process</dt>
                    <dd>
                    <p>In stand-alone mode, eXist-db runs in its own Java virtual machine (JVM).
                        Clients have to access the database through the network, either using the
                        XML-RPC, WebDAV protocol or the REST-style HTTP API.</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt>Embedded in an Application</dt>
                    <dd>
                    <p>In embedded mode, the database is basically used as a Java library,
                        controlled by the client application. It runs in the same JVM as the client,
                        no network connection is needed. The client has full access to the
                        database.</p>
                </dd>
                </dlentry>
            </dl>

        <p>All three deployments are thread-safe and allow concurrent operations by multiple
            users. Servlets running in the same web application context will have direct access to
            the database. External client applications may still use the supplied network
            interfaces.</p>
        <p>Detailed instructions on how to set up eXist-db for use with a servlet-engine are
            provided in the <xref href="basic-installation" scope="external">Basic Installation Guide</xref>.
            The sections in this document address the other two deployment options. </p>
        <ul>
                <li>
                <p>Section 2 introduces the XML:DB URI and explain how different servers are
                    addressed by Java clients.</p>
            </li>
                <li>
                <p>Section 3 deals with running eXist as a stand-alone server</p>
            </li>
                <li>
                <p>Section 4 addresses the required steps to directly embed eXist into an
                    application, including how to embed it in the <xref href="http://titanium.dstc.edu.au" scope="external">XMLdbGUI</xref>.</p>
            </li>
            </ul>
    </section>
        <section id="xmldb-uri">
            <title>Addressing Servers using the XML:DB URI</title>
        

        <p>One way to access eXist-db from Java applications is to use the XML:DB API. The XML:DB
            API uses a specific URI scheme to locate a collection of XML resources on the server.
            You will encounter XML:DB URIs when working with the Java client, the backup tools and
            sometimes in XQuery functions. It is therefore important to understand how the URI
            scheme addresses servers and resources.</p>
        <p>eXist's XML:DB API implementation supports transparent access to remote as well as
            embedded database servers. This means the database on the server is available on the
            client as though it were locally connected to the client. The user should not have to be
            aware of where a resource is physically located. Applications need not be affected by
            how the database has been deployed.</p>

        <p>The XML:DB URI identifies the database implementation, the name of the collection, and
            optionally the location of the database server on the network. </p>

        <dl>
                <dlentry>
                    <dt>In a Servlet Context</dt>
                    <dd>
                    <p>Use the following URI to points to the Shakespeare collection on a remote
                        server which is running in a <i>servlet engine</i>:</p>
                    <pre>xmldb:exist://localhost:8080/exist/xmlrpc/db/shakespeare</pre>
                    <p>The host part: <ph>localhost:8080/exist/xmlrpc</ph> describes
                        the path to the XML-RPC listener, which is running as a servlet. The
                        collection part (<ph>/db/shakespeare</ph>) defines the collection
                        to retrieve.</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt>Stand-alone Server Process</dt>
                    <dd>
                    <p>If eXist had started as a stand-alone server, the URI would change its
                        port to <ph>8088</ph>:</p>
                    <pre>xmldb:exist://localhost:8088/xmlrpc/db/shakespeare</pre>
                </dd>
                </dlentry>
                <dlentry>
                    <dt>Embedded in an Application</dt>
                    <dd>
                    <p>To access an <i>embedded instance</i> of the database, we
                        simply drop the host part altogether and use three forward slashes
                        (<ph>///</ph>) in front of the collection path:</p>
                    <pre>xmldb:exist:///db/shakespeare</pre>
                </dd>
                </dlentry>
            </dl>

        <p>You can use the <xref href="java-admin-client" scope="external">Java Admin Client</xref>, which
            utilizes the XML:DB API to access the database, to experiment with these settings. The
            <ph>Login</ph> dialog offers a text field where you can specify an
            XML:DB URI to connect to.</p>
        <p>By default, the client tries to access the database engine located at the base
            URI:</p>
        <pre>xmldb:exist://localhost:8080/exist/xmlrpc/</pre>
        <p>Change the entry in the GUI to try another URI or pass it using the
            <ph>-ouri</ph> option on the command line. For example:</p>
        <pre>bin/client.sh -ouri=xmldb:exist://</pre>
        <p>This will cause a local database instance to run in the same Java virtual machine as
            the client.</p>
        <p>There is also a short form for this command. The <ph>-l</ph> option causes
            the client to launch a local database instance. For instance:</p>
        <pre>bin/client.sh -l</pre>
    </section>
        <section id="run-stand-alone">
            <title>Running as a Stand-alone Server</title>
        

        <p>There are many cases in which it is preferable to have the database engine running in
            its own Java virtual machine. In stand-alone mode, eXist will launch its own, embedded
            web server to provide XML-RPC, WebDAV and REST services to the outside world. The
            embedded server is based on a stripped-down Jetty. It uses a limited configuration,
            excluding all the additional services available in a full-blown servlet
            environment.</p>
        <p>The stand-alone deployment is more reliable and performs better than the web
            application setup, since no other threads (simultaneous tasks) are running. The
            stand-alone database server offers XML-RPC, WebDAV and REST-style HTTP interfaces for
            external client access. Please note that it does not support SOAP. </p>
        <p>By default, the stand-alone server listens on port <ph>8088</ph>, though
            this can be changed (see configuration below). The Java class for launching the server
            is <ph>org.exist.jetty.StandaloneServer</ph>.</p>

        

        <section id="start-stand-alone">
                <title>Starting the Stand-alone Server</title>
            

            <p>To start the server use either the <ph>bin/server.sh</ph> (Unix) or the
                <ph>bin\server.bat</ph> (Windows) command.</p>

            <p>The server is multi-threaded, a server thread is assigned to each client request.
                If the specified maximum number of threads is reached, the server will block the
                client until one of the server-threads is available to respond. By default, the
                maximum number of threads is 5. To change this, use the <ph>-t</ph>
                option, e.g.:</p>
            <pre>bin\server.bat -t 20</pre>
            <p>To access the stand-alone server using the <xref href="java-admin-client" scope="external">Java Admin Client</xref> (or by your own Java classes), start the XML:DB server URI
                to <ph>xmldb:exist://localhost:8088/xmlrpc</ph> </p>
        </section>

        

        <section id="config-stand-alone">
                <title>Standalone Server Configuration</title>
            

            <p>The <ph>tools/jetty/etc/standalone/WEB-INF/web.xml</ph> configuration
                file is used when the server operates in standalone mode. An instance of the Jetty
                web server is configured using its settings. The services offered with eXist have
                their own configuration servlet element in this file.</p>
            <p>Currently, the servlet API alternatives are "<ph>webdav</ph>,
                "<ph>xmlrpc</ph>", and "<ph>rest</ph>". Any of these servlets
                can be disabled by setting a init-param <ph>enabled</ph> attribute to
                "<ph>no</ph>".</p>

            <section id="binding-address">
                    <title>Controlling the Binding Address (&lt;indexer&gt;)</title>
                

                <p>You can control the binding address by changing the addConnector
                    <ph>Call</ph> element in <ph>tools/jetty/etc/standalone.xml</ph>:</p>
                <dl>
                        <dlentry>
                            <dt> <ph>port</ph> </dt>
                            <dd>
                            <p>The port on which the server will listen. The Jetty web server
                                will bind to port <ph>8088</ph> by default.</p>
                        </dd>
                        </dlentry>
                        <dlentry>
                            <dt> <ph>host</ph> </dt>
                            <dd>
                            <p>The host-name on which the server will respond.</p>
                        </dd>
                        </dlentry>
                        <dlentry>
                            <dt> <ph>address</ph> </dt>
                            <dd>
                            <p>The IP address on which the server should bind. This may be useful
                                when the server has multiple addresses that serve the same
                                host-name.</p>
                        </dd>
                        </dlentry>
                    </dl>
            </section>

            <section id="webdav">
                    <title>WebDAV Servlet</title>
                

                <p>The WebDAV servlet provides services at the context address specified. This
                    servlet is represented by the <ph>webdav</ph> element in the
                    <ph>server.xml</ph> configuration file. For this element, the
                    <ph>context</ph> attribute controls the web server context at which
                    the WebDAV services are provided. If the <ph>context</ph> attribute is
                    not specified, it defaults to <ph>/webdav/</ph>.</p>
            </section>

            <section id="xml-rpc">
                    <title>XML-RPC Servlet</title>
                

                <p>The XML-RPC servlet provides database API services to clients, like the Admin
                    GUI client. This servlet is represented by the <ph>xmlrpc</ph> element in the
                    <ph>server.xml</ph> configuration file. For this element, the
                    <ph>context</ph> attribute controls the web server context at which
                    the XMLRPC services are provided. If the <ph>context</ph> attribute is
                    not specified, it defaults to <ph>/xmlrpc/</ph>. </p>
                <note type="warning">
                        <p>If you disable this servlet, you disable the use of the <xref href="java-admin-client" scope="external">Java Admin Client</xref>.</p>
                    </note>
            </section>

            <section id="rest">
                    <title>REST Servlet</title>
                

                <p>The REST servlet provides HTTP/REST-style interactions with the database. It
                    is configured by a <ph>servlet</ph> element with <ph>servlet-class</ph>
                    <ph>org.exist.http.servlets.EXistServlet</ph> in the
                    <ph>tools/jetty/etc/standalone/WEB-INF/web.xml</ph> configuration
                    file. It depends on the <ph>XQueryURLRewrite</ph> filter for context,
                    default is <ph>/</ph>.</p>
                <p>The REST Servlet has a number of other parameters that can be set by child
                    elements:</p>
                <pre outputclass="xml">&lt;servlet&gt;
  &lt;servlet-name&gt;
    EXistServlet
  &lt;/servlet-name&gt;
  &lt;servlet-class&gt;
    org.exist.http.servlets.EXistServlet
  &lt;/servlet-class&gt;
  &lt;init-param&gt;
    &lt;param-name&gt;
      use-default-user
    &lt;/param-name&gt;
    &lt;param-value&gt;
      true
    &lt;/param-value&gt;
  &lt;/init-param&gt;
  ...
&lt;/servlet&gt;</pre>
                <dl>
                        <dlentry>
                            <dt> <ph>form-encoding</ph> </dt>
                            <dd>
                            <p>The default encoding of form POSTs.</p>
                        </dd>
                        </dlentry>
                        <dlentry>
                            <dt> <ph>container-encoding</ph> </dt>
                            <dd>
                            <p>The default encoding of the servlet container for all HTTP
                                interactions except POSTs.</p>
                        </dd>
                        </dlentry>
                        <dlentry>
                            <dt> <ph>use-default-user</ph> </dt>
                            <dd>
                            <p>A boolean value (<ph>true</ph>/<ph>false</ph>)
                                that indicates whether the effective user must be used for
                                non-authenticated interactions. If any of these fails a simple AUTH
                                interaction is undertaken. </p>
                        </dd>
                        </dlentry>
                        <dlentry>
                            <dt> <ph>user</ph> </dt>
                            <dd>
                            <p>Takes the username of the default user.</p>
                        </dd>
                        </dlentry>
                        <dlentry>
                            <dt> <ph>password</ph> </dt>
                            <dd>
                            <p>Takes the password of the default user.</p>
                        </dd>
                        </dlentry>
                    </dl>
            </section>

            <section id="custom">
                    <title>Custom Servlets</title>
                

                <p>Any servlet can be configured to run in the Jetty server by adding a
                    <ph>servlet</ph> element to the <ph>server.xml</ph> configuration file.
                    The <ph>servlet</ph> element has the standard attributes of
                    <ph>enabled</ph> and <ph>context</ph>, as well as the <ph>class</ph>
                    attribute to specify the servlet implementation class.</p>
                <p>This element can have any number of <ph>param</ph> child-elements to set
                    parameters for the servlet. For example:</p>

                <pre outputclass="xml">&lt;servlet enabled="yes" context="/myservlet/*" class="com.example.MyServlet"&gt;
  &lt;param name="auth" value="true"/&gt;
  &lt;param name="demo" value="false"/&gt;
&lt;/servlet&gt;</pre>
            </section>

            <section id="forwarding">
                    <title>Forwarding Requests</title>
                

                <p>The forwarding-request settings allow you to map incoming URL requests to
                    specific resources on the server (e.g. queries). This is done with the
                    <ph>forwarding</ph> element. Inside this element is a single <ph>root</ph>
                    element and any number of <ph>forward</ph> elements. </p>
                <p>Each <ph>forward</ph> element specifies a specific primary URL that is
                    forwarded to secondary URL. This allows you to map a "clean" URL to an XQuery or
                    some other resource within the server. The target is always specified by a
                    <ph>destination</ph> attribute. For instance:</p>
                <pre outputclass="xml">&lt;forward path="/admin" destination="/db/admin/admin.xql"/&gt;
&lt;forward path="/docs" destination="/db/products/docs.xml"/&gt;</pre>
                <p>Here the first <ph>forward</ph> element maps the URI path
                    <ph>/admin</ph> to the XQuery resource
                    <ph>/db/admin/admin.xql</ph>, the second one maps the URI path
                    <ph>/docs</ph> to the document
                    <ph>/db/products/docs.xml</ph>.</p>

                <p>The <ph>root</ph> element maps requests to the root directory of the server
                    (for instance <ph>/</ph>) to a specified resource. For example:</p>
                <pre outputclass="xml">&lt;root destination="/db/admin/admin.xql"/&gt;</pre>
                <p>This maps a requests for the (default) server root to the XQuery resource
                    <ph>/db/admin/admin.xql</ph>.</p>

            </section>
        </section>

        

        <section id="shut-down">
                <title>Shutting Down the Database</title>
            

            <p>Use the <ph>shutdown.bat</ph> (Windows/DOS) and
                <ph>shutdown.sh</ph> (Unix) scripts to shutdown the database. These
                commands connect to the default server URI, usually
                <ph>xmldb:exist://localhost:8080/exist/xmlrpc</ph>.</p>
            <p>If your database is running in stand-alone mode, you must specify a different
                server URI. For instance, to stop eXist when running in stand-alone mode, listening
                on port <ph>8088</ph>, use:</p>
            <pre>$EXIST_HOME/bin/shutdown.sh --uri=xmldb:exist://localhost:8088/xmlrpc</pre>
        </section>
    </section>
        <section id="embedding">
            <title>Embedding eXist in an Application</title>
        

        <p>In the embedded mode, the database runs in the same Java virtual machine as the client
            application. The database will not be accessible by any outside application and no
            network listeners are started. You can embed eXist-db into any Java application using
            the XML:DB API.</p>
        <p>To prepare the environment for using an embedded eXist, follow the steps below:</p>
        <ol>
                <li>
                <p>Copy <ph>conf.xml</ph> and <ph>log4j.xml</ph> to the
                    target directory.</p>
            </li>
                <li>
                <p>Create a subdirectory <ph>data</ph> in the target directory.</p>
            </li>
                <li>
                <p>Edit the <ph>files</ph> attribute in the <ph>db-connection</ph>
                    section of <ph>conf.xml</ph> to point to this data directory.</p>
            </li>
                <li>
                <p>Do the same for the <ph>journal-dir</ph> in the <ph>recovery</ph>
                    element.</p>
            </li>
                <li>
                <p>To see logging output, edit <ph>log4j.xml</ph>. The simplest way is
                    to change the <ph>appender-ref</ph> in the <ph>root</ph> category to
                    <ph>console</ph>. This will result in most log messages being written
                    to the console.</p>
            </li>
                <li>
                <p>Create a <ph>lib</ph> directory below the target directory and copy
                    the following <ph>.jar</ph> files from eXist-db into it:</p>
                <ul>
                        <li>
                        <p> <ph>exist.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/extensions/exist-modules.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/core/antlr-X.X.X.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/core/commons-pool-X.X.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/core/commons-collections-X.X.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/core/commons-logging-X.X.X.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/core/log4j-X.X.X.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/optional/slf4j-api-X.X.X.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/optional/slf4j-log4j12-X.X.X.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/core/quartz-X.X.X.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/core/sunxacml.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/core/xmldb.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/core/xmlrpc-client-X.X.X.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/core/xmlrpc-common-X.X.X.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/core/xmlrpc-server-X.X.X.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/core/jta.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lib/core/pkg-repo.jar</ph> </p>
                    </li>
                    </ul>
                <p>If you plan to use extension modules (like the n-gram index), copy the
                    corresponding jars from <ph>lib/extensions</ph>. The default eXist
                    configuration needs:</p>
                <ul>
                        <li>
                        <p> <ph>exist-ngram-module.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>exist-lucene-module.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>exist-versioning.jar</ph> </p>
                    </li>
                    </ul>
                <p>For the Lucene module copy the jars from
                    <ph>extensions/indexes/lib</ph>:</p>
                <ul>
                        <li>
                        <p> <ph>lucene-core-X.X.X.jar</ph> </p>
                    </li>
                        <li>
                        <p> <ph>lucene-regex-X.X.X.jar</ph> </p>
                    </li>
                    </ul>
                <p>The <ph>lib/endorsed</ph> directory furthermore plays a special
                    role: the Java releases come with their own XML support libraries, including
                    Xalan (for XSLT processing), an XML parser, and the standard Java interfaces for
                    SAX and DOM. Some features of eXist will not work properly with a wrong version
                    of Xerces or the resolver <ph>.jar</ph> (schema validation, catalog loading,
                    ...). To ensure the correct versions are available, we have included these
                    versions of Xerces, Xalan and Saxon, plus the standard interfaces used by both
                    of them. You can use Java's endorsed library loading mechanism to ensure the
                    correct XML support libraries are loaded. </p>
                <ul>
                        <li>
                        <p>Create a directory <ph>lib/endorsed</ph> </p>
                    </li>
                        <li>
                        <p>Copy all <ph>.jar</ph> files from eXist's
                            <ph>lib/endorsed</ph> directory into the newly created
                            directory</p>
                    </li>
                    </ul>
                <p>Specifying the <ph>-Djava.endorsed.dirs=lib/endorsed</ph> system
                    property on the Java command line will force the JVM to prefer any library it
                    finds in the endorsed directory over its own system libraries (copying the jars
                    into <ph>$JAVA_HOME/jre/lib/endorsed</ph> will do the same thing).
                    Note that the batch and shell scripts included with eXist all set the
                    <ph>java.endorsed.dirs</ph> system property to point to
                    <ph>lib/endorsed</ph>.</p>
            </li>
                <li>
                <p>Make sure your <ph>CLASSPATH</ph> includes these <ph>.jar</ph>
                    files.</p>
            </li>
            </ol>

        <p>Internally, eXist has two different XML:DB driver implementations: the first
            communicates with a remote database engine using XML-RPC calls; the second has direct
            access to a local instance of eXist. Which implementation is selected depends on the
            XML:DB URI as described above.</p>
        <p>To access an embedded database, simply drop the host portion (for instance
            <ph>localhost:8088/xmlrpc/</ph>) from the URI. For instance:
            <ph>xmldb:exist:///db</ph> </p>
        <p>To start an embedded database instance, simply set the system property
            <ph>exist.initdb</ph> to <ph>true</ph>. This will notify the XML:DB
            driver to read the configuration settings when starting the database if none has been
            previously started. For example, to launch your own Java application with an embedded
            instance, you may enter the command:</p>
        <pre>java -Dexist.initdb=true MyApp</pre>
        <p>The driver will try to read the eXist configuration file, create the required database
            files (if absent) and launch the database. </p>
        <p>The driver must be able to read the configuration file <ph>conf.xml</ph>. It
            seraches for <ph>conf.xml</ph> in the directory specified by the
            <ph>exist.home</ph> system property. You should therefore ensure a copy of
            <ph>conf.xml</ph> is present in the correct directory. For example, to launch
            your own application, while setting <ph>exist.home</ph>:</p>
        <pre>java -Dexist.initdb=true -Dexist.home=/home/exist/eXist MyApp</pre>
        <note type="notice">
                <p>The paths to the data and log directories in <ph>conf.xml</ph> need to
                point to <i>writable</i> directories.</p>
            </note>
        <p>Instead of using the <ph>-Dexist.initdb</ph> property, you can also tell the
            database driver to create a local database instance during the initialization of the
            XML:DB database manager within your Java code. Set the
            <ph>create-database</ph> property on the created <ph>Database</ph>
            object to <ph>true</ph>. For example:</p>
        <pre outputclass="java">Class cl = Class.forName("org.exist.xmldb.DatabaseImpl");
Database database = (Database) cl.newInstance();
database.setProperty("create-database", "true");
DatabaseManager.registerDatabase(database);</pre>

        <p>When running eXist in embedded mode, ensure proper shutdown the database before your
            application exits. The main reason is to flush all of the unwritten data buffers to
            disk. The database uses a background thread to periodically synchronize its buffers with
            the data files on disk, and this thread will keep running unless you shutdown the
            database.</p>
        <p>There is a special XML:DB service, <ph>DatabaseInstanceManager</ph>, which
            contains a single method: <ph>shutdown</ph>. To properly shutdown the database
            instance, retrieve the service from the <ph>/db-collection</ph> and call
            <ph>shutdown()</ph>. For example:</p>
        <pre outputclass="java">DatabaseInstanceManager 
manager = (DatabaseInstanceManager) 
    collection.getService("DatabaseInstanceManager", "1.0"); 
manager.shutdown();</pre>
        <p>To summarize, a minimal working class with a single static main to start/stop the db
            may look like this:</p>

        <pre outputclass="java">import org.xmldb.api.DatabaseManager;
import org.xmldb.api.base.Collection;
import org.xmldb.api.base.Database;
import org.exist.xmldb.DatabaseInstanceManager;

public class TestDB {		
	public static void main(String args[]) throws Exception {
		// initialize driver
		Class cl = Class.forName("org.exist.xmldb.DatabaseImpl");
		Database database = (Database)cl.newInstance();
		database.setProperty("create-database", "true");
		DatabaseManager.registerDatabase(database);
		
		// try to read collection
		Collection col = 
			DatabaseManager.getCollection("xmldb:exist:///db", "admin", "");
		String resources[] = col.listResources();
		System.out.println("Resources:");
		for (int i = 0; i &lt; resources.length; i++) {
			System.out.println(resources[i]);
		}
		
		// shut down the database
		DatabaseInstanceManager manager = (DatabaseInstanceManager) 
			col.getService("DatabaseInstanceManager", "1.0"); 
		manager.shutdown();
	}
}</pre>
        <p>Put this code into a Java file <ph>TestDB.java</ph>, store it into the
            target directory (which we prepared above), and compile and run it with:</p>
        <pre>javac TestDB.java java -Dexist.initdb=true -Dexist.home=. TestDB</pre>
    </section>
    </body>
</topic>