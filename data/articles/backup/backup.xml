<topic id="backup">
    <title>Database Backup and Restore</title>
    <prolog>
        <data name="date" value="4Q21"/>
        <data name="keyword" value="operations"/>
    </prolog>
    <body>
        <p>This article discusses eXist-db's database backup/restore procedures and related
        details</p>
        <section id="overview">
            <title>Overview</title>
        

        <p>During backup, eXist-db exports the contents of its database (both XML and binary
            files) to a hierarchy of directories on the hard drive. This hierarchy is organized
            according to the organization of collections in the database.</p>

        <p>There are two main methods for creating a backup:</p>
        <dl>
                <dlentry>
                    <dt>Server-side</dt>
                    <dd>
                    <p>
                            <xref href="#server-side-backup" scope="local"/> are run through eXist-db's job
                        scheduler or triggered via the web interface. This type of backup extracts
                        the data directly from the low-level database files. It is therefore much
                        faster then a client-side backup and also provides support for incremental
                        backups.</p>
                    <p>A server-side backup guarantees that the database is in a consistent state
                        during backup. Possible corruptions in the database will be detected and the
                        backup process will try to work around them. In many cases, damaged
                        resources can at least be partially recovered. However it should be noted,
                        that database operations will be suspended whilst the backup is
                        in-progress.</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt>Client-side</dt>
                    <dd>
                    <p>You can use the <xref href="java-admin-client" scope="external">Java Admin
                        Client</xref> or a command line utility to create a <xref href="#client-side-backup" scope="local"/> of the data on the server.</p>
                    <p>When the client controls the backup process, unlike the server-side
                        backups, the server is not blocked and continues to accept requests from
                        other clients. Other users can modify the db while the backup is running, so
                        logical dependencies between different resources may not be preserved
                        correctly. The client uses the XML:DB API to access the database. This means
                        that it cannot backup documents or collections if they are damaged in any
                        way.</p>
                    <p>Consider client-side backups a lightweight form of backup. During backup
                        the database is not locked so people can keep working. However, the result
                        may therefore be inconsistent. Client-side backups are <i>not
                        safe</i> in terms of getting a complete and consistent copy of your
                        data.</p>
                </dd>
                </dlentry>
            </dl>
        <p>The format of the generated backup archives will be the same for both backup methods:
            Resource and collection <i>metadata</i> is exported to a special XML
            descriptor file, named <ph>__contents__.xml</ph>, with information including
            resource type, owner, modification date and/or permissions assigned to a resource. You
            will find one <ph>__contents__.xml</ph> file in each directory created by the
            backup. This descriptor file is required to restore the backup. A full backup includes
            index configuration files and user settings.</p>

        <p>The location of the backup files depends on how the backup was initiated:</p>
        <ul>
                <li>
                <p> Backups via the dashboard or XQuery, are server-side backups, and write
                    backups into the <ph>export</ph> directory inside the data directory of the
                    eXist instance.</p>
            </li>
                <li>
                <p>The Emergency Backup Tool writes into <ph>$EXIST_HOME/export</ph>, unless
                    configured otherwise.</p>
            </li>
                <li>
                <p>Client Side backups done via the Java Admin Client are configurable with
                    regards the location that the backup files should be written to.</p>
            </li>
                <li>
                <p>Likewise, other methods (e.g. <xref href="ant-tasks" scope="external">
                            <ph>Ant
                    tasks</ph>
                        </xref>) are also fully configurable.</p>
            </li>
            </ul>

        <p>Users can manually modify files in the backup directories without requiring special
            software. Any changes made to these files are reflected in the database after a restore
            (or when the data is imported into another database system).</p>

        <note type="warning">
                <p> When migrating to a new eXist-db version, take care to use a version of the
                client corresponding to your server version. Usually, the backup process is
                backwards compatible. However, using a newer client version to create the backup
                from a server running an older version may lead to unexpected problems.</p>
                <p>The backup process also generates a report. Normally this contains the list of
                collections which were backed up. If you notice any warnings or errors, it is
                especially important that you perform a “full restore” of your backed-up data onto
                the new installation of eXist, as described in the <xref href="#restore" scope="external">following steps.</xref> </p>
            </note>
    </section>
        <section id="server-side-backup">
            <title>Server-side Backups</title>
        

        <p>Server-side backups are executed as system tasks, which means that when performing a
            server-side backup, eXist-db will do the following:</p>
        <ul>
                <li>
                <p>Wait for all pending transactions to complete.</p>
            </li>
                <li>
                <p>Enter protected mode. No new transactions will be allowed. Concurrent requests
                    by other clients will be blocked and added to the internal queue.</p>
            </li>
                <li>
                <p>The system runs an <xref href="#automatic-consistency-check" scope="external">automatic
                    consistency check</xref> on the database.</p>
            </li>
                <li>
                <p>Run the backup process.</p>
            </li>
                <li>
                <p>Once the backup is complete, the database will switch back to normal service
                    and all locks will be released.</p>
            </li>
            </ul>

        <section id="trigger-dashboard">
                <title>Triggering backups using the Dashboard</title>
            
            <p>You can trigger the backup using the <xref href="dashboard" scope="external">Dashboard</xref>:</p>
            <fig>
                    <image href="assets/Backup.png"/>
                </fig>
            <p>Clicking the <ph>Trigger Backup</ph> button will schedule a
                single server-side backup task.</p>
            <p>You can click on <ph>Refresh</ph> to update the view, which lists all
                backup archives currently available within the standard backup directory. Click on
                the name of an archive to download it.</p>
            <p>eXist-db can also create <i>incremental backups</i>, meaning only
                resources which were modified since the last backup are backed up.</p>
            <p>The default storage directory is
                <ph>$EXIST_HOME/data/export/</ph>. You can configure a different
                directory in the configuration file <ph>conf.xml</ph>.</p>
        </section>

        <section id="schedule">
                <title>Scheduling Backups</title>
            

            <p>You can use eXist-db's <xref href="configuration#scheduler" scope="external">scheduler</xref> to
                schedule server-side backup and consistency check tasks. To do this, add the
                following definition to the scheduler section in the configuration file
                <ph>$EXIST_HOME/etc/conf.xml</ph>.</p>


            <section>
                    <title>Job Definition in conf.xml</title>
                    <pre outputclass="xml">&lt;job type="system" name="check1" class="org.exist.storage.ConsistencyCheckTask" cron-trigger="0 0 0/12 * * ?"&gt;
  &lt;parameter name="output" value="export"/&gt;
  &lt;parameter name="zip" value="yes"/&gt;
  &lt;parameter name="backup" value="yes"/&gt;
  &lt;parameter name="incremental" value="no"/&gt;
  &lt;parameter name="incremental-check" value="no"/&gt;
  &lt;parameter name="max" value="2"/&gt;
&lt;/job&gt;</pre>
                </section>
            <p>The time/frequency of the backup is specified in the
                <ph>cron-trigger</ph> attribute. The syntax is borrowed from the Unix
                <ph>cron</ph> utility, though there are small differences. Please consult
                the Quartz documentation about <xref href="http://www.quartz-scheduler.org/documentation/quartz-2.3.0/tutorials/tutorial-lesson-06.html" scope="external">CronTrigger</xref> configuration. The definition in the example means every 12
                hours, starting at midnight. </p>
            <p>The task accepts the following parameters:</p>
            <dl>
                    <dlentry>
                        <dt>output</dt>
                        <dd>
                        <p>The directory to which the backup is written. Relative paths are
                            interpreted relative to eXist-db's main data directory <ph>$EXIST_HOME/data</ph>.</p>
                    </dd>
                    </dlentry>
                    <dlentry>
                        <dt>backup</dt>
                        <dd>
                        <p>Setting this to <ph>yes</ph> will create a full database backup
                            (in addition to running the system checks).</p>
                        <p>Setting this to "no" will not create a backup unless errors were
                            detected during the consistency check. In that case the task will
                            <i>always</i> try to generate an emergency backup.</p>
                    </dd>
                    </dlentry>
                    <dlentry>
                        <dt>zip</dt>
                        <dd>
                        <p>If set to <ph>yes</ph>, the backup will be written into a zip
                            archive. Due to limitations of the zip format, archives larger than 4
                            gigabytes may not be readable.</p>
                        <p>Setting this to <ph>no</ph> will create a backup on the file
                            system which has no such limitations.</p>
                    </dd>
                    </dlentry>
                    <dlentry>
                        <dt>incremental</dt>
                        <dd>
                        <p>Setting this to <ph>yes</ph> will trigger an incremental backup.
                            The first backup will always be a full backup. Subsequent backups will
                            be incremental: only resources which were modified since the last backup
                            will be saved.</p>
                    </dd>
                    </dlentry>
                    <dlentry>
                        <dt>incremental-check</dt>
                        <dd>
                        <p>By default, for incremental backups no consistency check is done
                            because this may take too long. Setting
                            <ph>incremental-check</ph> to <ph>yes</ph> will enable
                            consistency checking for incremental backups.</p>
                    </dd>
                    </dlentry>
                    <dlentry>
                        <dt>max</dt>
                        <dd>
                        <p>On incremental backup, create a full backup every
                            <ph>max</ph> backup runs. For eaxmple, if you set the
                            parameter to <ph>2</ph>, a full backup will be performed after every
                            two incremental backups.</p>
                    </dd>
                    </dlentry>
                    <dlentry>
                        <dt>dir</dt>
                        <dd>
                        <p>The directory to store the backup in. The path is relative to exist's default data directory.</p>
                    </dd>
                    </dlentry>
                    <dlentry>
                        <dt>suffix / prefix</dt>
                        <dd>
                        <p>Start and end of the filename for a zipped backup.</p>
                    </dd>
                    </dlentry>
                    <dlentry>
                        <dt>collection</dt>
                        <dd>
                        <p>The name of the instances' collection to be included in the backup.</p>
                    </dd>
                    </dlentry>
                    <dlentry>
                        <dt>user / password</dt>
                        <dd>
                        <p>The username and password combination necessary for accessing the backup.</p>
                    </dd>
                    </dlentry>
                    <dlentry>
                        <dt>zip-files-max</dt>
                        <dd>
                        <p>The maximum number of backups to keep in the backup location.</p>
                    </dd>
                    </dlentry>
                </dl>

            <note type="note">
                    <p>You can schedule more than one backup job. For example, an incremental backup
                    could be done multiple e.g. every two minutes while a full backup is created once during
                    the night.</p>
                    <pre outputclass="xml">&lt;job type="system" name="databackup" class="org.exist.storage.BackupSystemTask" period="120000"&gt;
  &lt;parameter name="dir" value="backup"/&gt;
  &lt;parameter name="suffix" value=".zip"/&gt;
  &lt;parameter name="prefix" value="backup-"/&gt;
  &lt;parameter name="collection" value="/db"/&gt;
  &lt;parameter name="user" value="admin"/&gt;
  &lt;parameter name="password"/&gt;
  &lt;parameter name="zip-files-max" value="28"/&gt;
&lt;/job&gt;</pre>
                </note>
        </section>

        <section id="trigger-xq">
                <title>Triggering Backups from XQuery</title>
            

            <p>System jobs can also be triggered from within an XQuery script using the
                <ph>system:trigger-system-task</ph> function:</p>
            <section>
                    <title>Triggering a Backup From XQuery</title>
                    <pre outputclass="xquery">let $params :=
 &lt;parameters&gt;
   &lt;param name="output" value="export"/&gt;
    &lt;param name="backup" value="yes"/&gt;
    &lt;param name="incremental" value="yes"/&gt;
 &lt;/parameters&gt;
 return
    system:trigger-system-task("org.exist.storage.ConsistencyCheckTask", $params)</pre>
                </section>
            <p>This will schedule a backup to be executed as soon as possible.</p>
        </section>

        <section id="automatic-consistency-check">
                <title>Automatic Consistency Checks</title>
            

            <p>The system task runs a consistency and sanity check on the database before a
                server-side backup. Reports of this check are written into the same directory as the
                backup archive. The last report can also be viewed using JMX (see <xref href="#using-jmx" scope="local"/>).</p>
            <p>The consistency check will first check the collection hierarchy, then scan through
                the stored node tree of every document in the db, testing node properties like the
                node's id, child count, attribute count and node relationships. Contrary to normal
                database operations, the different <ph>.dbx</ph> files are checked
                independently. This means that even if a collection is no longer readable, the tool
                will still be able to scan the documents in the damaged collection.</p>
            <p>The backup task uses the information provided by the consistency check to work
                around damages in the db. It tries to export as much data as possible, even if parts
                of the collection hierarchy are corrupted or documents are damaged:</p>
            <ul>
                    <li>
                    <p>Descendant collections will be exported properly even if their ancestor
                        collection is corrupted</p>
                </li>
                    <li>
                    <p>Documents which are intact but belong to a destroyed collection will be
                        stored into a special collection <ph>/db/lost_and_found</ph>
                    </p>
                </li>
                    <li>
                    <p>Damaged documents removed from the backup</p>
                </li>
                </ul>

            <section id="using-jmx">
                    <title>Using JMX to View Check Reports</title>
                

                <p>If Java Management Extensions (JMX) are enabled in the Java VM that is running
                    eXist, you can use a JMX client to see the latest consistency check reports. The
                    screen shot below shows <ph>jconsole</ph>,(included with the Java
                    JDKs):</p>
                <fig>
                        <image href="assets/jconsole.png"/>
                    </fig>

                <p>eXist also includes a command-line JMX client. Call it with parameter
                    <ph>-s</ph> to see the latest consistency report:</p>
                <ul>
                        <li>
                        <p>Unix/Linux:</p>
                        <pre>$EXIST_HOME/bin/jmxclient.sh -s</pre>
                    </li>
                        <li>
                        <p>Windows:</p>
                        <pre>$EXIST_HOME\bin\jmxclient.bat -s</pre>
                    </li>
                    </ul>
                <p>This may produce output as shown below:</p>
                <pre>Sanity report
-----------------------------------------------
                Status: FAIL
      Last check start: Thu May 08 21:40:00 CEST 2008
        Last check end: Thu May 08 21:40:00 CEST 2008
            Check took: 594ms
            Error code: RESOURCE_ACCESS_FAILED
            Description: 32</pre>
                <p>You can also subscribe to the notifications made available by the SanityReport
                    MBean to be informed of sanity check results. Please consult eXist-db's <xref href="jmx" scope="external">JMX documentation</xref> on how to configure this.</p>
            </section>

        </section>

    </section>
        <section id="client-side-backup">
            <title>Client-side Backup</title>
        

        <p>For client-side backups you can either use the <xref href="java-admin-client" scope="external">Java Admin Client</xref> or the backup command line utility.</p>
        <section id="jac">
                <title>Using the Java Admin Client</title>
            


            <p>If you are using the <xref href="java-admin-client" scope="external">Java Admin
                Client</xref>, do the following:</p>
            <ol>
                    <li>
                    <p>Select either the Backup Icon (arrow pointed upward) in the toolbar or
                        <ph>Tools, Backup</ph> from the menu.</p>
                </li>
                    <li>
                    <p>From the <ph>Collection</ph> drop-down menu, select the
                        collection to backup. To backup the entire database, select
                        <ph>/db</ph>.</p>
                    <fig>
                            <image href="assets/backup1.png"/>
                        </fig>
                </li>
                    <li>
                    <p>In the <ph>Backup-Directory</ph> field enter where the backup
                        must be stored. Use one of the following:</p>
                    <ul>
                            <li>
                            <p>A full directory path. The backup will be written on the file
                                system</p>
                        </li>
                            <li>
                            <p>The name of a zip file (with a <ph>.zip</ph> extension). The
                                backup will stored as a zip.</p>
                        </li>
                        </ul>
                </li>
                    <li>
                    <p>Click <ph>OK</ph>.</p>
                </li>
                </ol>
        </section>

        <section id="command-line-utility">
                <title>Using the command-line utility</title>
            

            <p>If you are using the command-line utility for the client-side backup/restore, do
                the following:</p>
            <p>To launch the utility, do one of the following:</p>
            <ul>
                    <li>
                    <p>Run either the <ph>bin/backup.sh</ph> (Linux/Unix) or
                        <ph>bin\backup.bat</ph> (Windows) script</p>
                </li>
                    <li>
                    <p>Enter on the command-line (to view the all the available options use the
                        <ph>-h</ph> parameter):</p>
                    <pre>backup.sh --h 

    Usage: backup.sh [Arguments]
    
    Arguments:
    -a, --overwrite-apps                       Overwrite newer applications installed in the database.
                                               Default: disabled
    -b, --backup &lt;string&gt;                      Backup the specified collection.
                                               &lt;string&gt;: any string
                                               Default: 
    -d, --dir &lt;path&gt;                           Specify the directory to use for backups.
                                               &lt;path&gt;: a file path
                                               Default: .
    --deduplicate-blobs &lt;boolean&gt;              Deduplicate BLOBS in the backup.
                                               &lt;boolean&gt;: true or false
                                               Default: false
    -h, --help &lt;argument-to-print-help-for&gt;    &lt;argument-to-print-help-for&gt;: an argument to print help for.
                                               Default: If no specific parameter is given the whole usage text is given
    -o, --option&lt;string&gt;=&lt;string&gt;              Specify extra options: 
                                               property=value. For available properties see client.properties.
                                               &lt;string&gt;=&lt;string&gt;: where "&lt;string&gt;" is any string and "&lt;string&gt;" is any string
                                               Default: Empty map
    -p, --password &lt;string&gt;                    Set the password for connecting to the database.
                                               &lt;string&gt;: any string
                                               Default: 
    -P, --dba-password &lt;string&gt;                If the backup specifies a different password for the admin user, use this option 
                                               to specify the new password. Otherwise you will get a permission denied.
                                               &lt;string&gt;: any string
                                               Default: 
    -q, --quiet                                Be quiet. Just print errors.
                                               Default: disabled
    -r, --restore &lt;path&gt;                       Restore from the specified 'full' backup file in ZIP format, or read the 
                                               specified __contents__.xml file and restore the resources described in there.
                                               &lt;path&gt;: a file path
                                               Default: .
    -R, --rebuild                              Rebuild the EXpath app repository after restore.
                                               Default: disabled
    -S, --use-ssl                              Use SSL by default for remote connections.
                                               Default: disabled
    -u, --user &lt;string&gt;                        Set user.
                                               &lt;string&gt;: any string
                                               Default: admin
    -U, --gui                                  Start in GUI mode.
                                               Default: disabled
</pre>
                    <p>To view the all the available options use the <ph>-h</ph>
                        parameter.</p>
                </li>
                </ul>
            <p>For example, to backup the entire database on a Unix system to the target
                directory <ph>/var/backup/hd060501</ph>, enter the following:</p>
            <pre>bin/backup.sh -u admin -p admin-pass -b /db -d /var/backup/hd060501</pre>

            <p>By default, the utility connects to the database at the URI:
                <ph>xmldb:exist://localhost:8080/exist/xmlrpc</ph>. If you want to backup
                a database at a different location, specify its <ph>XML:DB URI</ph>
                (excluding any collection path) using the <ph>-ouri</ph> parameter. For
                example:</p>

            <pre>bin/backup.sh -u admin -p admin-pass -b /db -d /var/backup/hd060501
                -ouri=xmldb:exist://192.168.1.2:80/xmlrpc</pre>
            <p>Default settings for the user, password or server URIs can also be set in the
                <ph>backup.properties</ph> file.</p>
            <p>If you want to connect to a database using SSL, use the parameter <ph>--use-ssl</ph> or
                (shorter) <ph>-S</ph>:</p>
            <pre>bin/backup.sh -u admin -p admin-pass --use-ssl
                -ouri=xmldb:exist://example.org:443/exist/xmlrpc -b /db
</pre>
            <p>NOTE: You still have to set the correct port (usually port 443), otherwise the client will attempt
                SSL to port 8080.</p>
        </section>

    </section>
        <section id="restore">
            <title>Restoring the Database</title>
        

        <note type="note">
                <p>Restoring from a backup (or parts of it) does not mean that the existing data in
                the current database instance will be deleted entirely. The restore process will
                upload the collections and documents contained in the backup. Collections and
                documents which exist in the database but are not part of the backup will not be
                modified.</p>
                <p>This is a feature, not a bug. It allows us to restore selected parts of the
                database without touching the rest.</p>
                <p>If you really need to restore into a completely clean database, proceed as
                follows:</p>
                <ol>
                    <li>
                    <p>Stop the running eXist database instance</p>
                </li>
                    <li>
                    <p>Change into directory <ph>$EXIST_HOME/data</ph> or
                        another directory you specified as data directory in the configuration
                        (<ph>$EXIST_HOME/etc/conf.xml</ph>).</p>
                </li>
                    <li>
                    <p>Remove all <ph>.dbx</ph>, <ph>.lck</ph> and
                        <ph>.log</ph> files. <b>This means removing all
                        your old data!</b> eXist will recreate those files upon the next
                        restart.</p>
                </li>
                    <li>
                    <p>Start eXist again and launch a restore.</p>
                </li>
                </ol>
            </note>

        <p>To restore the database files from a backup, you can use either the <xref href="java-admin-client" scope="external">Java Admin Client</xref> or the backup command line
            utility.</p>

        <section id="restore-jac">
                <title>Restore using the Java Admin Client</title>
            

            <p>For restoring a backup using the <xref href="java-admin-client" scope="external">Java Admin
                Client</xref>, do the following:</p>
            <ol>
                    <li>
                    <p>Select either the Restore Icon (arrow pointed downward) in the toolbar or
                        <ph>Tools, Restore</ph> from the menu.</p>
                </li>
                    <li>
                    <p>The dialog box will prompt you to select the backup file to restore.
                        Select one of the following: </p>
                    <ul>
                            <li>
                            <p>The descriptor <ph>__contents__.xml</ph> file from the
                                topmost directory (Collection) you want to restore.</p>
                        </li>
                            <li>
                            <p>A zip file containing the backup to restore.</p>
                        </li>
                        </ul>


                </li>
                    <li>
                    <p>A second dialog box will prompt you for an admin password to use for the restore process. You may also request that newer versions of
                        application packages already installed in the database should be overwritten by those contained in the backup. This is normally not
                        desirable, but you can check the checkbox if you still wish to do so.</p>
                </li>
                    <li>
                    <p>If the restore was accepted, a progress dialog box will display the
                        restored files:</p>
                    <fig>
                            <image href="assets/backup3.png"/>
                        </fig>
                </li>
                </ol>

        </section>

        <section id="cli">
                <title>Using the Command Line</title>
            

            <p>To restore from a backup using the command-line utility, please refer to <xref href="#command-line-utility" scope="local"/>. </p>
            <p>Add the <ph>-r</ph> parameter and the full path to the
                <ph>__contents__.xml</ph> file to restore. For example:</p>

            <pre>bin/backup.sh -u admin -p admin-pass -P backup-pass 
                        -r /var/backup/hd060501/db/__contents__.xml</pre>
            <p>The restore process compares app packages contained in the backup with those already installed in the database. By default, older versions of
                an application package contained in the backup will not be restored if a newer version is installed in the database. To change this behavior and
                enforce an overwrite, pass parameter <ph>--overwrite-apps</ph> or <ph>-a</ph>.</p>
        </section>

        <section id="repair">
                <title>Repairing the Package Repository After Restore</title>
            

            <p>After a complete restore, your package repository will probably be out of sync.
                The dashboard will not show all the packages you had previously installed, even
                though their data collections have been restored (into <ph>/db/apps</ph>
                by default). This happens because the package registry, which is stored on the file
                system, is not part of a backup.</p>

            <p>A manual repair step is required to get the package repository in sync again. This
                is implemented as an XQuery module, which you can run (via for instance
                eXide).</p>
            <p>The repair module provides the following interface:</p>
            <dl>
                    <dlentry>
                        <dt>repair:clean-all()</dt>
                        <dd>
                        <p>Unlinks all currently installed packages from the package manager
                            without removing deployed data from the db.</p>
                    </dd>
                    </dlentry>
                    <dlentry>
                        <dt>repair:repair()</dt>
                        <dd>
                        <p>Scan the app root collection in the db for deployed packages and
                            register each of them with the package repository. This reconstructs the
                            metadata for the package. The data stored in the db will not be modified
                            in any way.</p>
                    </dd>
                    </dlentry>
                    <dlentry>
                        <dt>repair:repair($collection)</dt>
                        <dd>
                        <p>Only try to repair the application whose deployed data is stored in
                            the given collection.</p>
                    </dd>
                    </dlentry>
                </dl>
            <p>After a complete restore into a clean database, the clean-all and repair functions
                would typically be combined in the following XQuery:</p>
            <pre outputclass="xquery">xquery version "3.0";

import module namespace repair="http://exist-db.org/xquery/repo/repair" 
at "resource:org/exist/xquery/modules/expathrepo/repair.xql";

repair:clean-all(),
repair:repair()</pre>
        </section>
    </section>
        <section id="emergency-export-tool">
            <title>Emergency Export Tool</title>
        

        <p>eXist-db provides an emergency consistency check and backup utility. This tool is
            typically used if the database becomes corrupt and does not start up anymore.</p>
        <p>This tool needs direct access to the database files, so you must stop the running
            database instance first if it is active.</p>
        <p>Use the following command line to start the utility as a GUI:</p>
        <ul>
                <li>
                <p>Unix/Linux:</p>
                <pre>$EXIST_HOME/bin/export-gui.sh</pre>
            </li>
                <li>
                <p>Windows:</p>
                <pre>$EXIST_HOME\bin\export-gui.bat</pre>
            </li>
            </ul>
        <fig>
                <image href="assets/exportgui.png"/>
            </fig>
        <p>Click <ph>Check</ph> to run a consistency check only. An error
            report will be written into the directory specified in <ph>Output
            Directory</ph>. Click <ph>Check &amp; Export</ph> to run a
            consistency check <i>and</i> a full backup.</p>
        <p>On a headless system use the command-line version instead (The <ph>-h</ph>
            parameter will get you a list of possible options):</p>
        <ul>
                <li>
                <p>Unix/Linux:</p>
                <pre>$EXIST_HOME/bin/export.sh</pre>
            </li>
                <li>
                <p>Windows:</p>
                <pre>$EXIST_HOME\bin\export.bat</pre>
            </li>
            </ul>
    </section>
        <section id="standalone">
            <title>Standalone Backup/Restore on Server Platforms</title>

        

        <p>The instructions above assume you have a standard eXist installation in place, either
            from a release package or a full build environment. That is not always the case on
            remotely deployed server instances.</p>
        <p>To support such deployment scenarios, there is a tool called
            <ph>backrest</ph>, that makes it easier to do restores local to the deployed
            server. The tool can be found in the sub-folder <ph>tools/backrest</ph>. You
            can compile the tool by running <ph>ant</ph> in that folder.</p>

        <p>Building the backrest tool will create a fully self-contained zip file in the
            <ph>tools/backrest/dist/eXist-db-backrest-4.0.0/lib</ph> directory that can be
            copied to a remote server and used to do both restores and backups local to that
            server.</p>
        <p>Just unzip the <ph>backrest</ph> zip file (preserving subdirectories) and execute
            either the <ph>backup.bat</ph> or <ph>backup.sh</ph> scripts per the prior
            instructions and parameter usage. </p>

    </section>
    </body>
</topic>