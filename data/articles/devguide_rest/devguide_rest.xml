<topic id="devguide_rest">
    <title>REST-Style Web API</title>
    <prolog>
        <data name="date" value="2Q19"/>
        <data name="keyword" value="application-development"/>
        <data name="keyword" value="interfaces"/>
    </prolog>
    <body>
        <p>eXist-db provides a REST-style (or <i>RESTful</i>) API through HTTP, which
        provides a simple and quick way to access the database. To use this API, all one needs is an
        HTTP client, which is provided by nearly all programming languages and environments. Or
        simply use a web-browser…</p>
        <section id="rest">
            <title>Introduction</title>
        

        <p>In the standard eXIst-db configuration, the system will listen for REST request
            at:</p>
        <pre>http://localhost:8080/exist/rest/</pre>

        <p>The server treats all HTTP request paths as paths to a database collection (instead of
            the file system). Relative paths are resolved relative to the database root collection.
            For instance:</p>
        <pre>http://localhost:8080/exist/rest/db/shakespeare/plays/hamlet.xml</pre>
        <p>The server will receive an HTTP GET request for the resource
            <ph>hamlet.xml</ph> in the collection
            <ph>/db/shakespeare/plays</ph>. Itr will look for this collection and check if
            the resource is available. If so, it will retrieve its contents and send this back to
            the client. If the document does not exist, an <i>HTTP 404 (Not Found)</i>
            status response will be returned.</p>

        <p>To keep the interface simple, the basic database operations are directly mapped to
            HTTP request methods wherever possible:</p>
        <dl>
                <dlentry>
                    <dt> <ph>GET</ph> </dt>
                    <dd>
                    <p>Retrieves a resource or collection from the database. XQuery and XPath
                        queries may also be specified using GET's optional parameters applied to the
                        selected resource. See <xref href="#get-requests" scope="local"/>.</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt> <ph>PUT</ph> </dt>
                    <dd>
                    <p>Uploads a resource to the database. If required, collections are
                        automatically created and existing resources overwritten. See <xref href="#put-requests" scope="local"/>.</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt> <ph>DELETE</ph> </dt>
                    <dd>
                    <p>Removes a resource (document or collection) from the database. See <xref href="#delete-requests" scope="local"/>.</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt> <ph>POST</ph> </dt>
                    <dd>
                    <p>Submits an XML fragment in the content of the request. This fragment
                        specifies the action to take. The fragment can be either an XUpdate document
                        or a query request. Query requests are used to pass complex XQuery
                        expressions too large to be URL-encoded. See <xref href="#post-requests" scope="local"/>.</p>
                </dd>
                </dlentry>
            </dl>

        <note type="note">
                <p> When running eXist-db as a stand-alone server(when the database has been started
                using the shell-script <ph>bin/server.sh</ph> (Unix) or batch file
                <ph>bin/server.bat</ph> (Windows/DOS)), HTTP access is supported through a
                simple, built-in web server. This web server has limited capabilities, restricted to
                the basic operations defined by eXist's REST API (<ph>GET</ph>,
                <ph>POST</ph>, <ph>PUT</ph> and
                <ph>DELETE</ph>).</p>
                <p>When running in a servlet-context (the usual way of starting eXist-db), this same
                server functionality is provided by the <ph>EXistServlet</ph>.</p>
                <p>Both the stand-alone server and the servlet rely on Java class
                <ph>org.exist.http.RESTServer</ph> to do the actual work.</p>
            </note>
    </section>
        <section id="http-auth">
            <title>HTTP Authentication</title>
        

        <p>Authentication is done through the basic HTTP authentication mechanism so only
            authenticated users can access the database. If no username and password are specified,
            the server assumes a "guest" user identity, which has limited capabilities. If the
            username submitted is not known, or an incorrect password is submitted, an error page
            (<ph>403</ph> - Forbidden) is returned.</p>
    </section>
        <section id="get-requests">
            <title>GET Requests</title>
        

        <p>If the server receives an HTTP <ph>GET</ph> request, it first checks the request
            for known parameters. If no parameters are given or known it will try to locate the
            collection or document specified in the URI database path and return a representation of
            this resource the client.</p>
        <note type="note">
                <p>When the located resource is XML, the returned <i>content-type</i>
                attribute value is <ph>application/xml</ph> and for binary resources
                <ph>application/octet-stream</ph>.</p>
            </note>
        <p>If the path resolves to a database collection, the retrieved results are returned as
            an XML fragment. For example:</p>
        <pre outputclass="xml">&lt;exist:result xmlns:exist="http://exist.sourceforge.net/NS/exist"&gt;
  &lt;exist:collection name="/db/xinclude" owner="guest" group="guest" permissions="rwur-ur-u"&gt;
    &lt;exist:resource name="disclaimer.xml" owner="guest" group="guest" permissions="rwur-ur--"/&gt;
    &lt;exist:resource name="sidebar.xml" owner="guest" group="guest" permissions="rwur-ur--"/&gt;
    &lt;exist:resource name="xinclude.xml" owner="guest" group="guest" permissions="rwur-ur--"/&gt;
  &lt;/exist:collection&gt;
&lt;/exist:result&gt;</pre>

        <p>If an <ph>xml-stylesheet</ph> processing instruction is found in a requested
            XML document, the database will try to apply this stylesheet before returning the
            document. A relative path will be resolved relative to the location of the source
            document. For example, if the document <ph>hamlet.xml</ph>, which is stored in
            collection <ph>/db/shakespeare/plays</ph> contains the XSLT processing
            instruction:</p>
        <pre>&lt;?xml-stylesheet type="application/xml" href="shakes.xsl"?&gt;</pre>
        <p>The database will load the stylesheet from
            <ph>/db/shakespeare/plays/shakes.xsl</ph>.</p>
        <p> <ph>GET</ph> accepts the following optional request parameters (which must be
            URL-encoded):</p>
        <dl>
                <dlentry>
                    <dt> <ph>_xsl=XSL Stylesheet</ph> </dt>
                    <dd>
                    <p>Applies an XSL stylesheet to the requested resource. A relative path is
                        considered relative to the database root collection. This option will
                        override any XSL stylesheet processing instructions found in the source XML
                        file.</p>
                    <p>Setting <ph>_xsl</ph> to <ph>no</ph> disables any
                        stylesheet processing. This is useful for retrieving unprocessed XML from
                        documents that have a stylesheet declaration.</p>
                    <note type="warning">
                            <p>If your document has a valid XSL stylesheet declaration, the web
                            browser may still decide to apply the XSL. In this case, passing
                            <ph>_xsl=no</ph> has no visible effect, though the XSL is now
                            rendered by the browser, <i>not</i> eXist.</p>
                        </note>
                </dd>
                </dlentry>
                <dlentry>
                    <dt> <ph>_query=XPath/XQuery Expression</ph> </dt>
                    <dd>
                    <p>Executes the query specified. The collection or resource referenced in the
                        request path is added to the set of statically known documents for the
                        query. </p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt> <ph>_indent=yes | no</ph> </dt>
                    <dd>
                    <p>Whether to return indented pretty-printed XML. The default value is
                        <ph>yes</ph>.</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt> <ph>_encoding=Character Encoding Type</ph> </dt>
                    <dd>
                    <p>Sets the character encoding for the resulting XML. The default value is
                        <ph>UTF-8</ph>.</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt> <ph>_howmany=Number of Items</ph> </dt>
                    <dd>
                    <p>Specifies the maximum number of items to return from the result sequence.
                        The default value is <ph>10</ph>.</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt> <ph>_start=Starting Position in Sequence</ph> </dt>
                    <dd>
                    <p>Specifies the index position of the first item in the result sequence to
                        return. The default value is <ph>1</ph>.</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt> <ph>_wrap=yes | no</ph> </dt>
                    <dd>
                    <p>Specifies whether the returned query results must be wrapped in a parent
                        <ph>exist:result</ph> element. The default value is
                        <ph>yes</ph>.</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt> <ph>_source=yes | no</ph> </dt>
                    <dd>
                    <p>Specifies whether the query should display its source code instead of
                        being executed. The default value is <ph>no</ph>. See the
                        <ph>allow-source</ph> section in <ph>descriptor.xml</ph> about
                        explicitly allowing this behaviour.</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt> <ph>_cache=yes | no</ph> </dt>
                    <dd>
                    <p>If set to <ph>yes</ph>, the results of the current query are stored in
                        a session on the server. A session id will be returned with the response.
                        Subsequent requests can pass this session id via the
                        <ph>_session</ph> parameter. If the server finds a valid session
                        id, it will return the cached results instead of re-evaluating the query.
                        See below. </p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt> <ph>_session=session id</ph> </dt>
                    <dd>
                    <p>Specifies a session id returned by a previous query request. Query results
                        will be read from the cached session.</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt> <ph>_release=session id</ph> </dt>
                    <dd>
                    <p>Release the session identified by session id.</p>
                </dd>
                </dlentry>
            </dl>
        <p>As an example: The following URI will find all <ph>SPEECH</ph> elements in the
            collection <ph>/db/shakespeare</ph> with "Juliet" as the <ph>SPEAKER</ph>.
            As specified, it will return 5 items from the result sequence, starting at position
            3:</p>
        <pre>http://localhost:8080/exist/rest/db/shakespeare?_query=//SPEECH[SPEAKER=%22JULIET%22]&amp;_start=3&amp;_howmany=5</pre>
    </section>
        <section id="put-requests">
            <title>PUT Requests</title>
        

        <p>Documents can be stored or updated in the database using an HTTP <ph>PUT</ph>
            request. The request URI points to the location where the document must be stored. As
            defined by the HTTP specifications, an existing document at the specified path will be
            updated. Any collections defined in the path that do not exist are created
            automatically.</p>
        <p>For example, the following Python script stores a document (the name is specified on
            the command-line) in the database collection <ph>/db/test</ph>,. This will be
            created if it does not exist. Note that the HTTP header field
            <ph>content-type</ph> is specified as <ph>application/xml</ph>,
            since otherwise the document would be stored as a binary resource.</p>

        <pre outputclass="java">import httplib
import sys
from string import rfind

collection = sys.argv[1]
file = sys.argv[2]

f = open(file, 'r')
print "reading file %s ..." % file
xml = f.read()
f.close()

p = rfind(file, '/')
if p &gt; -1:
    doc = file[p+1:]
else:
    doc = file
print doc
print "storing document to collection %s ..." % collection
con = httplib.HTTP('localhost:8080')
con.putrequest('PUT', '/exist/rest/%s/%s' % (collection, doc))
con.putheader('Content-Type', 'application/xml')
clen = len(xml)
con.putheader('Content-Length', `clen`)
con.endheaders()
con.send(xml)

errcode, errmsg, headers = con.getreply()

if errcode != 200:
    f = con.getfile()
    print 'An error occurred: %s' % errmsg
    f.close()
else:
    print "Ok."</pre>
    </section>
        <section id="delete-requests">
            <title>DELETE Requests</title>
        

        <p> <ph>DELETE</ph> removes a collection or resource from the database.</p>
    </section>
        <section id="post-requests">
            <title>POST Requests</title>
        

        <p> <ph>POST</ph> requests require an XML fragment in the content of the request.
            This fragment specifies the action to take.</p>
        <ul>
                <li>
                <p>If the root node of the fragment uses the XUpdate namespace
                    (<ph>http://www.xmldb.org/xupdate</ph>), the fragment is sent to the
                    XUpdateProcessor to be processed.</p>
            </li>
                <li>
                <p>Otherwise the root node must have the namespace for eXist requests
                    (<ph>http://exist.sourceforge.net/NS/exist</ph>). The fragment is
                    interpreted as an <i>extended query request</i>. Extended query
                    requests can be used to post complex XQuery scripts that are too large to be
                    encoded in a <ph>GET</ph> request.</p>
            </li>
            </ul>


        <p>The structure of the POST XML request is as follows:</p>

        <pre outputclass="xml">&lt;query xmlns="http://exist.sourceforge.net/NS/exist" start="[first item to be returned]" max="[maximum number of items to be returned]" cache="[yes|no: create a session and cache results]" session-id="[session id as returned by previous request]"&gt;
  &lt;text&gt;
    [XQuery expression]
  &lt;/text&gt;
  &lt;properties&gt;
    &lt;property name="[name1]" value="[value1]"/&gt;
  &lt;/properties&gt;
&lt;/query&gt;</pre>
        <p>The root element <ph>query</ph> identifies the fragment as an extended query
            request. The XQuery expression for this request is enclosed in the
            <ph>text</ph> element. The <ph>start</ph>, <ph>max</ph>,
            <ph>cache</ph> and <ph>session-id</ph> attributes have the same meaning as the
            corresponding <ph>GET</ph> parameters (see <xref href="#get-requests" scope="local"/>).</p>
        <note type="note">
                <p>You may have to enclose the XQuery expression in a CDATA section (i.e.
                &lt;![CDATA[ ... ]]&gt;) to avoid parsing errors. </p>
            </note>
        <p>Optional output properties, such as <ph>pretty-print</ph>, can be passed in the
            <ph>properties</ph> element.</p>
        <p> An example of <ph>POST</ph> for Perl is provided below:</p>
        <pre outputclass="java">require LWP::UserAgent;

$URL = 'http://localhost:8080/exist/rest/db/';
$QUERY = &lt;&lt;END;
&lt;?xml version="1.0" encoding="UTF-8"?&gt;
&lt;query xmlns="http://exist.sourceforge.net/NS/exist"
    start="1" max="20"&gt;
    &lt;text&gt;
        for \$speech in //SPEECH[LINE &amp;= 'corrupt*']
        order by \$speech/SPEAKER[1]
        return
            &lt;hit&gt;{\$speech}&lt;/hit&gt;
    &lt;/text&gt;
    &lt;properties&gt;
        &lt;property name="indent" value="yes"/&gt;
    &lt;/properties&gt;
&lt;/query&gt;
END

$ua = LWP::UserAgent-&gt;new();
$req = HTTP::Request-&gt;new(POST =&gt; $URL);
$req-&gt;content_type('application/xml');
$req-&gt;content($QUERY);

$res = $ua-&gt;request($req);
if($res-&gt;is_success) {
    print $res-&gt;content . "\n";
} else {
    print "Error:\n\n" . $res-&gt;status_line . "\n";
}</pre>

        <p>The returned query results are enclosed in an <ph>exist:result</ph> element:</p>
        <pre outputclass="xml">&lt;exist:result xmlns:exist="http://exist.sourceforge.net/NS/exist" hits="2628" start="1" count="10"&gt;
  &lt;SPEECH&gt;
    &lt;SPEAKER&gt;
      BERNARDO
    &lt;/SPEAKER&gt;
    &lt;LINE&gt;
      Who's there?
    &lt;/LINE&gt;
  &lt;/SPEECH&gt;
  ... more items follow ...
&lt;/exist:result&gt;</pre>
    </section>
        <section id="call-xq">
            <title>Calling Stored XQueries</title>
        

        <p>The REST interface supports executing stored XQueries on the server. If the target
            resource of a <ph>GET</ph> or <ph>POST</ph> request is a binary resource with
            the mime-type <ph>application/xquery</ph>, the REST server will try to compile
            and execute it as an XQuery script. The script has access to the entire HTTP context,
            including parameters and session attributes.</p>
        <p>Stored XQueries are a good way to provide dynamic views on data or create small
            services. However, they can do more: because you can also store binary resources like
            images, CSS stylesheets or Javascript files into a database collection, it is entirely
            possible to serve a complex application out of the database. For instance, have a look
            at the example <xref href="https://demo.exist-db.org/exist/apps/demo/examples/web/index.html" scope="external">Using
            XQuery for Web Applications</xref> on the demo server.</p>
    </section>
        <section id="cached-results">
            <title>Cached Query Results</title>
        

        <p>When executing queries using <ph>GET</ph> or <ph>POST</ph>, the server is able
            to cache query results in a server-side session. These results are cached in
            memory.</p>
        <note type="note">
                <p>Memory consumption will be low for query results which reference nodes stored in
                the database and high for nodes constructed within the XQuery itself.</p>
            </note>
        <p>To create a session and store query results, pass <ph>_cache=yes</ph> with a
            <ph>GET</ph> request or set attribute <ph>cache="yes"</ph> within the XML
            payload of a <ph>POST</ph> query request. The server will execute the query as
            usual. If the result sequence contains more than one item, the entire sequence will be
            stored into a newly created session.</p>
        <p>The id of the created session is included in the response. For requests which return a
            <ph>exist:result</ph> wrapper element, the session id will be specified in the
            <ph>exist:session</ph> attribute. The session id is also available in the HTTP
            header <ph>X-Session-Id</ph>. </p>
        <p>The following example shows an example of the HTTP header and <ph>exist:result</ph>
            tag returned by the server:</p>

        <pre>HTTP/1.1 200 OK
Date: Thu, 01 May 2008 16:28:16 GMT
Server: Jetty/5.1.12 (Linux/2.6.22-14-generic i386 java/1.6.0_03
Expires: Thu, 01 Jan 1970 00:00:00 GMT
Last-Modified: Tue, 29 Apr 2008 20:34:33 GMT
X-Session-Id: 2
Content-Type: application/xml; charset=UTF-8
Content-Length: 4699

&lt;exist:result xmlns:exist="http://exist.sourceforge.net/NS/exist" 
    exist:hits="3406" exist:start="1" exist:count="10" 
    exist:session="2"&gt;
...
&lt;/exist:result&gt;</pre>
        <p>The session id can be passed with subsequent requests to retrieve further chunks of
            data without re-evaluating the query. For a <ph>GET</ph> request, pass the session
            id with parameter <ph>_session</ph>. For a <ph>POST</ph> request, add an
            attribute <ph>session="sessionId"</ph> to the XML content of the
            request.</p>
        <p>If the session does not exist or has timed out, the server will re-evaluate the query.
            The timeout is set to 2 minutes.</p>
        <p>A session can be deleted by sending a GET <ph>request</ph> to an arbitrary
            collection URL. Pass the session id in the <ph>_release</ph> parameter:</p>
        <pre>http://localhost:8080/exist/rest/db?_release=0</pre>
    </section>
    </body>
</topic>