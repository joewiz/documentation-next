<topic id="devguide_indexes">
    <title>Developer's Guide to Modularized Indexes</title>
    <prolog>
        <data name="date" value="2Q19"/>
        <data name="keyword" value="java-development"/>
        <data name="keyword" value="indexes"/>
    </prolog>
    <body>
        <p>eXist-db provides a modular mechanism to index data. This eases index development and the
        development of more-or-less related custom functions (in Java).</p>
        <section id="intro">
            <title>Introduction</title>
        
        <p>eXist ships with four index types based on this mechanism:</p>
        <dl>
                <dlentry>
                    <dt>Lucene Range index</dt>
                    <dd>
                    <p>An index providing range and field based searches on an index built with
                        Apache Lucene..</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt>Lucene Full-text index</dt>
                    <dd>
                    <p>A Full text search index which uses Apache Lucene for indexing and
                        query.</p>
                </dd>
                </dlentry>
                <dlentry>
                    <dt>NGram index</dt>
                    <dd>
                    <p>An NGram index will store the N-grams contained in the data's characters.
                        For instance, if the index is configured to index 3-grams,
                        <ph>&lt;data&gt;abcde&lt;/data&gt;</ph> will generate these index
                        entries: </p>
                    <ul>
                            <li>
                            <p>abc</p>
                        </li>
                            <li>
                            <p>bcd</p>
                        </li>
                            <li>
                            <p>cde</p>
                        </li>
                            <li>
                            <p>de␣</p>
                        </li>
                            <li>
                            <p>e␣␣</p>
                        </li>
                        </ul>
                </dd>
                </dlentry>
                <dlentry>
                    <dt>Spatial index</dt>
                    <dd>
                    <p>A spatial index that stores some of the geometric characteristics of <xref href="https://www.opengeospatial.org/standards/gml" scope="external">Geography Markup Language</xref> geometries (tested against GML version
                        2.1.2). For instance:</p>
                    <pre outputclass="xml">&lt;gml:Polygon xmlns:gml="http://www.opengis.net/gml" srsName="osgb:BNG"&gt;
  &lt;gml:outerBoundaryIs&gt;
    &lt;gml:LinearRing&gt;
      &lt;gml:coordinates&gt;
        278515.400,187060.450 278515.150,187057.950 278516.350,187057.150 278546.700,187054.000 278580.550,187050.900 278609.500,187048.100 278609.750,187051.250 278574.750,187054.650 278544.950,187057.450 278515.400,187060.450
      &lt;/gml:coordinates&gt;
    &lt;/gml:LinearRing&gt;
  &lt;/gml:outerBoundaryIs&gt;
&lt;/gml:Polygon&gt;</pre>
                    <p>This will generate several index entries. The most important ones
                        are:</p>
                    <ul>
                            <li>
                            <p>The <xref href="https://en.wikipedia.org/wiki/Spatial_referencing_systems" scope="external">spatial referencing system</xref> (<ph>osgb:BNG</ph> for
                                this polygon)</p>
                        </li>
                            <li>
                            <p>The polygon itself, stored in a binary form (<xref href="https://en.wikipedia.org/wiki/Well-known_text" scope="external">Well-Known Binary</xref>)</p>
                        </li>
                            <li>
                            <p>The coordinates of its <xref href="https://en.wikipedia.org/wiki/Minimum_bounding_rectangle" scope="external">bounding box</xref> </p>
                        </li>
                        </ul>
                </dd>
                </dlentry>
            </dl>

    </section>
        <section id="classes">
            <title>Classes</title>
        
        <p>Below is a technical explanation of the java classes that lets you interact with exist's indexing features.</p>

        <section id="index-manager">
                <title> org.exist.indexing.IndexManager </title>
            

            <p>The indexing architecture introduces a package,
                <ph>org.exist.indexing</ph>, responsible for index management. It is
                created by <ph>org.exist.storage.BrokerPool</ph> which allocates
                <ph>org.exist.storage.DBBroker</ph>s to each process accessing each DB
                instance. Each time a DB instance is created (most installations generally have only
                one, most often called <ph>exist</ph>), the
                <ph>initialize()</ph> method contructs an <ph>IndexManager</ph>
                that will be available through the <ph>getIndexManager()</ph> method of
                <ph>org.exist.storage.BrokerPool</ph>. </p>
            <pre>public IndexManager(BrokerPool pool, Configuration config)</pre>
            <p>This constructor keeps track of the <ph>BrokerPool</ph> that has created
                the instance and receives the database's configuration object, usually defined in an
                XML file called <ph>conf.xml</ph>. This new entry is expected in the
                configuration file: </p>
            <pre outputclass="xml">&lt;modules&gt;
  &lt;module id="ngram-index" class="org.exist.indexing.ngram.NGramIndex" file="ngram.dbx" n="3"/&gt;
  &lt;module id="spatial-index" class="org.exist.indexing.spatial.GMLHSQLIndex" connectionTimeout="100000" flushAfter="300"/&gt;
&lt;/modules&gt;</pre>
            <p>This defines two indexes, backed-up by their specific classes
                (<ph>class</ph> attribute; these classes implement the
                <ph>org.exist.indexing.Index</ph> interface as will be seen below),
                eventually assigns them a human-readable (writable even) identifier and passes them
                custom parameters which are implementation-dependant. Then, it configures (by
                calling their <ph>configure()</ph> method), opens (by calling their
                <ph>open()</ph> method) and keeps track of each of them.</p>
            <p> <ph>org.exist.indexing.IndexManager</ph> also provides these public
                methods:</p>

            <pre>public BrokerPool getBrokerPool()</pre>
            <p>This returns the <ph>org.exist.storage.BrokerPool</ph> for which this
                <ph>IndexManager</ph> was created.</p>
            <pre>public synchronized Index getIndexById(String indexId)</pre>
            <p>A method that returns an <ph>Index</ph> given its class identifier (see
                below). Allows custom functions to access <ph>Index</ph>es whatever their
                human-defined name is. This is probably the only method in this class that will be
                really needed by a developer.</p>
            <pre>public synchronized Index getIndexByName(String indexName)</pre>
            <p>The counterpart of the previous method. Pass the human-readable name of the
                <ph>Index</ph> as defined in the configuration.</p>
            <pre>public void shutdown()</pre>
            <p>This method is called when eXist shuts down. <ph>close()</ph> will be
                called for every registered <ph>Index</ph>. That allows them to free the
                resources they have allocated.</p>
            <pre>public void removeIndexes()</pre>
            <p>This method is called when <ph>repair()</ph> is called from
                <ph>org.exist.storage.NativeBroker</ph>.</p>
            <note type="note">
                    <p> <ph>repair()</ph> reconstructs every index (including the
                    structural one) from what is contained in the persistent DOM (usually
                    <ph>dom.dbx</ph>).</p>
                </note>
            <p> <ph>remove()</ph> will be called for every registered
                <ph>Index</ph>. That allows each index to destroy its persistent storage
                if it wants to do so (but it is probably suitable given that
                <ph>repair()</ph> is called when the DB and/or its indexes are
                corrupted).</p>
            <pre>public void reopenIndexes()</pre>
            <p>This method is called when <ph>repair()</ph> is called from
                <ph>org.exist.storage.NativeBroker</ph>.</p>
            <p> <ph>open()</ph> will be called for every registered
                <ph>Index</ph>. That allows each index to (re)allocate the resources it
                needs for its persistent storage.</p>
        </section>

        

        <section id="index-controller">
                <title> org.exist.indexing.IndexController </title>
            

            <p>Another important class is <ph>org.exist.indexing.IndexController</ph>
                which, as its name suggests, controls the way data to be indexed are dispatched to
                the registered indexes, using <ph>org.exist.indexing.IndexWorker</ph>s
                that will be described below. Each <ph>org.exist.storage.DBBroker</ph>
                constructs such an <ph>IndexController</ph> when it is itself constructed,
                using this constructor:</p>
            <pre>public IndexController(DBBroker broker)</pre>
            <p>This registers the <ph>broker</ph>'s <ph>IndexWorker</ph>s,
                once for each registered <ph>Index</ph>. These
                <ph>IndexWorker</ph>s, that will be described below, are returned by the
                <ph>getWorker()</ph> method in
                <ph>org.exist.indexing.Index</ph>, which is usually a good place to create
                such an <ph>IndexWorker</ph>, at least the first time it is called.</p>
            <p>This <ph>IndexController</ph> will be available through the
                <ph>getIndexController()</ph> method of
                <ph>org.exist.storage.DBBroker</ph>.</p>
            <p> Here are the other public methods:</p>
            <pre>public Map configure(NodeList configNodes, Map namespaces)</pre>
            <p>This method receives the database's configuration object, usually defined in an
                XML file called <ph>conf.xml</ph>. Both configuration nodes and namespaces
                (remember that some configuration settings including e.g. pathes need namespaces to
                be defined) will be passed to the <ph>configure()</ph> method of each
                <ph>IndexWorker</ph>. The returned object is a
                <ph>java.util.Map</ph> that will be available from
                <ph>collection.getIndexConfiguration(broker).getCustomIndexSpec(INDEX_CLASS_IDENTIFIER)</ph>. </p>
            <pre>public IndexWorker getWorkerByIndexId(String indexId)</pre>
            <p>A method that returns an <ph>IndexWorker</ph> given the class identifier
                of its associated <ph>Index</ph> identifier. Very useful to the developer
                since it allows custom functions to access <ph>IndexWorker</ph>s whatever
                the human-defined name of their <ph>Index</ph> is. This is probably the
                only method in this class that will be really needed by a developer.</p>
            <pre>public IndexWorker getWorkerByIndexName(String indexName)</pre>
            <p>The counterpart of the previous method. For the human-readable name of the
                <ph>Index</ph> as defined in the configuration.</p>
            <pre>public void setDocument(DocumentImpl doc)</pre>
            <p>This method sets the <ph>org.exist.dom.DocumentImpl</ph> on which the
                <ph>IndexWorker</ph>s shall work. Calls
                <ph>setDocument(doc)</ph> on each registered
                <ph>IndexWorker</ph>.</p>
            <pre>public void setMode(int mode)</pre>
            <p>This method sets the operating mode in which the <ph>IndexWorker</ph>s
                shall work. See below for further details on operating modes. Calls
                <ph>setMode(mode)</ph> on each registered
                <ph>IndexWorker</ph>.</p>
            <pre>public void setDocument(DocumentImpl doc, int mode)</pre>
            <p>A convenience method that sets both the
                <ph>org.exist.dom.DocumentImpl</ph> and the operating mode. Calls
                <ph>setDocument(doc, mode)</ph> on each registered
                <ph>IndexWorker</ph>.</p>
            <pre>public DocumentImpl getDocument()</pre>
            <p>Returns the <ph>org.exist.dom.DocumentImpl</ph> on which the
                <ph>IndexWorker</ph>s will have to work.</p>
            <pre>public int getMode()</pre>
            <p>Returns the operating mode in which the <ph>IndexWorker</ph>s will have
                to work.</p>
            <pre>public void flush()</pre>
            <p>Called in various places when pending operations, obviously data insertion, update
                or removal, have to be completed. Calls <ph>flush()</ph> on each
                registered <ph>IndexWorker</ph>.</p>
            <pre>public void removeCollection(Collection collection, DBBroker broker)</pre>
            <p>Called when a collection is to be removed. That allows to delete index entries for
                this collection in a single operation. Calls <ph>removeCollection()</ph>
                on each registered <ph>IndexWorker</ph>.</p>
            <pre>public void reindex(Txn transaction, StoredNode reindexRoot, int mode)</pre>
            <p>Called when a document is to be reindexed. Only the <ph>reindexRoot</ph>
                node and its descendants will have their index entries updated or removed depending
                of the <ph>mode</ph> parameter.</p>
            <pre>public StoredNode getReindexRoot(StoredNode node, NodePath path)</pre>
            <p>Determines the node which should be reindexed together with its descendants. Calls
                <ph>getReindexRoot()</ph> on each registered
                <ph>IndexWorker</ph>. The top-most node will be the actual node from which
                the <ph>DBBroker</ph> will start reindexing.</p>
            <pre>public StoredNode getReindexRoot(StoredNode node, NodePath path, boolean includeSelf)</pre>
            <p>Same as above, with more parameters.</p>
            <pre>public StreamListener getStreamListener()</pre>
            <p>Returns the first <ph>org.exist.indexing.StreamListener</ph> in the
                <ph>StreamListener</ph>s pipeline. There is at most one
                <ph>StreamListener</ph> per <ph>IndexWorker</ph> that will
                intercept the (re)indexed nodes stream. <ph>IndexWorker</ph>s that are not
                interested in the data (depending of e.g. the document and/or the operating mode)
                may return <ph>null</ph> through their <ph>getListener()</ph>
                method and thus not participate in the (re)indexing process. In other terms, they
                will not listen to the indexed nodes.</p>
            <pre>public void indexNode(Txn transaction, StoredNode node, NodePath path, StreamListener listener)</pre>
            <p>Index any kind of indexable node (currently elements, attributes and text nodes ;
                comments and especially processing instructions might be considered in the
                future).</p>
            <pre>public void startElement(Txn transaction, ElementImpl node, NodePath path, StreamListener listener)</pre>
            <p>More specific than <ph>indexNode()</ph>. For an element. Will call
                <ph>startElement()</ph> on <ph>listener</ph> if it is not
                <ph>null</ph>. This is similar to Java SAX events.</p>
            <pre>public void attribute(Txn transaction, AttrImpl node, NodePath path, StreamListener listener)</pre>
            <p>More specific than <ph>indexNode()</ph>. For an attribute. Will call
                <ph>attribute()</ph> on <ph>listener</ph> if it is not
                <ph>null</ph>.</p>
            <pre>public void characters(Txn transaction, TextImpl node, NodePath path, StreamListener listener)</pre>
            <p>More specific than <ph>indexNode()</ph>. For a text node. Will call
                <ph>characters()</ph> on <ph>listener</ph> if it is not
                <ph>null</ph>.</p>
            <pre>public void endElement(Txn transaction, ElementImpl node, NodePath path, StreamListener listener)</pre>
            <p>Signals end of indexing for an element node. Will call
                <ph>endElement()</ph> on <ph>listener</ph> if it is not
                <ph>null</ph> </p>
            <pre>public MatchListener getMatchListener(NodeProxy proxy)</pre>
            <p>Returns a <ph>org.exist.indexing.MatchListener</ph> for the given
                node.</p>
            <p>The two classes aim to be essentially used by eXist itself. As a programmer you
                will probably need to use just one or two of the above methods.</p>
        </section>

        

        <section id="abstract-indexes">
                <title> org.exist.indexing.Index and org.exist.indexing.AbstractIndex </title>
            

            <p>Now let's get into the interfaces and classes that will need to be extended by the
                index programmer. The first of them is the interface
                <ph>org.exist.indexing.Index</ph> which will maintain the index
                itself.</p>
            <p>As described above, a new instance of the interface will be created by the
                constructor of <ph>org.exist.indexing.IndexManager</ph> which calls the
                interface's <ph>newInstance()</ph> method. No need for a constructor
                then.</p>
            <p>Here are the methods that have to be implemented in an implementation: </p>
            <pre>String getIndexId()</pre>
            <p>Returns the class identifier of the index.</p>
            <pre>String getIndexName()</pre>
            <p>Returns the human-defined name of the index, if one was defined in the
                configuration file.</p>
            <pre>BrokerPool getBrokerPool()</pre>
            <p>Returns the <ph>org.exist.storage.BrokerPool</ph> that has created the
                index.</p>
            <pre>void configure(BrokerPool pool, String dataDir, Element config)</pre>
            <p>Notifies the <ph>Index</ph> a data directory (normally
                <ph>${EXIST_HOME}/data</ph>) and the configuration element in which it is
                declared.</p>
            <pre>void open()</pre>
            <p>Method that is executed when the <ph>Index</ph> is opened, whatever it
                means. Consider this method as an initialization and allocate the necessary
                resources here.</p>
            <pre>void close()</pre>
            <p>Method that is executed when the <ph>Index</ph> is closed, whatever it
                means. Consider this method as a finalization and free the allocated resources
                here.</p>
            <pre>void sync()</pre>
            <p>Unused.</p>
            <pre>void remove()</pre>
            <p>Method that is executed when eXist requires the index content to be entitrely
                deleted, e.g. before repairing a corrupted database.</p>
            <pre>IndexWorker getWorker(DBBroker broker)</pre>
            <p>Returns the <ph>IndexWorker</ph> that operates on this
                <ph>Index</ph> on behalf of <ph>broker</ph>. One may want to
                create a new <ph>IndexWorker</ph> here or pick one form a pool.</p>
            <pre>boolean checkIndex(DBBroker broker)</pre>
            <p>To be called by applications that want to implement a consistency check on the
                <ph>Index</ph>.</p>
            <p>There is also an abstract class that implements
                <ph>org.exist.indexing.Index</ph>,
                <ph>org.exist.indexing.AbstractIndex</ph> that can be used a a basis for
                most <ph>Index</ph> implementations. Most of its methods are abstract and
                still have to be implemented in the concrete classes. These are the few concrete
                methods:</p>
            <pre>public String getDataDir()</pre>
            <p>Returns the directory in which this <ph>Index</ph> operates. Usually
                defined by <ph>configure()</ph> which itself receives eXist's
                configuration settings.</p>
            <note type="note">
                    <p>There might be some <ph>Index</ph>es for which the concept of data
                    directory isn't accurate.</p>
                </note>
            <pre>public void configure(BrokerPool pool, String dataDir, Element config)</pre>
            <p>Its minimal implementation retains the
                <ph>org.exist.storage.BrokerPool</ph>, the data directory and the
                human-defined name, if defined in the configuration file (in an attribute called
                <ph>id</ph>). Sub-classes may call <ph>super.configure()</ph> to
                retain this default behaviour.</p>
            <p>This member is protected:</p>
            <pre>protected static String ID = "Give me an ID !"</pre>
            <p>This is where the class identifier of the <ph>Index</ph> is defined.
                Override this member with, say, <ph>MyClass.class.getName()</ph> to
                provide a reasonably unique identifier within your system.</p>
        </section>

        

        <section id="index-worker">
                <title> org.exist.indexing.IndexWorker </title>
            

            <p>The next important interface that will need to be implemented is
                <ph>org.exist.indexing.IndexWorker</ph> which is responsible for managing
                the data in the index. Remember that each
                <ph>org.exist.storage.DBBroker</ph> will have such an
                <ph>IndexWorker</ph> at its disposal and that their
                <ph>IndexController</ph> will know what method of
                <ph>IndexWorker</ph> to call and when to call it.</p>
            <p>Here are the methods that have to be implemented in the concrete implementations: </p>
            <pre>public String getIndexId()</pre>
            <p>Returns the class identifier of the index.</p>
            <pre>public String getIndexName()</pre>
            <p>Returns the human-defined name of the index, if one was defined in the
                configuration file.</p>
            <pre>Object configure(IndexController controller, NodeList configNodes, Map namespaces)</pre>
            <p>This method receives the database's configuration object, usually defined in an
                XML file called <ph>conf.xml</ph>. Both configuration nodes and namespaces
                (remember that some configuration settings including e.g. pathes need namespaces to
                be defined) will be passed to the <ph>configure()</ph> method of the
                <ph>IndexWorker</ph>'s <ph>IndexController</ph>. The
                <ph>IndexWorker</ph> can use this method to retain custom configuration
                options in a custom object that will be available in the
                <ph>java.util.Map</ph> returned by
                <ph>collection.getIndexConfiguration(broker).getCustomIndexSpec(INDEX_CLASS_IDENTIFIER)</ph>.
                The return type is free but will probably generally be an implementation of
                <ph>java.util.Collection</ph> in order to retain several parameters. </p>
            <pre>void setDocument(DocumentImpl doc)</pre>
            <p>This method sets the <ph>org.exist.dom.DocumentImpl</ph> on which this
                <ph>IndexWorker</ph> will have to work.</p>
            <pre>void setMode(int mode)</pre>
            <p>This method sets the operating mode in which this <ph>IndexWorker</ph>
                will have to work. See below for further details on operating modes.</p>
            <pre>void setDocument(DocumentImpl doc, int mode)</pre>
            <p>A convenience method that sets both the
                <ph>org.exist.dom.DocumentImpl</ph> and the operating mode.</p>
            <pre>DocumentImpl getDocument()</pre>
            <p>Returns the <ph>org.exist.dom.DocumentImpl</ph> on which this
                <ph>IndexWorker</ph> will have to work.</p>
            <pre>int getMode()</pre>
            <p>Returns the operating mode in which this <ph>IndexWorker</ph> will have
                to work.</p>
            <pre>void flush()</pre>
            <p>Called periodically by the <ph>IndexController</ph> or by any other
                process. That is where data insertion, update or removal should actually take
                place.</p>
            <pre>void removeCollection(Collection collection, DBBroker broker)</pre>
            <p>Called when a collection is to be removed. That allows to delete index entries for
                this collection in a single operation without a need for a
                <ph>StreamListener</ph> (see below) or a call to
                <ph>setMode()</ph> nor <ph>setDocument()</ph>.</p>
            <pre>StoredNode getReindexRoot(StoredNode node, NodePath path, boolean includeSelf)</pre>
            <p>Determines the node which should be reindexed together with its descendants. This
                will give a hint to the <ph>IndexController</ph> to determine from which
                node reindexing should start.</p>
            <pre>StreamListener getListener()</pre>
            <p>Returns a <ph>StreamListener</ph> that will intercept the (re)indexed
                nodes stream. <ph>IndexWorker</ph>s that are not interested in the data
                (depending of e.g. the document and/or the operating mode) may return
                <ph>null</ph> here.</p>
            <pre>MatchListener getMatchListener(NodeProxy proxy)</pre>
            <p>Returns a <ph>org.exist.indexing.MatchListener</ph> for the given
                node.</p>
            <pre>boolean checkIndex(DBBroker broker)</pre>
            <p>To be called by applications that want to implement a consistency check on the
                index.</p>
            <pre>Occurrences[] scanIndex(DocumentSet docs)</pre>
            <p>Returns an array of <ph>org.exist.dom.DocumentImpl.Occurrences</ph> that
                is an <i>ordered</i> list of the index entries, in a textual form,
                associated with the number of occurences for the entries and a list of the documents
                containing them.</p>
            <note type="note">
                    <p>For some indexes, the concept of ordered or textual occurrences might not be
                    meaningful.</p>
                </note>
        </section>

        

        <section id="abstract-stream-listener">
                <title> org.exist.indexing.StreamListener and org.exist.indexing.AbstractStreamListener </title>
            

            <p>The interface <ph>org.exist.indexing.StreamListener</ph> has these
                public members:</p>
            <pre>public final static int UNKNOWN = -1;</pre>
            <pre>public final static int STORE = 0;</pre>
            <pre>public final static int REMOVE_ALL_NODES = 1;</pre>
            <pre>public final static int REMOVE_SOME_NODES = 2;</pre>
            <p>Obviously, they are used by the <ph>setMode()</ph> method in
                <ph>org.exist.indexing.IndexController</ph> which is istself called by the
                different <ph>org.exist.storage.DBBroker</ph>s when they have to (re)index
                a node and its descendants. As their name suggests, there is a mode for storing
                nodes and two modes for removing them from the indexes. The difference between
                <ph>StreamListener.REMOVE_ALL_NODES</ph> and
                <ph>StreamListener.REMOVE_SOME_NODES</ph> is that the former removes all
                the nodes from a document whereas the latter removes only some nodes from a
                document, usually the descendants of the node returned by
                <ph>getReindexRoot()</ph>. We thus have the opportunity to trigger a
                process that will directly remove all the nodes from a given document without having
                to listen to each of them. Such a technique is described below.</p>
            <p>Here are the methods that must be implement by an implemetation:</p>
            <pre>IndexWorker getWorker()</pre>
            <p>Returns the <ph>IndexWorker</ph> that owns this
                <ph>StreamListener</ph>.</p>
            <pre>void setNextInChain(StreamListener listener);</pre>
            <p>Should not be used. Used to specify which is the next
                <ph>StreamListener</ph> in the <ph>IndexController</ph>'s
                <ph>StreamListener</ph>s pipeline.</p>
            <pre>StreamListener getNextInChain();</pre>
            <p>Returns the next <ph>StreamListener</ph> in the
                <ph>IndexController</ph>'s <ph>StreamListener</ph>s pipeline.
                Very important because it is the responsability of the
                <ph>StreamListener</ph> to forward the event stream to the next
                <ph>StreamListener</ph> in the pipeline.</p>
            <pre>void startElement(Txn transaction, ElementImpl element, NodePath path)</pre>
            <p>Signals the start of an element to the listener.</p>
            <pre>void attribute(Txn transaction, AttrImpl attrib, NodePath path)</pre>
            <p>Passes an attribute to the listener.</p>
            <pre>void characters(Txn transaction, TextImpl text, NodePath path)</pre>
            <p>Passes some character data to the listener.</p>
            <pre>void endElement(Txn transaction, ElementImpl element, NodePath path)</pre>
            <p>Signals the end of an element to the listener. Allow to free any temporary
                resource created since the matching <ph>startElement()</ph> has been
                called.</p>
            <p>Beside the <ph>StreamListener</ph> interface, each custom listener
                should extend <ph>org.exist.indexing.AbstractStreamListener</ph>.</p>
            <p>This abstract class provides concrete implementations for
                <ph>setNextInChain()</ph> and <ph>getNextInChain()</ph> that
                should normally never be overridden.</p>
            <p>It also provides dummy <ph>startElement()</ph>,
                <ph>attribute()</ph>, <ph>characters()</ph>,
                <ph>endElement()</ph> methods that do nothing but forwarding the node to
                the next <ph>StreamListener</ph> in the
                <ph>IndexController</ph>'s <ph>StreamListener</ph>s
                pipeline.</p>
            <pre>public abstract IndexWorker getWorker()</pre>
            <p>These remain abstract, since we still can not know what
                <ph>IndexWorker</ph> will own the <ph>Listener</ph> until we
                have a concrete implementation.</p>
        </section>
    </section>
        <section id="spatial">
            <title>Use case: developing an indexing architecture for GML geometries</title>
        


        <p>To demonstrate how modular eXist <ph>Index</ph>es are, we have decided to
            show how a spatial <ph>Index</ph> could be implemented. What makes its design
            interesting is that this kind of <ph>Index</ph> doesn't store character data
            from the document, nor does it use a <ph>org.exist.storage.index.BFile</ph> to
            store the index entries. Instead, we will store WKB index entries in a JDBC database,
            namely a <xref href="http://hsqldb.org/" scope="external">HSQLDB</xref> to keep the distribution as
            light as possible and reduce the number of external dependencies, but it wouldn't be too
            difficult to use another one like <xref href="http://postgis.refractions.net/" scope="external">PostGIS</xref> given that the implementation has itself been designed in a quite
            modular way. </p>
        <p>In eXist's Git repository, the modularized <ph>Index</ph>es code is in
            <ph>extensions/indexes</ph> and the file system's architecture is designed to
            follow eXist's core architecture, i.e. <ph>org.exist.indexing.*</ph> for the
            <ph>Index</ph>es and <ph>org.exist.xquery.*</ph> for their
            associated <ph>Module</ph>s. There is also a dedicated location for required
            external libraries and for the test cases. The build system should normally be able to
            download the required libraries and build all the files automatically, and even launch
            the tests provided that the DB's configuration file declares the
            <ph>Index</ph>es (see above) and their associated <ph>Module</ph>s
            (see below).</p>
        <p>The described spatial <ph>Index</ph> heavily relies on the excellent open
            source librairies provided by the <xref href="http://geotools.codehaus.org/" scope="external">Geotools</xref> project. We have experienced a few problems that will be mentioned
            further, but since feedback has been provided, the situation will unquestionably improve
            in the future, making current workarounds redundant.</p>
        <p>The <ph>Index</ph> has been tested with only one file which is available
            from the <xref href="http://www.ordnancesurvey.co.uk/oswebsite/" scope="external">Ordnance Survey
            of Great-Britain</xref>, a topography layer of Port-Talbot, which is available as <xref href="http://www.ordnancesurvey.co.uk/oswebsite/products/try-now/sample-data.html" scope="external">sample data</xref>.</p>

        

        <section id="abstract-index">
                <title>Writing the concrete implementation of org.exist.indexing.AbstractIndex </title>
            

            <p>Well, in fact we will start by writing an abstract implementation first. As said
                above, we have planned a modular JDBC spatial <ph>Index</ph>, which will
                be abstract, and that will be extended by a concrete HSQLDB
                <ph>Index</ph>.</p>
            <p>Let's start with this:</p>
            <pre>package org.exist.indexing.spatial;			
	
	public abstract class AbstractGMLJDBCIndex extends AbstractIndex {

	public final static String ID = AbstractGMLJDBCIndex.class.getName();	
	private final static Logger LOG = Logger.getLogger(AbstractGMLJDBCIndex.class);
	protected HashMap workers = new HashMap();
	protected Connection conn = null;
	
	}</pre>
            <p>Here we define an abstract class that extends
                <ph>org.exist.indexing.AbstractIndex</ph> and thus implements
                <ph>org.exist.indexing.Index</ph>. We also define a few members like
                <ph>ID</ph> that will be returned by the unoverriden
                <ph>getIndexId()</ph> from
                <ph>org.exist.indexing.AbstractIndex</ph>, a <ph>Logger</ph>, a
                <ph>java.util.HashMap</ph> that will be a "pool" of
                <ph>IndexWorker</ph>s (one for each
                <ph>org.exist.storage.DBBroker</ph>) and a
                <ph>java.sql.Connection</ph> that will handle the database operations at
                the index level. </p>
            <p>Let' now introduce this general purpose interface:</p>
            <pre>public interface SpatialOperator { 
	  public static int UNKNOWN = -1;
	  public static int EQUALS = 1;
	  public static int DISJOINT = 2;
	  public static int INTERSECTS = 3;
	  public static int TOUCHES = 4;
	  public static int CROSSES = 5;
	  public static int WITHIN = 6;
	  public static int CONTAINS = 7;
	  public static int OVERLAPS = 8;
	}</pre>
            <p>This defines the spatial operators that will be used by spatial queries. For more
                information about the semantics, see the <xref href="http://www.vividsolutions.com/jts/bin/JTS%20Technical%20Specs.pdf" scope="external">JTS
                documentation</xref> (chapter 11). We will use this wonderful library everytime a
                spatial computation is required. So does the Geotools project by the way.</p>
            <p>Here are a few concrete methods that should be usable by any JDBC-enabled
                database:</p>
            <pre>public AbstractGMLJDBCIndex() {    	
	}  
	
	public void configure(BrokerPool pool, String dataDir, Element config) throws DatabaseConfigurationException {        
	  super.configure(pool, dataDir, config);
	  try {
	    checkDatabase();
	  } catch (ClassNotFoundException e) {
	  throw new DatabaseConfigurationException(e.getMessage()); 
	  } catch (SQLException e) {
	  throw new DatabaseConfigurationException(e.getMessage()); 
	  }
	}

	public void open() throws DatabaseConfigurationException {     
	}

	public void close() throws DBException {
	  Iterator i = workers.values().iterator();
	  while (i.hasNext()) {
	    AbstractGMLJDBCIndexWorker worker = (AbstractGMLJDBCIndexWorker)i.next();		
	    worker.flush();		
	    worker.setDocument(null, StreamListener.UNKNOWN);
	  }
	  shutdownDatabase();
	}

	public void sync() throws DBException {
	}
	
	public void remove() throws DBException {
	  Iterator i = workers.values().iterator();
	  while (i.hasNext()) {
	    AbstractGMLJDBCIndexWorker worker = (AbstractGMLJDBCIndexWorker)i.next();		
	    worker.flush();		
	    worker.setDocument(null, StreamListener.UNKNOWN);
	  }
	  removeIndexContent();
	  shutdownDatabase();
	  deleteDatabase();	
	}			

	public boolean checkIndex(DBBroker broker) {
	  return getWorker(broker).checkIndex(broker);
	}</pre>
            <p>First, an empty constructor, not even necessary since the <ph>Index</ph>
                is created through the <ph>newInstance()</ph> method of its interface (see
                above).</p>
            <p>Then, a configuration method that calls its ancestor, whose behaviour fullfills
                our needs. This method calls a <ph>checkDatabase()</ph> method whose
                semantics will be dependant of the underlying DB. The basic idea is to prevent eXist
                to continue its initialization if there is a problem with the DB.</p>
            <p>Then we will do nothing during <ph>open()</ph>. No need to open a
                database, which is costly, if we dont need it.</p>
            <p>The <ph>close()</ph> will flush any pending operation currently queued
                by the <ph>IndexWorker</ph>s and resets their state in order to prevent
                them to start any further operation, which should never be possible if eXist is
                their only user. Then it will call a <ph>shutdownDatabase()</ph> method
                whose semantics will be dependant of the underlying DB. They can be fairly simple
                for DBs that shut down automatically when the virtual machine shuts down.</p>
            <p>The <ph>sync()</ph> is never called by eXist. It's here to make the
                interface happy.</p>
            <p>The <ph>remove()</ph> method is similar to <ph>close()</ph>.
                It then calls two database-dependant methods that are pretty redundant.
                <ph>deleteDatabase()</ph> will probably not be able to do what its name
                suggests if eXist doesn't own the admin rights. Conversely,
                <ph>removeIndexContent()</ph> wiould probably have nothing to do if eXist
                owns the admin rights since physically destroying the DB would probably be more
                efficient than deleting table contents.</p>
            <p> <ph>checkIndex()</ph> will delegate the task to the
                <ph>broker</ph>'s <ph>IndexWorker</ph>.</p>
            <p>The remaining methods are DB-dependant and thus abstract:</p>
            <pre>public abstract IndexWorker getWorker(DBBroker broker);
	protected abstract void checkDatabase() throws ClassNotFoundException, SQLException;
	protected abstract void shutdownDatabase() throws DBException;
	protected abstract void deleteDatabase() throws DBException;
	protected abstract void removeIndexContent() throws DBException;
	protected abstract Connection acquireConnection(DBBroker broker) throws SQLException;   
	protected abstract void releaseConnection(DBBroker broker) throws SQLException;</pre>
            <p>Let's see now how a HSQL-dependant implementation would be going by describing the
                concrete class:</p>
            <pre>package org.exist.indexing.spatial;

	public class GMLHSQLIndex extends AbstractGMLJDBCIndex {
	
	  private final static Logger LOG = Logger.getLogger(GMLHSQLIndex.class);
	  public static String db_file_name_prefix = "spatial_index";
	  public static String TABLE_NAME = "SPATIAL_INDEX_V1";
	  private DBBroker connectionOwner = null;
	  private long connectionTimeout = 100000L;
	
	  public GMLHSQLIndex() {    	
	  } 
	
	}</pre>
            <p>Of course, we extend
                <ph>org.exist.indexing.spatial.AbstractGMLJDBCIndex</ph>, then a few
                members are defined: a <ph>Logger</ph>, a file prefix (which will be
                required by the files required by HSQLDB storage, namely
                <ph>spatial_index.lck</ph>, <ph>spatial_index.log</ph>,
                <ph>spatial_index.script</ph> and
                <ph>spatial_index.properties</ph>), then a table name in which the spatial
                index data will be stored, then a variable that will hold the
                <ph>org.exist.storage.DBBroker</ph> that currently holds a connection to
                the DB (we could have used an <ph>IndexWorker</ph> here, given their 1:1
                relationship). The problem is that we run HSQLDB in embedded mode and that only one
                connection is available at a given time.</p>
            <p>A more elaborated DBMS, or HSQLDB running in server mode would permit the
                allocation of one connection per <ph>IndexWorker</ph>, but we have chosen
                to keep things simple for now. Indeed, if <ph>IndexWorker</ph>s are
                thread-safe (because each <ph>org.exist.storage.DBBroker</ph> operates
                within its own thread), a single connection will have to be controlled by the
                <ph>Index</ph> which is controlled by the
                <ph>org.exist.storage.BrokerPool</ph>. See below how we will handle
                concurrency, given such perequisites.</p>
            <p>The last member is the timeout when a <ph>Connection</ph> to the DB is
                requested.</p>
            <p>As we can see, we have an empty constructor again.</p>
            <p>The next method calls its ancestor's <ph>configure()</ph> method and
                just retains the content of the <ph>connectionTimeout</ph> attribute as
                defined in the configuration file.</p>
            <pre>public void configure(BrokerPool pool, String dataDir, Element config) 
	  throws DatabaseConfigurationException {
	  super.configure(pool, dataDir, config);
	  String param = ((Element)config).getAttribute("connectionTimeout");
	  if (param != null) {
	    try {
	    connectionTimeout = Long.parseLong(param);
	    } catch (NumberFormatException e) {
	    LOG.error("Invalid value for 'connectionTimeout'", e);
	    }
	  }    	
	}</pre>
            <p>The next method is also quite straightforward:</p>
            <pre>public IndexWorker getWorker(DBBroker broker) {
	  GMLHSQLIndexWorker worker = (GMLHSQLIndexWorker)workers.get(broker);
	  if (worker == null) {
	    worker = new GMLHSQLIndexWorker(this, broker);
	    workers.put(broker, worker);
	  }
	  return worker;
	}</pre>
            <p>This picks an <ph>IndexWorker</ph> (more precisely a
                <ph>org.exist.indexing.spatial.GMLHSQLIndexWorker</ph> that will be
                described below) for the given <ph>broker</ph> from the "pool". If needed,
                namely the first time the method is called with with parameter, it creates one.
                Notice that this <ph>IndexWorker</ph> is DB-dependant. It will be
                described below.</p>
            <p>Then come a few general-purpose methods:</p>
            <pre>protected void checkDatabase() 
	  throws ClassNotFoundException, SQLException {	
	  Class.forName("org.hsqldb.jdbcDriver");		
	}
	
	protected void shutdownDatabase()
	  throws DBException {
	  try {
	    if (conn != null) {
	    Statement stmt = conn.createStatement();				
	    stmt.executeQuery("SHUTDOWN");
	    stmt.close();
	    conn.close();				
	    if (LOG.isDebugEnabled()) 
	      LOG.debug("GML index: " + getDataDir() + "/" + db_file_name_prefix + " closed");
	    }
	  } catch (SQLException e) {
	    throw new DBException(e.getMessage()); 
	  } finally {
	  conn = null;
	  }
	}
	
	protected void deleteDatabase()
	  throws DBException {
	  File directory = new File(getDataDir());
	  File[] files = directory.listFiles( 
	    new FilenameFilter() {
              public boolean accept(File dir, String name) {
	        return name.startsWith(db_file_name_prefix);
	      }
	    }
	  );
	  boolean deleted = true;
	  for (int i = 0; i &lt; files.length ; i++) {
  	    deleted &amp;= files[i].delete();
	  }
	}
	
	protected void removeIndexContent()
          throws DBException {
	  try {
	    //Let's be lazy here : we only delete th index content if we have a connection
	    if (conn != null) {
	    Statement stmt = conn.createStatement(); 
	    int nodeCount = stmt.executeUpdate("DELETE FROM " + GMLHSQLIndex.TABLE_NAME + ";");       
	    stmt.close();
	    if (LOG.isDebugEnabled()) 
 	      LOG.debug("GML index: " + getDataDir() + "/" + db_file_name_prefix + ". " + 
	      nodeCount + " nodes removed");
	    }		
	  } catch (SQLException e) {
	    throw new DBException(e.getMessage()); 
	  }
	}</pre>
            <p> <ph>checkDatabase()</ph> just checks that we have a suitable driver in
                the CLASSPATH. We don't want to open the database right now. It costs too
                much.</p>
            <p> <ph>shutdownDatabase()</ph> is just one of the many ways to shutdown a
                HSQLDB.</p>
            <p> <ph>deleteDatabase()</ph> is just a file system management problem ;
                remember that the database should be closed at that moment no file locking
                issues.</p>
            <p> <ph>removeIndexContent()</ph> deletes the table that contains spatial
                data. As explained above, this is less efficient than deleting the whole database
                though!</p>
            <p>The two next methods are totally JDBC-specific and, given the way they are
                implemented, are totally embedded HSQLDB-specific. The <i>current</i>
                code is directly adapted from
                <ph>org.exist.storage.lock.ReentrantReadWriteLock</ph> to show that
                connection management should be severely controlled given the concurrency context
                induced by using many <ph>org.exist.storage.DBBroker</ph>. Despite the
                fact <ph>DBBroker</ph>s are thread-safe, access to
                <i>shared</i> storage must be concurrential, in particular when
                <ph>flush()</ph> is called. </p>
            <p> <ph>org.exist.storage.index.BFile</ph> users would call
                <ph>getLock()</ph> to acquire and release locks on the index files. Our
                solution is thus very similar.</p>
            <p>However, since most JDBC databases are able to work in a concurrential context, it
                would then be better to never call these <ph>Index</ph>-level methods from
                the <ph>IndexWorker</ph>s and let each <ph>IndexWorker</ph>
                handle its connection to the underlying DB.</p>
            <pre>protected Connection acquireConnection(DBBroker broker)
	  throws SQLException {
	  synchronized (this) {	
	    if (connectionOwner == null) {
	      connectionOwner = broker;
	      if (conn == null)
	        initializeConnection();
	      return conn;
	    } else {    
	      long waitTime = connectionTimeout;
	      long waitTime = timeOut_;
	      long start = System.currentTimeMillis();
	      try {
	        for (;;) {
		  wait(waitTime);  
		  if (connectionOwner == null) {
		    connectionOwner = broker;	    			
		    if (conn == null)
		      //We should never get there since the connection should have been initialized
		      //by the first request from a worker
		    initializeConnection();
		    return conn; 			
		  } else {
		    waitTime = timeOut_ - (System.currentTimeMillis() - start);
		    if (waitTime &lt;= 0) {
		      LOG.error("Time out while trying to get connection");
		    }
		  }
	      }
	    } catch (InterruptedException ex) {
	      notify();
	      throw new RuntimeException("interrupted while waiting for lock");
	    }
	  }
	  }
	}

	protected synchronized void releaseConnection(DBBroker broker)
	  throws SQLException {   
	  if (connectionOwner == null)
	    throw new SQLException("Attempted to release a connection that wasn't acquired");
	  connectionOwner = null;
	}</pre>
            <p> <ph>acquireConnection()</ph> acquires an <i>exclusive</i>
                JDBC <ph>Connection</ph> to the storage engine for an
                <ph>IndexWorker</ph> (or a <ph>org.exist.storage.DBBroker</ph>,
                which roughly means the same thing). This is where a <ph>Connection</ph>
                is created if necessary (see below) and makes the first connection's performance
                cost due only when needed.</p>
            <p> <ph>releaseConnection()</ph> marks the connection as being unused. It
                will thus become available when requested again.</p>
            <p>The last method concentrates the index-level DB-dependant code in just one place
                (<ph>removeIndexContent()</ph> is relatively DB-independant).</p>
            <pre>private void initializeConnection()
	  throws SQLException {
	  System.setProperty("hsqldb.cache_scale", "11");
	  System.setProperty("hsqldb.cache_size_scale", "12");
	  System.setProperty("hsqldb.default_table_type", "cached");
	  //Get a connection to the DB... and keep it
	  this.conn = DriverManager.getConnection("jdbc:hsqldb:" + getDataDir() + "/" + db_file_name_prefix, "sa", "");
	  try {	
	    ResultSet rs = this.conn.getMetaData().getTables(null, null, TABLE_NAME, new String[] { "TABLE" });
	    rs.last(); 
	    if (rs.getRow() == 1) {
	      if (LOG.isDebugEnabled()) 
	        LOG.debug("Opened GML index: " + getDataDir() + "/" + db_file_name_prefix); 
	      //Create the data structure if it doesn't exist
	    } else if (rs.getRow() == 0) {
	      Statement stmt = conn.createStatement();
	      stmt.executeUpdate("CREATE TABLE " + TABLE_NAME + "(" +
	      /*1*/ "DOCUMENT_URI VARCHAR, " +        		
	      /*2*/ "NODE_ID_UNITS INTEGER, " + 
	      /*3*/ "NODE_ID BINARY, " +        			
	      ...	        	
	      /*26*/ "IS_VALID BOOLEAN, " +
	      //Enforce uniqueness
	      "UNIQUE (" +
	      "DOCUMENT_URI, NODE_ID_UNITS, NODE_ID" +
	      ")" +
	      ")"
	      );
	      stmt.executeUpdate("CREATE INDEX DOCUMENT_URI ON " + TABLE_NAME + " (DOCUMENT_URI);");
	      ...
	      stmt.close();        	
	      if (LOG.isDebugEnabled()) 
	        LOG.debug("Created GML index: " + getDataDir() + "/" + db_file_name_prefix);  
	    } else {
	      throw new SQLException("2 tables with the same name ?"); 
	    }
	  } finally {
	    if (rs != null)
	    rs.close();    				
	  }        
	}</pre>
            <p>This method opens a <ph>Connection</ph> and, if it is a new one
                (<i>the</i> new one since we only have one), checks that we have a SQL
                table for the spatial data. If not, i.e. if the spatial index doesn't exist yet, a
                table is created with the following structure:</p>
            <p>
                    <b/>
                </p>
                <simpletable>
                    <sthead>
                        <stentry> Field name </stentry>
                        <stentry> Field type </stentry>
                        <stentry> Description </stentry>
                        <stentry> Comments </stentry>
                    </sthead>
                    <strow>
                        <stentry> DOCUMENT_URI </stentry>
                        <stentry> VARCHAR </stentry>
                        <stentry> The document's URI </stentry>
                        <stentry/>
                    </strow>
                    <strow>
                        <stentry> NODE_ID_UNITS </stentry>
                        <stentry> INTEGER </stentry>
                        <stentry> The number of useful <i>bits</i> in
                                NODE_ID </stentry>
                        <stentry> See below </stentry>
                    </strow>
                    <strow>
                        <stentry> NODE_ID </stentry>
                        <stentry> BINARY </stentry>
                        <stentry> The node ID, as a byte array </stentry>
                        <stentry> See above. Only <i>some</i> bits might be
                                considered due to obvious data alignment requirements
                            </stentry>
                    </strow>
                    <strow>
                        <stentry> GEOMETRY_TYPE </stentry>
                        <stentry> VARCHAR </stentry>
                        <stentry> The geometry type </stentry>
                        <stentry> As returned by the JTS </stentry>
                    </strow>
                    <strow>
                        <stentry> SRS_NAME </stentry>
                        <stentry> VARCHAR </stentry>
                        <stentry> The SRS of the geometry </stentry>
                        <stentry>  <ph>srsName</ph> attribute in the GML
                                element </stentry>
                    </strow>
                    <strow>
                        <stentry> WKT </stentry>
                        <stentry> VARCHAR </stentry>
                        <stentry> The <xref href="https://de.wikipedia.org/wiki/Well_Known_Text" scope="external">Well-Known Text</xref> representation of the geometry </stentry>
                        <stentry/>
                    </strow>
                    <strow>
                        <stentry> WKB </stentry>
                        <stentry> BINARY </stentry>
                        <stentry> The WKB representation of the geometry </stentry>
                        <stentry/>
                    </strow>
                    <strow>
                        <stentry> MINX </stentry>
                        <stentry> DOUBLE </stentry>
                        <stentry> The minimal X of the geometry </stentry>
                        <stentry/>
                    </strow>
                    <strow>
                        <stentry> MAXX </stentry>
                        <stentry> DOUBLE </stentry>
                        <stentry> The maximal X of the geometry </stentry>
                        <stentry/>
                    </strow>
                    <strow>
                        <stentry> MINY </stentry>
                        <stentry> DOUBLE </stentry>
                        <stentry> The minimal Y of the geometry </stentry>
                        <stentry/>
                    </strow>
                    <strow>
                        <stentry> MAXY </stentry>
                        <stentry> DOUBLE </stentry>
                        <stentry> The maximal Y of the geometry </stentry>
                        <stentry/>
                    </strow>
                    <strow>
                        <stentry> CENTROID_X </stentry>
                        <stentry> DOUBLE </stentry>
                        <stentry> The X of the geometry's centroid </stentry>
                        <stentry/>
                    </strow>
                    <strow>
                        <stentry> CENTROID_Y </stentry>
                        <stentry> DOUBLE </stentry>
                        <stentry> The Y of the geometry's centroid </stentry>
                        <stentry/>
                    </strow>
                    <strow>
                        <stentry> AREA </stentry>
                        <stentry> DOUBLE </stentry>
                        <stentry> The area of the geometry </stentry>
                        <stentry> Expressed in the measure defined in its SRS
                            </stentry>
                    </strow>
                    <strow>
                        <stentry> EPSG4326_WKT </stentry>
                        <stentry> VARCHAR </stentry>
                        <stentry> The WKT representation of the geometry </stentry>
                        <stentry> In the <xref href="https://en.wikipedia.org/wiki/EPSG:4326" scope="external">epsg:4326</xref> SRS </stentry>
                    </strow>
                    <strow>
                        <stentry> EPSG4326_WKB </stentry>
                        <stentry> BINARY </stentry>
                        <stentry> The WKB representation of the geometry </stentry>
                        <stentry> In the epsg:4326 SRS </stentry>
                    </strow>
                    <strow>
                        <stentry> EPSG4326_MINX </stentry>
                        <stentry> DOUBLE </stentry>
                        <stentry> The minimal X of the geometry </stentry>
                        <stentry> In the epsg:4326 SRS </stentry>
                    </strow>
                    <strow>
                        <stentry> EPSG4326_MAXX </stentry>
                        <stentry> DOUBLE </stentry>
                        <stentry> The maximal X of the geometry </stentry>
                        <stentry> In the epsg:4326 SRS </stentry>
                    </strow>
                    <strow>
                        <stentry> EPSG4326_MINY </stentry>
                        <stentry> DOUBLE </stentry>
                        <stentry> The minimal Y of the geometry </stentry>
                        <stentry> In the epsg:4326 SRS </stentry>
                    </strow>
                    <strow>
                        <stentry> EPSG4326_MAXY </stentry>
                        <stentry> DOUBLE </stentry>
                        <stentry> The maximal Y of the geometry </stentry>
                        <stentry> In the epsg:4326 SRS </stentry>
                    </strow>
                    <strow>
                        <stentry> EPSG4326_CENTROID_X </stentry>
                        <stentry> DOUBLE </stentry>
                        <stentry> The X of the geometry's centroid </stentry>
                        <stentry> In the epsg:4326 SRS </stentry>
                    </strow>
                    <strow>
                        <stentry> EPSG4326_CENTROID_Y </stentry>
                        <stentry> DOUBLE </stentry>
                        <stentry> The Y of the geometry's centroid </stentry>
                        <stentry> In the epsg:4326 SRS </stentry>
                    </strow>
                    <strow>
                        <stentry> EPSG4326_AREA </stentry>
                        <stentry> DOUBLE </stentry>
                        <stentry> The area of the geometry </stentry>
                        <stentry> In the epsg:4326 SRS (measure unknown, to be
                                clarified) </stentry>
                    </strow>
                    <strow>
                        <stentry> IS_CLOSED </stentry>
                        <stentry> BOOLEAN </stentry>
                        <stentry> Whether or not this geometry is "closed" </stentry>
                        <stentry> See the <xref href="http://www.vividsolutions.com/jts/bin/JTS%20Technical%20Specs.pdf" scope="external">JTS documentation</xref> (chapter 13) </stentry>
                    </strow>
                    <strow>
                        <stentry> IS_SIMPLE </stentry>
                        <stentry> BOOLEAN </stentry>
                        <stentry> Whether or not this geometry is "simple" </stentry>
                        <stentry> See the <xref href="http://www.vividsolutions.com/jts/bin/JTS%20Technical%20Specs.pdf" scope="external">JTS documentation</xref> (chapter 13) </stentry>
                    </strow>
                    <strow>
                        <stentry> IS_VALID </stentry>
                        <stentry> BOOLEAN </stentry>
                        <stentry> Whether or not this geometry is "valid" </stentry>
                        <stentry> See the <xref href="http://www.vividsolutions.com/jts/bin/JTS%20Technical%20Specs.pdf" scope="external">JTS documentation</xref> (chapter 13). Should always be TRUE
                            </stentry>
                    </strow>
                </simpletable>
            <p>Uniqueness will be enforced on a <ph>(DOCUMENT_URI, NODE_ID_UNITS,
                NODE_ID)</ph> basis. Indeed, we can have at most one index entry for a given
                node in a given document.</p>
            <p>Also, indexes are created on these fields to help queries:</p>
            <ul>
                    <li>
                    <p>DOCUMENT_URI</p>
                </li>
                    <li>
                    <p>NODE_ID</p>
                </li>
                    <li>
                    <p>GEOMETRY_TYPE</p>
                </li>
                    <li>
                    <p>WKB</p>
                </li>
                    <li>
                    <p>EPSG4326_WKB</p>
                </li>
                    <li>
                    <p>EPSG4326_MINX</p>
                </li>
                    <li>
                    <p>EPSG4326_MAXX</p>
                </li>
                    <li>
                    <p>EPSG4326_MINY</p>
                </li>
                    <li>
                    <p>EPSG4326_MAXY</p>
                </li>
                    <li>
                    <p>EPSG4326_CENTROID_X</p>
                </li>
                    <li>
                    <p>EPSG4326_CENTROID_Y</p>
                </li>
                </ul>
            <p>Every geometry will be internally stored in <i>both</i> its original
                SRS and in the epsg:4326 SRS. Having this kind of common, world-wide applicable, SRS
                for <i>all</i> geometries in the index allows to make operations on
                them even if they are originally defined in different SRSes.</p>
            <note type="notice">
                    <p>By default, eXist's build will download the lightweight
                    <ph>gt2-epsg-wkt-XXX.jar</ph> library which lacks some parameters, the
                    <xref href="http://udig.refractions.net/docs/api-geotools/org/geotools/referencing/datum/BursaWolfParameters.html" scope="external"> Bursa-Wolf</xref> ones. A better accuracy for geographic transformations might
                    be obtained by using a heavier library like <xref href="http://lists.refractions.net/m2/org/geotools/gt2-epsg-hsql/" scope="external">
                    <ph>gt2-epsg-hsql-XXX.jar</ph> </xref> which is documented <xref href="http://javadoc.geotools.fr/snapshot/org/geotools/referencing/factory/epsg/FactoryOnHSQL.html" scope="external">here</xref>.</p>
                </note>
        </section>

        

        <section id="write-index-worker">
                <title>Writing the concrete implementation of org.exist.indexing.IndexWorker </title>
            

            <p>Just like for <ph>org.exist.indexing.spatial.AbstractGMLJDBCIndex</ph>,
                we will start to design a database-independant abstract class. This class should
                normally be the basis of every JDBC spatial index. It will handle most of the hard
                work.</p>
            <p>Let's start by a few members and a few general-purpose public methods:</p>
            <pre>package org.exist.indexing.spatial;
      
      public abstract class AbstractGMLJDBCIndexWorker implements IndexWorker {
      
        public static String GML_NS = "http://www.opengis.net/gml";    
        protected final static String INDEX_ELEMENT = "gml";   
      
	private static final Logger LOG = Logger.getLogger(AbstractGMLJDBCIndexWorker.class);
      
	protected IndexController controller;
	protected AbstractGMLJDBCIndex index;
	protected DBBroker broker;
	protected int currentMode = StreamListener.UNKNOWN;    
	protected DocumentImpl currentDoc = null;  
	private boolean isDocumentGMLAware = false;
	protected Map geometries = new TreeMap();    
	NodeId currentNodeId = null;    
	Geometry streamedGeometry = null;
	boolean documentDeleted = false;
	int flushAfter = -1;
	protected GMLHandlerJTS geometryHandler = new GeometryHandler(); 
	protected GMLFilterGeometry geometryFilter = new GMLFilterGeometry(geometryHandler); 
	protected GMLFilterDocument geometryDocument = new GMLFilterDocument(geometryFilter);
	protected TreeMap transformations = new TreeMap();
	protected boolean useLenientMode = false;   
	protected GMLStreamListener gmlStreamListener = new GMLStreamListener(); 
	protected GeometryCoordinateSequenceTransformer coordinateTransformer = new GeometryCoordinateSequenceTransformer();
	protected GeometryTransformer gmlTransformer = new GeometryTransformer();		
	protected WKBWriter wkbWriter = new WKBWriter();
	protected WKBReader wkbReader = new WKBReader();
	protected WKTWriter wktWriter = new WKTWriter();
	protected WKTReader wktReader = new WKTReader();
	protected Base64Encoder base64Encoder = new Base64Encoder();
	protected Base64Decoder base64Decoder = new Base64Decoder();  
      
	public AbstractGMLJDBCIndexWorker(AbstractGMLJDBCIndex index, DBBroker broker) {
	  this.index = index;
	  this.broker = broker;
	}
      
	public String getIndexId() {
	  return AbstractGMLJDBCIndex.ID;
	}        
      
	public String getIndexName() {
	  return index.getIndexName();
	}
      
	public Index getIndex() {
	  return index;
        }
      
      }</pre>
            <p>Of course,
                <ph>org.exist.indexing.spatial.AbstractGMLJDBCIndexWorker</ph> implements
                <ph>org.exist.indexing.IndexWorker</ph>.</p>
            <p> <ph>GML_NS</ph> is the GML namespace for which the spatial index is
                specially designed. Use this public member to avoid redundancy and, worse,
                inconsistencies.</p>
            <p> <ph>INDEX_ELEMENT</ph> is the configuration's element name which is
                accurate for our <ph>Index</ph> configuration. To configure a collection
                in order to index its GML data, define such a configuration: </p>
            <pre outputclass="xml">&lt;collection xmlns="http://exist-db.org/collection-config/1.0"&gt;
  &lt;index&gt;
    &lt;gml flushAfter="200"/&gt;
  &lt;/index&gt;
&lt;/collection&gt;</pre>
            <p>Got the <ph>gml</ph> element? We will shortly see how this information is able
                to configure our <ph>IndexWorker</ph>.</p>
            <p> <ph>controller</ph>, <ph>index</ph> and
                <ph>broker</ph> should now be quite straightforward.</p>
            <p> <ph>currentMode</ph> and <ph>currentDoc</ph> should also be
                straightforward.</p>
            <p> <ph>geometries</ph> is a collection of
                <ph>com.vividsolutions.jts.geom.Geometry</ph> instances that are currently
                held in memory, waiting for being "flushed" to the database. Depending of
                <ph>currentMode</ph>, they're pending for insertion or removal.</p>
            <p> <ph>currentNodeId</ph> is used to share the ID of the node currently
                being processed between the different inner classes.</p>
            <p> <ph>streamedGeometry</ph> is the last
                <ph>com.vividsolutions.jts.geom.Geometry</ph> that has been generated by
                GML parsing. It is <ph>null</ph> if the geometry is topologically not well
                formed. This latter case is maybe a too restrictive feature of Geotools parser which
                also throws <ph>NullPointerException</ph>s (!) if the GML is somehow not
                well-formed. See <xref href="https://osgeo-org.atlassian.net/browse/GEOT-742" scope="external">GEOT-742</xref> for more information on this issue.</p>
            <p> <ph>documentDeleted</ph> is a flag indicating that the current document
                has been deleted and that we don't have to process it any more. Remember that
                <ph>StreamListener.REMOVE_ALL_NODES</ph> send some events for
                <i>all</i> nodes.</p>
            <p> <ph>flushAfter </ph> will hold our configuration's setting.</p>
            <p> <ph>geometryHandler</ph> is our GML geometries SAX handler that will
                convert GML to a <ph>com.vividsolutions.jts.geom.Geometry</ph> instance.
                It is included in a handler chain composed of <ph>geometryFilter</ph> and
                <ph>geometryDocument</ph>.</p>
            <p> <ph>transforms</ph> will cache a list a transformations between a
                source and a target SRS.</p>
            <p> <ph>useLenientMode</ph> will be set to <ph>true</ph> is the
                transformation libraries that are in the CLASSPATH don't have the Bursa-Wolf
                parameters. Transformations will be attempted, but with a precision loss (see
                above).</p>
            <p> <ph>gmlStreamListener</ph> is our own implementation of
                <ph>org.exist.indexing.StreamListener</ph>. Since there is a 1:1 (or even
                1:0) relationship with the <ph>IndexWorker</ph>, it will be implemented as
                an inner class and will be described below.</p>
            <p> <ph>coordinateTransformer</ph> will be needed during
                <ph>Geometry</ph> transformations to other SRSes.</p>
            <p> <ph>gmlTransformer</ph> will be needed during
                <ph>Geometry</ph> transformations to XML.</p>
            <p> <ph>wkbWriter</ph> and <ph>wkbReader</ph> will be needed
                during <ph>Geometry</ph> serialization and deserialization to and from the
                database.</p>
            <p> <ph>wktWriter</ph> and <ph>wktReader</ph> will be needed
                during <ph>Geometry</ph> WKT serialization and deserialization to and from
                the database. WKT could be dynamically generated from <ph>Geometry</ph>
                but we have chosen to store it in the HSQLDB.</p>
            <p> <ph>base64Encoder</ph> and <ph>base64Decoder</ph> will be
                needed to convert binary date, namely WKB, to XML types, namely
                <ph>xs:base64Binary</ph>.</p>
            <p>No need to comment the methods, expect maybe <ph>getIndexId()</ph> that
                will return the <i>static</i> ID of the <ph>Index</ph>. No
                chance to be wrong with such a design.</p>
            <p>The next method is a bit specific:</p>
            <pre>public Object configure(IndexController controller, NodeList configNodes, Map namespaces) 
        throws DatabaseConfigurationException {
	this.controller = controller;
	Map map = null;      
	for(int i = 0; i &lt; configNodes.getLength(); i++) {
	  Node node = configNodes.item(i);
	  if (node.getNodeType() == Node.ELEMENT_NODE &amp;&amp; INDEX_ELEMENT.equals(node.getLocalName())) { 
	    map = new TreeMap();
	    GMLIndexConfig config = new GMLIndexConfig(namespaces, (Element)node);
	    map.put(AbstractGMLJDBCIndex.ID, config);
	  }
        }
	return map;
      }</pre>
            <p>It is only interested in the <ph>gml</ph> element of the configuration. If it
                finds one, it creates a <ph>org.exist.indexing.spatial.GMLIndexConfig</ph>
                instance wich is a very simple class:</p>
            <pre>package org.exist.indexing.spatial;

      public class GMLIndexConfig {

        private static final Logger LOG = Logger.getLogger(GMLIndexConfig.class);
      
	private final static String FLUSH_AFTER = "flushAfter";	
	private int flushAfter = -1;
      
	public GMLIndexConfig(Map namespaces, Element node)
	  throws DatabaseConfigurationException {       
	  String param = ((Element)node).getAttribute(FLUSH_AFTER);
	  if (param != null &amp;&amp; !"".equals(param)) {) {
	    try {
	      flushAfter = Integer.parseInt(param);
	    } catch (NumberFormatException e) {
	      LOG.info("Invalid value for '" + FLUSH_AFTER + "'", e);
	    }
	  }	    	
        }
      
	public int getFlushAfter() {
	  return flushAfter;
        }
      }</pre>
            <p>This retains the configuration attribute and provides a getter for it.</p>
            <p>This configuration object is saved in a Map with the <ph>Index</ph> ID
                and will be available as shown in the next method:</p>
            <pre>public void setDocument(DocumentImpl document) {  
        isDocumentGMLAware = false;
	documentDeleted= false;
	if (document != null) {
	  IndexSpec idxConf = document.getCollection().getIndexConfiguration(document.getBroker());
	  if (idxConf != null) {
	    Map collectionConfig = (Map) idxConf.getCustomIndexSpec(AbstractGMLJDBCIndex.ID);
	    if (collectionConfig != null) {
	      isDocumentGMLAware = true;
	      if (collectionConfig.get(AbstractGMLJDBCIndex.ID) != null)
	        flushAfter = ((GMLIndexConfig)collectionConfig.
	      get(AbstractGMLJDBCIndex.ID)).getFlushAfter();
	    }
          }
        }
	if (isDocumentGMLAware) {
	  currentDoc = document;	        
	} else {
	  currentDoc = null;
	  currentMode = StreamListener.UNKNOWN;    		
        }
      }</pre>
            <p>The objective is to determine if <ph>document</ph> should be indexed by
                the spatial <ph>Index</ph>.</p>
            <p>For this, we look up its collection configuration and try to find a "custom" index
                specification for our <ph>Index</ph>. If one is found, our
                <ph>document</ph> will be processed by the <ph>IndexWorker</ph>.
                We also take advantage of this process to set one of our members. If
                <ph>document</ph> doesn't interest our <ph>IndexWorker</ph>, we
                reset some members to avoid having an inconsistent sticky state.</p>
            <p>The next methods don't require any particular comment:</p>
            <pre>public void setDocument(DocumentImpl doc, int mode) {
        setDocument(doc);
	setMode(mode);
      }

      public DocumentImpl getDocument() {
        return currentDoc;
      }

      public int getMode() {
        return currentMode;
      }</pre>
            <p>The next method is somehow tricky:</p>
            <pre>public StreamListener getListener() {      
        if (currentDoc == null || currentMode == StreamListener.REMOVE_ALL_NODES)
	  return null;
	return gmlStreamListener;
      }</pre>
            <p>It doesn't return any <ph>StreamListener</ph> in the
                <ph>StreamListner.REMOVE_ALL_NODES</ph>. It would be totally unnecessary
                to listen at every node whereas a JDBC database will be able to delete all the
                document's nodes in one single statement.</p>
            <p>The next method is a place holder that needs more thinking. How to highlight a
                geometric information smartly?</p>
            <pre>public MatchListener getMatchListener(NodeProxy proxy) {
        return null;
      }</pre>
            <p>The next method computes the reindexing root. We will go bottom-up form the not to
                be modified until the top-most element in the GML namespace. Indeed, GML allows
                "nested" or "multi" geometries. If a single part of such <ph>Geometry</ph>
                is modified, the whole geometry has to be recomputed.</p>
            <pre>public StoredNode getReindexRoot(StoredNode node, NodePath path, boolean includeSelf) {
        if (!isDocumentGMLAware)
	  //Not concerned
	  return null;
	StoredNode topMost = node;
	StoredNode currentNode = node;
	for (int i = path.length() ; i &gt; 0; i--) {
	  currentNode = (StoredNode)currentNode.getParentNode();
	  if (GML_NS.equals(currentNode.getNamespaceURI()))		
	    topMost = currentNode;
        }
	return topMost;    	
      }</pre>
            <p>The next method delegates the write operations:</p>
            <pre>public void flush() {
        if (!isDocumentGMLAware)
	  //Not concerned
	  return;
	//Is the job already done ?
	if (currentMode == StreamListener.REMOVE_ALL_NODES &amp;&amp; documentDeleted)
	  return;
	Connection conn = null;
	try {
	  conn = acquireConnection();
	  conn.setAutoCommit(false);
	  switch (currentMode) {
	    case StreamListener.STORE :
	      saveDocumentNodes(conn);
	      break;
	    case StreamListener.REMOVE_SOME_NODES :
	      dropDocumentNode(conn);
	      break;
	    case StreamListener.REMOVE_ALL_NODES:
	      removeDocument(conn);
	      documentDeleted = true;
	      break;
	  }
	  conn.commit();	  
	} catch (SQLException e) {
	  LOG.error("Document: " + currentDoc + " NodeID: " + currentNodeId, e);
	  try {
	    conn.rollback();
	  } catch (SQLException ee) {
	    LOG.error(ee);
	  }
	} finally {
	  try {
	    if (conn != null) {
	      conn.setAutoCommit(true);
	      releaseConnection(conn);
	    }
	  } catch (SQLException e) {
	    LOG.error(e);
	  }        		
	}
      }</pre>
            <p>Even though its code looks thick, it proves to be a good way to acquire (then
                release) a <ph>Connection</ph> whatever the way it is provided by the
                <ph>IndexWorker</ph> (see above for these aspects, concurrency in
                particular). It then delegates the write operations to dedicated methods, which do
                not have to care about the <ph>Connection</ph>. Write operations are
                embedded in a transaction. Should an exception occur, it would be logged and
                swallowed: eXist doesn't like exceptions when it flushes its data.</p>
            <p>The next method delegates node storage:</p>
            <pre>private void saveDocumentNodes(Connection conn) throws SQLException {
        if (geometries.size() == 0)
	  return;  
	try {	        
	  for (Iterator iterator = geometries.entrySet().iterator(); iterator.hasNext();) {
	    Map.Entry entry = (Map.Entry) iterator.next();
	    NodeId nodeId = (NodeId)entry.getKey();
	    SRSGeometry srsGeometry = (SRSGeometry)entry.getValue();       	
	    try {
	      saveGeometryNode(srsGeometry.getGeometry(), srsGeometry.getSRSName(), 
	      currentDoc, nodeId, conn);
	    } finally {
	      //Help the garbage collector
	      srsGeometry = null;
	    }
	  }
	} finally {
	  geometries.clear();
	}
      }</pre>
            <p>It will call <ph>saveGeometryNode()</ph> (see below) passing a container
                inner class that will not be described given its simplicity.</p>
            <p>The next two methods are built with the same design. The first one destroys the
                index entry for the currently processed node and the second one removes the index
                entries for the whole document.</p>
            <pre>private void dropDocumentNode(Connection conn)
        throws SQLException {    	
	if (currentNodeId == null)
	  return;        
	try {         
	  boolean removed = removeDocumentNode(currentDoc, currentNodeId, conn);
	  if (!removed)
	    LOG.error("No data dropped for node " + currentNodeId.toString() + " from GML index");
	  else {
	    if (LOG.isDebugEnabled())	            
	      LOG.debug("Dropped data for node " + currentNodeId.toString() + " from GML index");
	  }
	} finally {        
	currentNodeId = null;
	}
      }

      private void removeDocument(Connection conn)
        throws SQLException {
	if (LOG.isDebugEnabled())
	  LOG.debug("Dropping GML index for document " + currentDoc.getURI());
	int nodeCount = removeDocument(currentDoc, conn);
	if (LOG.isDebugEnabled())
	  LOG.debug("Dropped " + nodeCount + " nodes from GML index");
      }</pre>
            <p>The next method is a mix of the designs described above. It also previously makes
                a check:</p>
            <pre>public void removeCollection(Collection collection, DBBroker broker) {
        boolean isCollectionGMLAware = false;
	IndexSpec idxConf = collection.getIndexConfiguration(broker);
	if (idxConf != null) {
	  Map collectionConfig = (Map) idxConf.getCustomIndexSpec(AbstractGMLJDBCIndex.ID);
	  if (collectionConfig != null) {
	    isCollectionGMLAware = (collectionConfig != null);
	  }
	}
	if (!isCollectionGMLAware)
	  return;  
      
	Connection conn = null;
	try {
	  conn = acquireConnection();
	  if (LOG.isDebugEnabled())
	    LOG.debug("Dropping GML index for collection " + collection.getURI());
	  int nodeCount = removeCollection(collection, conn);
	  if (LOG.isDebugEnabled())
	    LOG.debug("Dropped " + nodeCount + " nodes from GML index");
	} catch (SQLException e) {
	  LOG.error(e);
	} finally {
	  try {
	    if (conn != null)
	      releaseConnection(conn);
	  } catch (SQLException e) {
	    LOG.error(e);
	  }    		
	}
      }</pre>
            <p>Indeed, we have to check if the collection is indexable by the
                <ph>Index</ph> before trying to delete its index entries.</p>
            <p>The next methods are built on the same design (<ph>Collection</ph> and
                exception management) and will thus not be described.</p>
            <pre>public NodeSet search(DBBroker broker, NodeSet contextSet, Geometry EPSG4326_geometry, int spatialOp)
        ...    	
      }

      public Geometry getGeometryForNode(DBBroker broker, NodeProxy p, boolean getEPSG4326) throws  SpatialIndexException {
        ...
      }

      protected Geometry[] getGeometriesForNodes(DBBroker broker, NodeSet contextSet, boolean getEPSG4326) throws SpatialIndexException {
        ...
      }

      public AtomicValue getGeometricPropertyForNode(DBBroker broker, NodeProxy p, String propertyName) 
      throws  SpatialIndexException {
        ...
      }    

      public ValueSequence getGeometricPropertyForNodes(DBBroker broker, NodeSet contextSet, String propertyName) 
      throws  SpatialIndexException {
        ...
      }</pre>
            <p>All these methods delegate to the following abstract methods that will have to be
                implemented by the DB-dependant concrete classes:</p>
            <pre>protected abstract boolean saveGeometryNode(Geometry geometry, String srsName, DocumentImpl doc, NodeId nodeId, Connection conn)
        throws SQLException;

      protected abstract boolean removeDocumentNode(DocumentImpl doc, NodeId nodeID, Connection conn)
        throws SQLException;

      protected abstract int removeDocument(DocumentImpl doc, Connection conn)
        throws SQLException;

      protected abstract int removeCollection(Collection collection, Connection conn)
        throws SQLException;
      
      protected abstract Connection acquireConnection() throws SQLException;

      protected abstract void releaseConnection(Connection conn) throws SQLException;	 
      
      protected abstract NodeSet search(DBBroker broker, NodeSet contextSet, Geometry EPSG4326_geometry, int spatialOp, Connection conn)
        throws SQLException;		 

      protected abstract Map getGeometriesForDocument(DocumentImpl doc, Connection conn)
        throws SQLException;

      protected abstract AtomicValue getGeometricPropertyForNode(DBBroker broker, NodeProxy p, Connection conn, 
      String propertyName) throws SQLException, XPathException;

      protected abstract ValueSequence getGeometricPropertyForNodes(DBBroker broker, NodeSet contextSet, Connection conn, String propertyName)
        throws SQLException, XPathException;

      protected abstract Geometry getGeometryForNode(DBBroker broker, NodeProxy p, boolean getEPSG4326, Connection conn)
        throws SQLException;

      protected abstract Geometry[] getGeometriesForNodes(DBBroker broker, NodeSet contextSet, boolean getEPSG4326, Connection conn)
        throws SQLException;

      protected abstract boolean checkIndex(DBBroker broker, Connection conn)
        throws SQLException, SpatialIndexException;</pre>
            <p>Let's have a look however at this method that doesn't need a DB-dependant
                implementation:</p>
            <pre>public Occurrences[] scanIndex(DocumentSet docs) {    	
        Map occurences = new TreeMap();
	Connection conn = null;
	try { 
	  conn = acquireConnection();
          //Collect the (normalized) geometries for each document
	  for (Iterator iDoc = docs.iterator(); iDoc.hasNext();) {
	    DocumentImpl doc = (DocumentImpl)iDoc.next();
	    //TODO : check if document is GML-aware ?
	    //Aggregate the occurences between different documents
	    for (Iterator iGeom = getGeometriesForDocument(doc, conn).entrySet().iterator(); iGeom.hasNext();) {
	      Map.Entry entry = (Map.Entry) iGeom.next();
	      Geometry key = (Geometry)entry.getKey();
	      //Do we already have an occurence for this geometry ?
	      Occurrences oc = (Occurrences)occurences.get(key);
	      if (oc != null) {
	        //Yes : increment occurence count
		oc.addOccurrences(oc.getOccurrences() + 1);
		//...and reference the document
		oc.addDocument(doc);
              } else {
	        //No : create a new occurence with EPSG4326_WKT as "term"
		oc = new Occurrences((String)entry.getValue());
		//... with a count set to 1
		oc.addOccurrences(1);
		//... and reference the document
		oc.addDocument(doc);
		occurences.put(key, oc);
	      }
	    }
	  }
	} catch (SQLException e) {
	  LOG.error(e);
	  return null;
	} finally {
	  try {
	    if (conn != null)
	      releaseConnection(conn);
	  } catch (SQLException e) {
	    LOG.error(e);
	    return null;
	  } 
        }
	Occurrences[] result = new Occurrences[occurences.size()];
	occurences.values().toArray(result);
	return result;
      }</pre>
            <p>Same design (<ph>Collection</ph> and exception management, delegation
                mechanism). We probably will add more like this in the future.</p>
            <p>The following methods are utility methods to stream <ph>Geometry</ph>
                instances to XML and vice-versa.</p>
            <pre>public Geometry streamNodeToGeometry(XQueryContext context, NodeValue node)
        throws SpatialIndexException {
	try {
	  context.pushDocumentContext();
	  try {			
	    node.toSAX(context.getBroker(), geometryDocument, null);
	  } finally {
	    context.popDocumentContext();
	  }
	} catch (SAXException e) {
	  throw new SpatialIndexException(e);
	}
	return streamedGeometry;
      }	

      public Element streamGeometryToElement(Geometry geometry, String srsName, Receiver receiver)
        throws SpatialIndexException {           
	String gmlString = null;
	try {
	  gmlString = gmlTransformer.transform(geometry);
	} catch (TransformerException e) {
	  throw new SpatialIndexException(e);
	}
	try {
	  SAXParserFactory factory = SAXParserFactory.newInstance();
	  factory.setNamespaceAware(true);
	  InputSource src = new InputSource(new StringReader(gmlString));
	  SAXParser parser = factory.newSAXParser();
	  XMLReader reader = parser.getXMLReader();
	  reader.setContentHandler((ContentHandler)receiver);
	  reader.parse(src);
	  Document doc = receiver.getDocument();
	  return doc.getDocumentElement(); 
	} catch (ParserConfigurationException e) {				
	  throw new SpatialIndexException(e);
	} catch (SAXException e) {
	  throw new SpatialIndexException(e);
	} catch (IOException e) {
	  throw new SpatialIndexException(e);	
	}
      }</pre>
            <p>The first one uses a <ph>org.geotools.gml.GMLFilterDocument</ph> (see
                below) and the second one uses a
                <ph>org.geotools.gml.producer.GeometryTransformer</ph> which needs some
                polishing because, despite it is called a transformer, it doesn't cope easily with a
                <ph>Handler</ph> and returns a... <ph>String</ph> ! See <xref href="https://osgeo-org.atlassian.net/browse/GEOT-1315" scope="external">GEOT-1315</xref>.</p>
            <p>The last method is also a utility method:</p>
            <pre>public Geometry transformGeometry(Geometry geometry, String sourceCRS, String targetCRS)
        throws SpatialIndexException {
	if ("osgb:BNG".equalsIgnoreCase(sourceCRS.trim()))
	  sourceCRS = "EPSG:27700";  	    
	if ("osgb:BNG".equalsIgnoreCase(targetCRS.trim()))
	  targetCRS = "EPSG:27700"; 
	MathTransform transform = (MathTransform)transformations.get(sourceCRS + "_" + targetCRS);
	if (transform == null) {
	  try {
	    try {        	
	      transform = CRS.findMathTransform(CRS.decode(sourceCRS), CRS.decode(targetCRS), useLenientMode);
	    } catch (OperationNotFoundException e) {
	      LOG.info(e);
	      LOG.info("Switching to lenient mode... beware of precision loss !");
	      useLenientMode = true;
	      transform = CRS.findMathTransform(CRS.decode(sourceCRS), CRS.decode(targetCRS), useLenientMode);	
	    }
	    transformations.put(sourceCRS + "_" + targetCRS, transform);
	    LOG.debug("Instantiated transformation from '" + sourceCRS + "' to '" + targetCRS + "'");
	  } catch (NoSuchAuthorityCodeException e) {
	    LOG.error(e);
	  } catch (FactoryException e) {
	    LOG.error(e);
	  }
	}
	if (transform == null) {
	  throw new SpatialIndexException("Unable to get a transformation from '" + sourceCRS + "' to '" + targetCRS +"'");        		           	
	}
	coordinateTransformer.setMathTransform(transform);
	try {
	  return coordinateTransformer.transform(geometry);
	} catch (TransformException e) {
	  throw new SpatialIndexException(e);
        }
      }</pre>
            <p>It implements a workaround for our test file SRS which isn't yet known by Geotools
                libraries (see <xref href="https://osgeo-org.atlassian.net/browse/GEOT-1307" scope="external">GEOT-1307</xref>), then it tries to get the transformation from our cache. If it
                doesn't succeed, it tries to find one in the libraries that are in the CLASSPATH.
                Should those libraries lack the Bursa-Wolf parameters, it will make another attempt
                in lenient mode, which will induce a loss of accuracy. Then, it transforms the
                <ph>Geometry</ph> from its <ph>sourceCRS</ph> to the required
                <ph>targetCRS</ph>.</p>
            <p>Now, let's study how the abstract methods are implement by the HSQLDB-dependant
                class:</p>
            <pre>package org.exist.indexing.spatial;

      public class GMLHSQLIndexWorker extends AbstractGMLJDBCIndexWorker {

        private static final Logger LOG = Logger.getLogger(GMLHSQLIndexWorker.class);
      
	public GMLHSQLIndexWorker(GMLHSQLIndex index, DBBroker broker) {
	  super(index, broker);
	}
      }</pre>
            <p>The only noticeable point is that we indeed extend our
                <ph>org.exist.indexing.spatial.AbstractGMLJDBCIndexWorker</ph> </p>
            <p>Now, this method will do something more interesting, store the
                <ph>Geometry</ph> associated to a node:</p>
            <pre>protected boolean saveGeometryNode(Geometry geometry, String srsName, DocumentImpl doc, NodeId nodeId, Connection conn)
        throws SQLException {
	PreparedStatement ps = conn.prepareStatement("INSERT INTO " + GMLHSQLIndex.TABLE_NAME + "(" +
	/*1*/ "DOCUMENT_URI, " +            		
	/*2*/ "NODE_ID_UNITS, " + 
	/*3*/ "NODE_ID, " +        			
	/*4*/ "GEOMETRY_TYPE, " +
	/*5*/ "SRS_NAME, " +
	/*6*/ "WKT, " +
	/*7*/ "WKB, " +
	/*8*/ "MINX, " +
	/*9*/ "MAXX, " +
	/*10*/ "MINY, " +
	/*11*/ "MAXY, " +
	/*12*/ "CENTROID_X, " +
	/*13*/ "CENTROID_Y, " +
	/*14*/ "AREA, " +
	//Boundary ?        		
	/*15*/ "EPSG4326_WKT, " +
	/*16*/ "EPSG4326_WKB, " +
	/*17*/ "EPSG4326_MINX, " +
	/*18*/ "EPSG4326_MAXX, " +
	/*19*/ "EPSG4326_MINY, " +
	/*20*/ "EPSG4326_MAXY, " +
	/*21*/ "EPSG4326_CENTROID_X, " +
	/*22*/ "EPSG4326_CENTROID_Y, " +
	/*23*/ "EPSG4326_AREA," +
	//Boundary ?
	/*24*/ "IS_CLOSED, " +
	/*25*/ "IS_SIMPLE, " +
	/*26*/ "IS_VALID" +    			
	") VALUES (" +
	"?, ?, ?, ?, ?, " +
	"?, ?, ?, ?, ?, " +
	"?, ?, ?, ?, ?, " +
	"?, ?, ?, ?, ?, " +
	"?, ?, ?, ?, ?, " +
	"?"
	+ ")"
	);       
	try {
	  Geometry EPSG4326_geometry = null;
	  try {
	    EPSG4326_geometry = transformGeometry(geometry, srsName, "EPSG:4326");
	  } catch (SpatialIndexException e) {
	    //Transforms the exception into an SQLException.
	    SQLException ee = new SQLException(e.getMessage());
	    ee.initCause(e);
	    throw ee;
	  }
	  /*DOCUMENT_URI*/ ps.setString(1, doc.getURI().toString());	
	  /*NODE_ID_UNITS*/ ps.setInt(2, nodeId.units());
	  byte[] bytes = new byte[nodeId.size()];
	  nodeId.serialize(bytes, 0);
	  /*NODE_ID*/ ps.setBytes(3, bytes);
	  /*GEOMETRY_TYPE*/ ps.setString(4, geometry.getGeometryType());
	  /*SRS_NAME*/ ps.setString(5, srsName);
	  /*WKT*/ ps.setString(6, wktWriter.write(geometry));
	  /*WKB*/ ps.setBytes(7, wkbWriter.write(geometry));
	  /*MINX*/ ps.setDouble(8, geometry.getEnvelopeInternal().getMinX());
	  /*MAXX*/ ps.setDouble(9, geometry.getEnvelopeInternal().getMaxX());
	  /*MINY*/ ps.setDouble(10, geometry.getEnvelopeInternal().getMinY());
	  /*MAXY*/ ps.setDouble(11, geometry.getEnvelopeInternal().getMaxY());
	  /*CENTROID_X*/ ps.setDouble(12, geometry.getCentroid().getCoordinate().x);   
	  /*CENTROID_Y*/ ps.setDouble(13, geometry.getCentroid().getCoordinate().y);  		
	  /*AREA*/ ps.setDouble(14, geometry.getArea());		
	  /*EPSG4326_WKT*/ ps.setString(15, wktWriter.write(EPSG4326_geometry));
	  /*EPSG4326_WKB*/ ps.setBytes(16, wkbWriter.write(EPSG4326_geometry));		
	  /*EPSG4326_MINX*/ ps.setDouble(17, EPSG4326_geometry.getEnvelopeInternal().getMinX());
	  /*EPSG4326_MAXX*/ ps.setDouble(18, EPSG4326_geometry.getEnvelopeInternal().getMaxX());
	  /*EPSG4326_MINY*/ ps.setDouble(19, EPSG4326_geometry.getEnvelopeInternal().getMinY());
	  /*EPSG4326_MAXY*/ ps.setDouble(20, EPSG4326_geometry.getEnvelopeInternal().getMaxY());
	  /*EPSG4326_CENTROID_X*/ ps.setDouble(21, EPSG4326_geometry.getCentroid().getCoordinate().x);   
	  /*EPSG4326_CENTROID_Y*/ ps.setDouble(22, EPSG4326_geometry.getCentroid().getCoordinate().y);
	  //EPSG4326_geometry.getRepresentativePoint()
	  /*EPSG4326_AREA*/ ps.setDouble(23, EPSG4326_geometry.getArea());
	  //For empty Curves, isClosed is defined to have the value false.
	  /*IS_CLOSED*/ ps.setBoolean(24, !geometry.isEmpty());
	  /*IS_SIMPLE*/ ps.setBoolean(25, geometry.isSimple());
	  //Should always be true (the GML SAX parser makes a too severe check)
	  /*IS_VALID*/ ps.setBoolean(26, geometry.isValid());
	  return (ps.executeUpdate() == 1);
	} finally {
	if (ps != null)
	  ps.close();
	//Let's help the garbage collector...
	geometry = null;
	}    	
      }</pre>
            <p>The generated SQL statement should be straightforward. We make a heavy use of the
                methods provided by <ph>com.vividsolutions.jts.geom.Geometry</ph>, both on
                the "native" <ph>Geometry</ph> and on its EPSG:4326 transformation. Would
                could probably store other properties here (like, e.g. the geometry's boundary).
                Other <ph>IndexWorker</ph>s, especially those accessing a
                spatially-enabled DBMS, might prefer to store fewer properties if they can be
                computed dynamically at a cheap price.</p>
            <p>The next method is even much easier to understand:</p>
            <pre>protected boolean removeDocumentNode(DocumentImpl doc, NodeId nodeId, Connection conn)
        throws SQLException {
	PreparedStatement ps = conn.prepareStatement(
	"DELETE FROM " + GMLHSQLIndex.TABLE_NAME + 
	" WHERE DOCUMENT_URI = ? AND NODE_ID_UNITS = ? AND NODE_ID = ?;"
	); 
	ps.setString(1, doc.getURI().toString());
	ps.setInt(2, nodeId.units());
	byte[] bytes = new byte[nodeId.size()];
	nodeId.serialize(bytes, 0);        
	ps.setBytes(3, bytes);
	try {	 
	  return (ps.executeUpdate() == 1);
	} finally {
	  if (ps != null)
	    ps.close();
        }
      }</pre>
            <p>This one even more so:</p>
            <pre>protected int removeDocument(DocumentImpl doc, Connection conn)
        throws SQLException {    	
	PreparedStatement ps = conn.prepareStatement(
	"DELETE FROM " + GMLHSQLIndex.TABLE_NAME + " WHERE DOCUMENT_URI = ?;"
	); 
	ps.setString(1, doc.getURI().toString());
	try {
	  return ps.executeUpdate();	 
	} finally {
	  if (ps != null)
	    ps.close();
	}
      }</pre>
            <p>This one however, is a little bit trickier:</p>
            <pre>protected int removeCollection(Collection collection, Connection conn)
        throws SQLException {
	PreparedStatement ps = conn.prepareStatement(
	"DELETE FROM " + GMLHSQLIndex.TABLE_NAME + " WHERE SUBSTRING(DOCUMENT_URI, 1, ?) = ?;"
	); 
	ps.setInt(1, collection.getURI().toString().length());
	ps.setString(2, collection.getURI().toString());
	try {
	  return ps.executeUpdate();
	} finally {
	  if (ps != null)
	  ps.close();
	}
      }</pre>
            <p>This is because it makes use of a SQL function to filter the correct
                documents.</p>
            <p>The two next methods are straightforward, now that we have explained that
                <ph>Connection</ph>s had to be requested from the <ph>Index</ph>
                to avoid concurrency problems on an embedded HSQLDB instance.</p>
            <pre>protected Connection acquireConnection()
        throws SQLException {   
	return index.acquireConnection(this.broker);
      }

      protected void releaseConnection(Connection conn)
        throws SQLException {   
	index.releaseConnection(this.broker);
      }</pre>
            <p>The next method is much more interesting. This is where is the core of the spatial
                index is:</p>
            <pre>protected NodeSet search(DBBroker broker, NodeSet contextSet, Geometry EPSG4326_geometry, int spatialOp, Connection conn)
        throws SQLException {
	String extraSelection = null;
	String bboxConstraint = null;    	
	switch (spatialOp) {
	  //BBoxes are equal
	  case SpatialOperator.EQUALS:
	    bboxConstraint = "(EPSG4326_MINX = ? AND EPSG4326_MAXX = ?)" +
	    " AND (EPSG4326_MINY = ? AND EPSG4326_MAXY = ?)";
	    break;
	  //Nothing much we can do with the BBox at this stage
	  case SpatialOperator.DISJOINT:
	    //Retrieve the BBox though...
	    extraSelection = ", EPSG4326_MINX, EPSG4326_MAXX, EPSG4326_MINY, EPSG4326_MAXY";
	    break;
	  //BBoxes intersect themselves
	  case SpatialOperator.INTERSECTS:        		
	  case SpatialOperator.TOUCHES:        		   		
	  case SpatialOperator.CROSSES:        		      		
	  case SpatialOperator.OVERLAPS: 
	    bboxConstraint = "(EPSG4326_MAXX &gt;= ? AND EPSG4326_MINX &lt;= ?)" +
	    " AND (EPSG4326_MAXY &gt;= ? AND EPSG4326_MINY &lt;= ?)";
	    break;
	  //BBox is fully within
	  case SpatialOperator.WITHIN:   
	    bboxConstraint = "(EPSG4326_MINX &gt;= ? AND EPSG4326_MAXX &lt;= ?)" +
	    " AND (EPSG4326_MINY &gt;= ? AND EPSG4326_MAXY &lt;= ?)";
	    break;
	  //BBox fully contains
	  case SpatialOperator.CONTAINS: 
	    bboxConstraint = "(EPSG4326_MINX &lt;= ? AND EPSG4326_MAXX &gt;= ?)" +
	    " AND (EPSG4326_MINY &lt;= ? AND EPSG4326_MAXY &gt;= ?)";
	    break;       		
	  default:
	    throw new IllegalArgumentException("Unsupported spatial operator:" + spatialOp);
        }
	PreparedStatement ps = conn.prepareStatement(
	"SELECT EPSG4326_WKB, DOCUMENT_URI, NODE_ID_UNITS, NODE_ID" + (extraSelection == null ? "" : extraSelection) +
	" FROM " + GMLHSQLIndex.TABLE_NAME + 
	(bboxConstraint == null ? "" : " WHERE " + bboxConstraint) + ";"
	);
	if (bboxConstraint != null) {
	  ps.setDouble(1, EPSG4326_geometry.getEnvelopeInternal().getMinX());
	  ps.setDouble(2, EPSG4326_geometry.getEnvelopeInternal().getMaxX());
	  ps.setDouble(3, EPSG4326_geometry.getEnvelopeInternal().getMinY());
	  ps.setDouble(4, EPSG4326_geometry.getEnvelopeInternal().getMaxY());
	}
	ResultSet rs = null;
	NodeSet result = null;
	try { 
	  int disjointPostFiltered = 0;
	  rs = ps.executeQuery();
	  result = new ExtArrayNodeSet(); //new ExtArrayNodeSet(docs.getLength(), 250)
	  while (rs.next()) {
	    DocumentImpl doc = null;
	    try {
	      doc = (DocumentImpl)broker.getXMLResource(XmldbURI.create(rs.getString("DOCUMENT_URI")));        			
	    } catch (PermissionDeniedException e) {
	      LOG.debug(e);
	      //Ignore since the broker has no right on the document
	      continue;
	    }
	    //contextSet == null should be use to scan the whole index
	    if (contextSet == null || contextSet.getDocumentSet().contains(doc.getDocId())) {
	      NodeId nodeId = new DLN(rs.getInt("NODE_ID_UNITS"), rs.getBytes("NODE_ID"), 0); 
	      NodeProxy p = new NodeProxy((DocumentImpl)doc, nodeId);
	      if (contextSet == null || contextSet.get(p) != null) {
	        boolean geometryMatches = false;
		if (spatialOp == SpatialOperator.DISJOINT) {
		  //No BBox intersection : obviously disjoint
		  if (rs.getDouble("EPSG4326_MAXX") &lt; EPSG4326_geometry.getEnvelopeInternal().getMinX() ||	        			
		  rs.getDouble("EPSG4326_MINX") &gt; EPSG4326_geometry.getEnvelopeInternal().getMaxX() ||	        			
		  rs.getDouble("EPSG4326_MAXY") &lt; EPSG4326_geometry.getEnvelopeInternal().getMinY() ||	        			
		  rs.getDouble("EPSG4326_MINY") &gt; EPSG4326_geometry.getEnvelopeInternal().getMaxY()) {
		    geometryMatches = true;
		    disjointPostFiltered++;
		  }
		}
		//Possible match : check the geometry
		if (!geometryMatches) {	
		  try {	        	
		    Geometry geometry = wkbReader.read(rs.getBytes("EPSG4326_WKB"));
		    switch (spatialOp) {
		      case SpatialOperator.EQUALS:
		        geometryMatches = geometry.equals(EPSG4326_geometry);
			break;
		      case SpatialOperator.DISJOINT:        		
		        geometryMatches = geometry.disjoint(EPSG4326_geometry);
		        break;  		
		      case SpatialOperator.INTERSECTS:        		
		        geometryMatches = geometry.intersects(EPSG4326_geometry);
		        break;
		      case SpatialOperator.TOUCHES:
		        geometryMatches = geometry.touches(EPSG4326_geometry);
		        break; 		
		      case SpatialOperator.CROSSES:
		        geometryMatches = geometry.crosses(EPSG4326_geometry);
		        break;
		      case SpatialOperator.WITHIN:        		
		        geometryMatches = geometry.within(EPSG4326_geometry);
		        break; 		
		      case SpatialOperator.CONTAINS:	        		
		        geometryMatches = geometry.contains(EPSG4326_geometry);
		        break;
		      case SpatialOperator.OVERLAPS:	        		
		        geometryMatches = geometry.overlaps(EPSG4326_geometry);
		        break;
		    }
		  } catch (ParseException e) {
		    //Transforms the exception into an SQLException.
		    //Very unlikely to happen though...
		    SQLException ee = new SQLException(e.getMessage());
		    ee.initCause(e);
		    throw ee;
		  }
		}
		if (geometryMatches)        	
		  result.add(p);
	      }
	    }
	  }
	  if (LOG.isDebugEnabled()) {
	    LOG.debug(rs.getRow() + " eligible geometries, " + result.getItemCount() + "selected" +
	    (spatialOp == SpatialOperator.DISJOINT ? "(" + disjointPostFiltered + " post filtered)" : ""));
	    }
	    return result;	    	
        } finally { 
          if (rs != null)
	    rs.close();
	  if (ps != null)
	    ps.close();	    		
        }
      }</pre>
            <p>The trick is to filter the geometries on (fast) BBox operations first
                (intersecting geometries have BBox intersecting as well) which is possible in every
                case but for the <ph>Spatial.DISJOINT</ph> operator. For the latter case,
                we will have to fetch the BBox coordinates in order to apply a further filtering.
                Then, we examine the results and filter out the documents that are not in the
                <ph>contextSet</ph>. <ph>Spatial.DISJOINT</ph> filtering is then
                applied to avoid the next step in case the BBoxes are themselves disjoint. Only
                then, we perform the costly operations, namely <ph>Geometry</ph>
                deserialization from the DB then performing spatial operations on it. Matching nodes
                are then returned.</p>
            <p>The next method is quite straightforward:</p>
            <pre>protected Map getGeometriesForDocument(DocumentImpl doc, Connection conn)
        throws SQLException {
	PreparedStatement ps = conn.prepareStatement(
	"SELECT EPSG4326_WKB, EPSG4326_WKT FROM " + GMLHSQLIndex.TABLE_NAME + " WHERE DOCUMENT_URI = ?;"
	); 
	ps.setString(1, doc.getURI().toString());
	ResultSet rs = null;
	try {
	  rs = ps.executeQuery();
	  Map map = new TreeMap();
	  while (rs.next()) {
	    Geometry EPSG4326_geometry = wkbReader.read(rs.getBytes("EPSG4326_WKB"));
	    //Returns the EPSG:4326 WKT for every geometry to make occurrence aggregation consistent
	    map.put(EPSG4326_geometry, rs.getString("EPSG4326_WKT"));
	  }
	  return map;
	} catch (ParseException e) {
	  //Transforms the exception into an SQLException.
	  //Very unlikely to happen though...
	  SQLException ee = new SQLException(e.getMessage());
	  ee.initCause(e);
	  throw ee;
	} finally {   
	  if (rs != null)
	    rs.close();
	  if (ps != null)
	    ps.close();
	}
      }</pre>
            <p>Notice that it will return EPSG:4326 <ph>Geometry</ph>ies and that it
                will rethrow a <ph>com.vividsolutions.jts.io.ParseException</ph> as a
                <ph>java.sql.SQLException</ph>.</p>
            <p>The next method is a bit more restrictive and modular:</p>
            <pre>protected Geometry getGeometryForNode(DBBroker broker, NodeProxy p, boolean getEPSG4326, Connection conn)
        throws SQLException {
	PreparedStatement ps = conn.prepareStatement(
	"SELECT " + (getEPSG4326 ? "EPSG4326_WKB" : "WKB") +
	" FROM " + GMLHSQLIndex.TABLE_NAME + 
	" WHERE DOCUMENT_URI = ? AND NODE_ID_UNITS = ? AND NODE_ID = ?;"
	);
	ps.setString(1, p.getDocument().getURI().toString());
	ps.setInt(2, p.getNodeId().units());
	byte[] bytes = new byte[p.getNodeId().size()];
	p.getNodeId().serialize(bytes, 0);
	ps.setBytes(3, bytes);   
	ResultSet rs = null;    	
	try {
	  rs = ps.executeQuery();
	  if (!rs.next())
	    //Nothing returned
	    return null;    		
	  Geometry geometry = wkbReader.read(rs.getBytes(1));        			
	  if (rs.next()) {   	
	    //Should be impossible    		
	    throw new SQLException("More than one geometry for node " + p);
	  }
	  return geometry;    
	} catch (ParseException e) {
	  //Transforms the exception into an SQLException.
	  //Very unlikely to happen though...
	  SQLException ee = new SQLException(e.getMessage());
	  ee.initCause(e);
	  throw ee;
	} finally {
	  if (rs != null)
	    rs.close();
	  if (ps != null)
	    ps.close();
        }
      }</pre>
            <p>It directly selects the right node and allows to return either the original
                <ph>Geometry</ph>, either its EPSG:4326 transformation.</p>
            <p>The next method is a generalization of the previous one:</p>
            <pre>protected Geometry[] getGeometriesForNodes(DBBroker broker, NodeSet contextSet, boolean getEPSG4326, Connection conn)
        throws SQLException {
	PreparedStatement ps = conn.prepareStatement(
	"SELECT " + (getEPSG4326 ? "EPSG4326_WKB" : "WKB") + ", DOCUMENT_URI, NODE_ID_UNITS, NODE_ID" +
	" FROM " + GMLHSQLIndex.TABLE_NAME 
	);
	ResultSet rs = null;    	
	try {
	  rs = ps.executeQuery();
	  Geometry[] result = new Geometry[contextSet.getLength()];
	  int index= 0;
	  while (rs.next()) {
	    DocumentImpl doc = null;
	    try {
	      doc = (DocumentImpl)broker.getXMLResource(XmldbURI.create(rs.getString("DOCUMENT_URI")));        			
	    } catch (PermissionDeniedException e) {
	      LOG.debug(e);
	      result[index++] = null;
	      //Ignore since the broker has no right on the document
	      continue;
	    }
	    if (contextSet.getDocumentSet().contains(doc.getDocId())) {
	      NodeId nodeId = new DLN(rs.getInt("NODE_ID_UNITS"), rs.getBytes("NODE_ID"), 0); 
	      NodeProxy p = new NodeProxy((DocumentImpl)doc, nodeId);
	      if (contextSet.get(p) != null) {
	        Geometry geometry = wkbReader.read(rs.getBytes(1));
		result[index++] = geometry;
	      }
            }
          }
	  return result;
	} catch (ParseException e) {
        //Transforms the exception into an SQLException.
	//Very unlikely to happen though...
	SQLException ee = new SQLException(e.getMessage());
	ee.initCause(e);
	throw ee;
        } finally {   
          if (rs != null)
	    rs.close();
	  if (ps != null)
	    ps.close();
	}
      }</pre>
            <p>It queries the whole index for the requested <ph>Geometry</ph>, ignoring
                the documents that are not in the <ph>contextSet</ph>, and it also ignores
                the nodes that are not in the <ph>contextSet</ph>. After that the
                <ph>Geometry</ph> is deserialized.</p>
            <note type="note">
                    <p>This method is not yet used by the spatial functions but it is planned to use
                    it in a future optimization effort.</p>
                </note>
            <p>This is the next method, designed like
                <ph>getGeometryForNode()</ph>:</p>
            <pre>protected AtomicValue getGeometricPropertyForNode(DBBroker broker, NodeProxy p, Connection conn, String propertyName)
        throws SQLException, XPathException {
	PreparedStatement ps = conn.prepareStatement(
	"SELECT " + propertyName + 
	" FROM " + GMLHSQLIndex.TABLE_NAME + 
	" WHERE DOCUMENT_URI = ? AND NODE_ID_UNITS = ? AND NODE_ID = ?"
	);
	ps.setString(1, p.getDocument().getURI().toString());
	ps.setInt(2, p.getNodeId().units());
	byte[] bytes = new byte[p.getNodeId().size()];
	p.getNodeId().serialize(bytes, 0);
	ps.setBytes(3, bytes);    	
	ResultSet rs = null;    	
	try {
	  rs = ps.executeQuery();
	  if (!rs.next())
	    //Nothing returned
	    return AtomicValue.EMPTY_VALUE;
          AtomicValue result = null;
	  if (rs.getMetaData().getColumnClassName(1).equals(Boolean.class.getName())) {
	    result = new BooleanValue(rs.getBoolean(1));
	  } else if (rs.getMetaData().getColumnClassName(1).equals(Double.class.getName())) {
	    result = new DoubleValue(rs.getDouble(1));
	  } else if (rs.getMetaData().getColumnClassName(1).equals(String.class.getName())) {
	    result = new StringValue(rs.getString(1));
	  } else if (rs.getMetaData().getColumnType(1) == java.sql.Types.BINARY) {
	    result = new Base64Binary(rs.getBytes(1));
	  } else 
	    throw new SQLException("Unable to make an atomic value from '" + rs.getMetaData().getColumnClassName(1) + "'");		
	  if (rs.next()) {   	
	    //Should be impossible    		
	    throw new SQLException("More than one geometry for node " + p);
	  }
	  return result;    
	} finally {   
	  if (rs != null)
	    rs.close();
	  if (ps != null)
	    ps.close();
	}
      }</pre>
            <p>It directly requests the required property from the DB and returns an appropriate
                XML atomic value.</p>
            <p>The next method is a generalization of the previous one:</p>
            <pre>protected ValueSequence getGeometricPropertyForNodes(DBBroker broker, NodeSet contextSet, Connection conn, String propertyName)
        throws SQLException, XPathException {
	PreparedStatement ps = conn.prepareStatement(
	"SELECT " + propertyName + ", DOCUMENT_URI, NODE_ID_UNITS, NODE_ID" + 
	" FROM " + GMLHSQLIndex.TABLE_NAME
	);
	ResultSet rs = null;    	
	try {
	  rs = ps.executeQuery();
	  ValueSequence result = new ValueSequence(contextSet.getLength());    		
	  while (rs.next()) {
	    DocumentImpl doc = null;
	    try {
	      doc = (DocumentImpl)broker.getXMLResource(XmldbURI.create(rs.getString("DOCUMENT_URI")));        			
	    } catch (PermissionDeniedException e) {
	      LOG.debug(e);				
	      if (rs.getMetaData().getColumnClassName(1).equals(Boolean.class.getName())) {
	        result.add(BooleanValue.EMPTY_VALUE);
	      } else if (rs.getMetaData().getColumnClassName(1).equals(Double.class.getName())) {
	        result.add(DoubleValue.EMPTY_VALUE);
	      } else if (rs.getMetaData().getColumnClassName(1).equals(String.class.getName())) {
	        result.add(StringValue.EMPTY_VALUE);
	      } else if (rs.getMetaData().getColumnType(1) == java.sql.Types.BINARY) {
	        result.add(Base64Binary.EMPTY_VALUE);
              } else 
	        throw new SQLException("Unable to make an atomic value from '" + rs.getMetaData().getColumnClassName(1) + "'");
	      //Ignore since the broker has no right on the document
	      continue;
	    }
	    if (contextSet.getDocumentSet().contains(doc.getDocId())) {
	      NodeId nodeId = new DLN(rs.getInt("NODE_ID_UNITS"), rs.getBytes("NODE_ID"), 0); 
	      NodeProxy p = new NodeProxy((DocumentImpl)doc, nodeId);
	      if (contextSet.get(p) != null) {
	        if (rs.getMetaData().getColumnClassName(1).equals(Boolean.class.getName())) {
		  result.add(new BooleanValue(rs.getBoolean(1)));
	        } else if (rs.getMetaData().getColumnClassName(1).equals(Double.class.getName())) {
	          result.add(new DoubleValue(rs.getDouble(1)));
	        } else if (rs.getMetaData().getColumnClassName(1).equals(String.class.getName())) {
	          result.add(new StringValue(rs.getString(1)));
	        } else if (rs.getMetaData().getColumnType(1) == java.sql.Types.BINARY) {
	          result.add(new Base64Binary(rs.getBytes(1)));
                } else 
	          throw new SQLException("Unable to make an atomic value from '" + rs.getMetaData().getColumnClassName(1) + "'");
	    }
	  }
        }
        return result;    
        } finally {   
          if (rs != null)
	    rs.close();
	  if (ps != null)
	    ps.close();
        }
      }</pre>
            <p>It queries the whole index for the requested property, ignoring the documents that
                are not in the <ph>contextSet</ph>, and it also ignores the nodes that are
                not in the <ph>contextSet</ph>. Finally the property mapped to the
                appropriate XML atomic value is returned.</p>
            <note type="note">
                    <p>This method is not yet used by the spatial functions but it is planned to use
                    it in a future optimization effort.</p>
                </note>
            <p>The last method is a utility method and we will only show a part of its
                body:</p>
            <pre>protected boolean checkIndex(DBBroker broker, Connection conn)
        throws SQLException {
	PreparedStatement ps = conn.prepareStatement(
	"SELECT * FROM " + GMLHSQLIndex.TABLE_NAME + ";"
	);
	ResultSet rs = null;
	try {
	  rs = ps.executeQuery();
	  while (rs.next()) {	        	
	    Geometry original_geometry = wkbReader.read(rs.getBytes("WKB"));
	    if (!original_geometry.equals(wktReader.read(rs.getString("WKT")))) {
	      LOG.info("Inconsistent WKT : " + rs.getString("WKT"));
	      return false;
            }		            	
	    Geometry EPSG4326_geometry = wkbReader.read(rs.getBytes("EPSG4326_WKB"));		        	
	    if (!EPSG4326_geometry.equals(wktReader.read(rs.getString("EPSG4326_WKT")))) {
	      LOG.info("Inconsistent WKT : " + rs.getString("EPSG4326_WKT"));
	      return false;
	    }
      
	    if (!original_geometry.getGeometryType().equals(rs.getString("GEOMETRY_TYPE"))) {
	      LOG.info("Inconsistent geometry type: " + rs.getDouble("GEOMETRY_TYPE"));
	      return false;
	    }
	    
	    if (original_geometry.getEnvelopeInternal().getMinX() != rs.getDouble("MINX")) {
	      LOG.info("Inconsistent MinX: " + rs.getDouble("MINX"));
	      return false;
	    }

        ...

	DocumentImpl doc = null;
	try {
	  doc = (DocumentImpl)broker.getXMLResource(XmldbURI.create(rs.getString("DOCUMENT_URI")));
	} catch (PermissionDeniedException e) {
	  //The broker has no right on the document
	  LOG.error(e);
	  return false;
        }
	NodeId nodeId = new DLN(rs.getInt("NODE_ID_UNITS"), rs.getBytes("NODE_ID"), 0); 	    		
	StoredNode node = broker.objectWith(new NodeProxy((DocumentImpl)doc, nodeId));
	if (node == null) {
	  LOG.info("Node " + nodeId + "doesn't exist");
	  return false;
	}      
	if (!GMLHSQLIndexWorker.GML_NS.equals(node.getNamespaceURI())) {
	  LOG.info("GML indexed node (" + node.getNodeId()+ ") is in the '" + 
	  node.getNamespaceURI() + "' namespace. '" + 
	  GMLHSQLIndexWorker.GML_NS + "' was expected !");
	  return false;
	}
	if (!original_geometry.getGeometryType().equals(node.getLocalName())) {
	  if ("Box".equals(node.getLocalName()) &amp;&amp; "Polygon".equals(original_geometry.getGeometryType())) {
	    LOG.debug("GML indexed node (" + node.getNodeId() + ") is a gml:Box indexed as a polygon");
	  } else {
	    LOG.info("GML indexed node (" + node.getNodeId() + ") has '" + 
	    node.getLocalName() + "' as its local name. '" + 
	    original_geometry.getGeometryType() + "' was expected !");
	    return false;
	  }
        }
      
	LOG.info(node);	        		
	}
	return true;
      
        } catch (ParseException e) {
          //Transforms the exception into an SQLException.
	  //Very unlikely to happen though...
	  SQL Exception ee = new SQLException(e.getMessage());
	  ee.initCause(e);
	  throw ee;
	} finally {   
	  if (rs != null)
            rs.close();
          if (ps != null)
            ps.close();	
        }
      }</pre>
            <p>It deserializes each <ph>Geometry</ph> and checks that its data are
                consistent with what is stored in the DB.</p>
        </section>

        

        <section id="writing-stream-listener">
                <title>Writing a concrete implementation of org.exist.indexing.StreamListener </title>
            

            <p>The <ph>StreamListener</ph>'s main purpose is to generate
                <ph>Geometry</ph> instances, if accurate, from the nodes it listens
                to.</p>
            <p>This will be done using a <ph>org.geotools.gml.GMLFilterDocument</ph>
                provided by the Geotools libraries. The trick is to map our STAX events to the
                expected SAX events.</p>
            <p>As stated above, our <ph>StreamListener</ph> will be implemented as an
                inner class of
                <ph>org.exist.indexing.spatial.AbstractGMLJDBCIndexWorker</ph>. Of course,
                it will extend <ph>org.exist.indexing.AbstractStreamListener</ph>:</p>
            <pre>private class GMLStreamListener extends AbstractStreamListener {
      
        Stack srsNamesStack = new Stack();
	ElementImpl deferredElement;
      
	public IndexWorker getWorker() {
	  return AbstractGMLJDBCIndexWorker.this;
	}
      }</pre>
            <p>There are only two members. <ph>srsNamesStack</ph> will maintain a
                (<ph>String</ph>) <ph>java.util.Stack</ph> for the
                <ph>srsName</ph> attribute of the elements in the GML namespace
                (http://www.opengis.net/gml). <ph>null</ph> will be pushed if such an
                attribute doesn't exist, namely because it isn't accurate.</p>
            <p> <ph>deferredElement</ph> will hold an element whose streaming is
                deferred, namely because we still haven't received its attributes.</p>
            <p>The <ph>getWorker()</ph> method should be straightforward.</p>
            <p>Let's see how the process is performed:</p>
            <pre>public void startElement(Txn transaction, ElementImpl element, NodePath path) { 
        if (isDocumentGMLAware) {
	  //Release the deferred element if any
	  if (deferredElement != null)        		
	    processDeferredElement();
	  //Retain this element
	  deferredElement = element;  
	}
	//Forward the event to the next listener 
	super.startElement(transaction, element, path);
      }
      
      public void attribute(Txn transaction, AttrImpl attrib, NodePath path) { 
        //Forward the event to the next listener 
	super.attribute(transaction, attrib, path);
      }        

      public void characters(Txn transaction, TextImpl text, NodePath path) {
        if (isDocumentGMLAware) {
	  //Release the deferred element if any
	  if (deferredElement != null)        		
	    processDeferredElement();	        	
	  try {
	    geometryDocument.characters(text.getData().toCharArray(), 0, text.getLength());
	  } catch (Exception e) {
	    LOG.error(e);
	  } 
	}
	//Forward the event to the next listener 
	super.characters(transaction, text, path);
      }

      public void endElement(Txn transaction, ElementImpl element, NodePath path) {
        if (isDocumentGMLAware) {   
	  //Release the deferred element if any
	  if (deferredElement != null)        		
	    processDeferredElement();	        	
	  //Process the element 
	  processCurrentElement(element);
	}
        //Forward the event to the next listener 
	super.endElement(transaction, element, path);
      }</pre>
            <p>Element deferring occurs only if <ph>currentDoc</ph> is to be indexed of
                course. If so, an incoming element is deferred but we do not forget to forward the
                event to the next <ph>StreamListener</ph> in the pipeline.</p>
            <p>If we have a deferred element, we will process it (see below) in order to collect
                its attributes and if relevant, <ph>endElement()</ph>, will add an index
                entry for the current element. The method <ph>characters()</ph> also
                forwards its data to the SAX handler.</p>
            <note type="note">
                    <p>We could have used <ph>attribute()</ph> to collect the deferred
                    element's attributes. The described design is just a matter of choice.</p>
                </note>
            <p>Let's see how the deferred element is processed:</p>
            <pre>private void processDeferredElement() {  
        //We need to collect the deferred element's attributes in order to feed the SAX handler
	AttributesImpl attList = new AttributesImpl();
	NamedNodeMap attrs = deferredElement.getAttributes();
      
	String whatToPush = null;
	
	for (int i = 0; i &lt; attrs.getLength() ; i++) {
	  AttrImpl attrib = (AttrImpl)attrs.item(i);
	  
	  //Store the srs
	  if (GML_NS.equals(deferredElement.getNamespaceURI())) {
	    //Maybe we could assume a configurable default value here
	    if (attrib.getName().equals("srsName")) {        		
	      whatToPush = attrib.getValue();
	    }
	    srsNamesStack.push(whatToPush);
	  } 
      
	  attList.addAttribute(attrib.getNamespaceURI(), 
	  attrib.getLocalName(), 
	  attrib.getQName().getStringValue(), 
	  Integer.toString(attrib.getType()), 
	  attrib.getValue());            		
        } 
      
	srsNamesStack.push(whatToPush);

	try {
	  geometryDocument.startElement(deferredElement.getNamespaceURI(), deferredElement.getLocalName(), deferredElement.getQName().getStringValue(), attList);
	} catch (Exception e) {
	  e.printStackTrace();
	  LOG.error(e);
	} finally {
	  deferredElement = null;       	       	
	}
      }</pre>
            <p>We first need to collect its attributes and that's why it is deferred, because
                attributes events come <i>after</i> the call to
                <ph>startElement()</ph>. Elements in the GML namespace that carry an
                <ph>srsName</ph> attribute will push its value. If the element is not in
                the GML namespace or if no <ph>srsName</ph> attribute exists,
                <ph>null</ph> is pushed.</p>
            <note type="note">
                    <p>We could have had a smarter mechanism, but we first have to take a decision
                    about the fact that we could define a default SRS here, either from the config,
                    or from a higher-level element. This part of the code will thus probably be
                    revisited once the decision is taken.</p>
                </note>
            <p>When the attributes are collected, we can send a <ph>startElement()</ph>
                event to the SAX handler, thus marking the end of the deferring process.</p>
            <p>Processing of the current element with <ph>endElement()</ph>:</p>
            <pre>private void processCurrentElement(ElementImpl element) {
        String currentSrsName = (String)srsNamesStack.pop();        		
	try {
	  geometryDocument.endElement(element.getNamespaceURI(), element.getLocalName(), element.getQName().getStringValue());
	  //Some invalid/(yet) incomplete geometries don't have a SRS
	  if (streamedGeometry != null &amp;&amp; currentSrsName != null) {   
	    currentNodeId = element.getNodeId();
	    geometries.put(currentNodeId, new SRSGeometry(currentSrsName, streamedGeometry));		        		
	    if (flushAfter != -1 &amp;&amp; geometries.size() &gt;= flushAfter) {
	      //Mmmh... doesn't flush since it is currently dependant from the
	      //number of nodes in the DOM file ; would need refactorings
	      //currentDoc.getBroker().checkAvailableMemory();
	      currentDoc.getBroker().flush();
	      ///Aaaaaargl !
	      final double percent = ((double) Runtime.getRuntime().freeMemory() / (double) Runtime.getRuntime().maxMemory()) * 100;
	      if (percent &lt; 30) {	        		
	        System.gc();
	      }
            }
          }        		
	} catch (Exception e) {
	  LOG.error("Unable to collect geometry for node: " + currentNodeId + ". Indexing will be skipped", e);        		
	} finally {  
	  streamedGeometry = null;	        			
	}
      }</pre>
            <p>We first pop a SRS name from the stack. <ph>null</ph> will indicate that
                the element doesn't have any and thus that it is an element which doesn't carry
                enough information to build a complete geometry. That doesn't prevent us to forward
                this element to the SAX handler.</p>
            <p>The SAX handler might have been able to build a <ph>Geometry</ph> then.
                If so, the current index entry (composed of <ph>currentSrsName</ph>,
                <ph>streamedGeometry</ph>, <ph>currentNodeId</ph> and the
                "global" <ph>org.exist.dom.DocumentImpl</ph>) is added to
                <ph>geometries</ph> (wrapped in the convenience
                <ph>SRSGeometry</ph> class). We then check if it's time to flush the
                pending index entries.</p>
            <p>This is how the <ph>GeometryHandler</ph> looks like. It is also
                implemented as an inner class of
                <ph>org.exist.indexing.spatial.AbstractGMLJDBCIndexWorker</ph> </p>
            <pre>private class GeometryHandler extends XMLFilterImpl implements GMLHandlerJTS {
        public void geometry(Geometry geometry) {
	  streamedGeometry = geometry;      	
	  //TODO : null geometries can be returned for many reasons, including a (too) strict
	  //topology check done by the Geotools SAX parser.
	  //It would be nice to have static classes extending Geometry to report such geometries
	  if (geometry == null)
	    LOG.error("Collected null geometry for node: " + currentNodeId + ". Indexing will be skipped");
	}
      }</pre>
            <p>Thanks to Geotools SAX parser, it hasn't to be more complicated than setting the
                <ph>streamedGeometry</ph> "global" member.</p>
            <note type="note">
                    <p>However, it may throw some <ph>NullPointerException</ph>s as
                    described above.</p>
                </note>
        </section>

        

        <section id="implement-spatial">
                <title>Implementing some functions that cooperate with the spatial index</title>
            

            <p>We currently provide three sets of functions that are able to cooperate with
                spatial indexes.</p>
            <p>The functions are declared in the
                <ph>org.exist.xquery.modules.spatial.SpatialModule</ph> module which
                operates in the <ph>http://exist-db.org/xquery/spatial</ph> namespace
                (whose default prefix is <ph>spatial</ph>).</p>
            <p>The functions signatures are documented together with the functions themselves in
                <xref href="{${fundocs.pkg.abbrev}}" scope="external">the XQuery function documentation</xref>.
                Here we will only look at their <ph>eval()</ph> methods.</p>
            <p>The first functions set we will describe is
                <ph>org.exist.xquery.modules.spatial.FunSpatialSearch</ph>, which performs
                searches on the spatial index:</p>
            <pre>public Sequence eval(Sequence[] args, Sequence contextSequence) throws XPathException {
        Sequence result = null;
	Sequence nodes = args[0];        
	if (nodes.isEmpty())
	  result = Sequence.EMPTY_SEQUENCE;
	else if (args[1].isEmpty())
	  //TODO : to be discussed. We could also return an empty sequence here        	
	  result = nodes;
	else {
	  try {
	    AbstractGMLJDBCIndexWorker indexWorker = (AbstractGMLJDBCIndexWorker)        	
	    context.getBroker().getIndexController().getWorkerByIndexId(AbstractGMLJDBCIndex.ID);
	    if (indexWorker == null)
	      throw new XPathException("Unable to find a spatial index worker");
	    Geometry EPSG4326_geometry = null;
	    NodeValue geometryNode = (NodeValue) args[1].itemAt(0);   
	    if (geometryNode.getImplementationType() == NodeValue.PERSISTENT_NODE)
	      //Get the geometry from the index if available
	      EPSG4326_geometry = indexWorker.getGeometryForNode(context.getBroker(), (NodeProxy)geometryNode, true);
	    if (EPSG4326_geometry == null) {
	      String sourceCRS = ((Element)geometryNode.getNode()).getAttribute("srsName").trim();
	      Geometry geometry = indexWorker.streamNodeToGeometry(context, geometryNode);	            
	      EPSG4326_geometry = indexWorker.transformGeometry(geometry, sourceCRS, "EPSG:4326");	        		        
	    }
	    if (EPSG4326_geometry == null) 
	      throw new XPathException("Unable to get a geometry from the node");
	    int spatialOp = SpatialOperator.UNKNOWN;
	    if (isCalledAs("equals"))
	      spatialOp = SpatialOperator.EQUALS;
	    else if (isCalledAs("disjoint"))
	      spatialOp = SpatialOperator.DISJOINT;
            else if (isCalledAs("intersects"))
	      spatialOp = SpatialOperator.INTERSECTS;	 
	    else if (isCalledAs("touches"))
	      spatialOp = SpatialOperator.TOUCHES;
	    else if (isCalledAs("crosses"))
	      spatialOp = SpatialOperator.CROSSES;	   
	    else if (isCalledAs("within"))
	      spatialOp = SpatialOperator.WITHIN;		
	    else if (isCalledAs("contains"))
	      spatialOp = SpatialOperator.CONTAINS;		
            else if (isCalledAs("overlaps"))
	      spatialOp = SpatialOperator.OVERLAPS;
	    //Search the EPSG:4326 in the index
	    result = indexWorker.search(context.getBroker(),  nodes.toNodeSet(), EPSG4326_geometry, spatialOp);
	    hasUsedIndex = true;	        	
	  } catch (SpatialIndexException e) {
	    throw new XPathException(e);
	  }
	}
	return result;
      }</pre>
            <p>We first build an early result if empty sequences are passed to the
                function.</p>
            <p>Then, we try to access the <ph>XQueryContext</ph>'s
                <ph>AbstractGMLJDBCIndex</ph> (remember that there is a 1:1 relationship
                between <ph>XQueryContext</ph> and <ph>DBBroker</ph> and a 1:1
                relationship between <ph>DBBroker</ph> and
                <ph>IndexWorker</ph>). If we can not find an
                <ph>AbstractGMLJDBCIndex</ph> we throw an <ph>Exception</ph>
                since we will need this and its concrete class to delegate spatial operations to
                (whatever its underlying DB implementation is, thanks to our generic design).</p>
            <p>Then, we examine if the geometry node is persistent, in which case it
                <i>might</i> be indexed. If so, we try to get an EPSG:4326
                <ph>Geometry</ph> from the index.</p>
            <p>If nothing is returned here, either because the node isn't indexed or because it
                is an in-memory node, we stream it to a <ph>Geometry</ph> and we transform
                this into an EPSG:4326 <ph>Geometry</ph>. Of course, this process is
                slower than a direct lookup into the index.</p>
            <p>Then we search for the geometry in the index after having determined the spatial
                operator from the function's name.</p>
            <p>The second functions set is
                <ph>org.exist.xquery.modules.spatial.FunGeometricProperties</ph>, which
                retrieves a property for a <ph>Geometry</ph>:</p>
            <pre>public Sequence eval(Sequence[] args, Sequence contextSequence) throws XPathException {
        Sequence result = null;
	Sequence nodes = args[0];        
	if (nodes.isEmpty())
	  result = Sequence.EMPTY_SEQUENCE;
	else {
	  try {
	    Geometry geometry = null;
	    String sourceCRS = null;
	    AbstractGMLJDBCIndexWorker indexWorker = (AbstractGMLJDBCIndexWorker)
	    context.getBroker().getIndexController().getWorkerByIndexId(AbstractGMLJDBCIndex.ID);
	    if (indexWorker == null)
	      throw new XPathException("Unable to find a spatial index worker");
	    String propertyName = null;
	    if (isCalledAs("getWKT")) {
	      propertyName = "WKT";
	    } else if (isCalledAs("getWKB")) {
	      propertyName = "WKB";						
	    } else if (isCalledAs("getMinX")) {
	      propertyName = "MINX";						
	    } else if (isCalledAs("getMaxX")) {					
	      propertyName = "MAXX";
	    } else if (isCalledAs("getMinY")) {
	      propertyName = "MINY";
	    } else if (isCalledAs("getMaxY")) {
	      propertyName = "MAXY";
	    } else if (isCalledAs("getCentroidX")) {
	      propertyName = "CENTROID_X";
	    } else if (isCalledAs("getCentroidY")) {
	      propertyName = "CENTROID_Y";
	    } else if (isCalledAs("getArea")) {
	      propertyName = "AREA";
	    } else if (isCalledAs("getEPSG4326WKT")) {
	      propertyName = "EPSG4326_WKT";      
	    } else if (isCalledAs("getEPSG4326WKB")) {
	      propertyName = "EPSG4326_WKB";
	    } else if (isCalledAs("getEPSG4326MinX")) {
	      propertyName = "EPSG4326_MINX";
	    } else if (isCalledAs("getEPSG4326MaxX")) {
	      propertyName = "EPSG4326_MAXX";
	    } else if (isCalledAs("getEPSG4326MinY")) {
	      propertyName = "EPSG4326_MINY";
	    } else if (isCalledAs("getEPSG4326MaxY")) {
	      propertyName = "EPSG4326_MAXY";
	    } else if (isCalledAs("getEPSG4326CentroidX")) {
	      propertyName = "EPSG4326_CENTROID_X";
	    } else if (isCalledAs("getEPSG4326CentroidY")) {
	      propertyName = "EPSG4326_CENTROID_Y";
	    } else if (isCalledAs("getEPSG4326Area")) {
	      propertyName = "EPSG4326_AREA";
	    } else if (isCalledAs("getSRS")) {
	      propertyName = "SRS_NAME";
	    } else if (isCalledAs("getGeometryType")) {
	      propertyName = "GEOMETRY_TYPE";
	    } else if (isCalledAs("isClosed")) {
	      propertyName = "IS_CLOSED";
	    } else if (isCalledAs("isSimple")) {
	      propertyName = "IS_SIMPLE";
	    } else if (isCalledAs("isValid")) {
	      propertyName = "IS_VALID";
	    } else {
	      throw new XPathException("Unknown spatial property: " + mySignature.getName().getLocalName());
	    } 
	    NodeValue geometryNode = (NodeValue) nodes.itemAt(0);
	    if (geometryNode.getImplementationType() == NodeValue.PERSISTENT_NODE) {
	      if (propertyName != null) {
	        //The node should be indexed : get its property
		result = indexWorker.getGeometricPropertyForNode(context.getBroker(), (NodeProxy)geometryNode, propertyName);
		hasUsedIndex = true;
	      } else {
	        //Or, at least, its geometry for further processing
		if (propertyName.indexOf("EPSG4326") != Constants.STRING_NOT_FOUND) {
		  geometry = indexWorker.getGeometryForNode(context.getBroker(), (NodeProxy)geometryNode, true);
		  sourceCRS = "EPSG:4326";
		} else {
		  geometry = indexWorker.getGeometryForNode(context.getBroker(), (NodeProxy)geometryNode, false);
		  sourceCRS = indexWorker.getGeometricPropertyForNode(context.getBroker(), (NodeProxy)geometryNode, "SRS_NAME").getStringValue();
		}
	      }
	    }
	    if (result == null) {
	      //builds the geometry
	      if (geometry == null) {
	        sourceCRS = ((Element)geometryNode.getNode()).getAttribute("srsName").trim();
		geometry = indexWorker.streamNodeToGeometry(context, geometryNode);		            	
	      }	
	      if (geometry == null) 
	        throw new XPathException("Unable to get a geometry from the node");
	      //Transform the geometry to EPSG:4326 if relevant
	      if (propertyName.indexOf("EPSG4326") != Constants.STRING_NOT_FOUND) {
	      geometry = indexWorker.transformGeometry(geometry, sourceCRS, "EPSG:4326");
	      if (isCalledAs("getEPSG4326WKT")) {
	        result = new StringValue(wktWriter.write(geometry));
	      } else if (isCalledAs("getEPSG4326WKB")) {
	        result = new Base64Binary(wkbWriter.write(geometry));
	      } else if (isCalledAs("getEPSG4326MinX")) {
	        result = new DoubleValue(geometry.getEnvelopeInternal().getMinX());
	      } else if (isCalledAs("getEPSG4326MaxX")) {
	        result = new DoubleValue(geometry.getEnvelopeInternal().getMaxX());
	      } else if (isCalledAs("getEPSG4326MinY")) {
	        result = new DoubleValue(geometry.getEnvelopeInternal().getMinY());
              } else if (isCalledAs("getEPSG4326MaxY")) {
	        result = new DoubleValue(geometry.getEnvelopeInternal().getMaxY());
	      } else if (isCalledAs("getEPSG4326CentroidX")) {
	        result = new DoubleValue(geometry.getCentroid().getX());
	      } else if (isCalledAs("getEPSG4326CentroidY")) {
	        result = new DoubleValue(geometry.getCentroid().getY());
	      } else if (isCalledAs("getEPSG4326Area")) {
	        result = new DoubleValue(geometry.getArea());
	      }
	    } else if (isCalledAs("getWKT")) {
	      result = new StringValue(wktWriter.write(geometry));
	    } else if (isCalledAs("getWKB")) {
	      result = new Base64Binary(wkbWriter.write(geometry));
	    } else if (isCalledAs("getMinX")) {
	      result = new DoubleValue(geometry.getEnvelopeInternal().getMinX());
	    } else if (isCalledAs("getMaxX")) {
	      result = new DoubleValue(geometry.getEnvelopeInternal().getMaxX());
	    } else if (isCalledAs("getMinY")) {
	      result = new DoubleValue(geometry.getEnvelopeInternal().getMinY());
	    } else if (isCalledAs("getMaxY")) {
	      result = new DoubleValue(geometry.getEnvelopeInternal().getMaxY());
	    } else if (isCalledAs("getCentroidX")) {
	      result = new DoubleValue(geometry.getCentroid().getX());
	    } else if (isCalledAs("getCentroidY")) {
	      result = new DoubleValue(geometry.getCentroid().getY());
	    } else if (isCalledAs("getArea")) {
	      result = new DoubleValue(geometry.getArea());
	    } else if (isCalledAs("getSRS")) {
	      result = new StringValue(((Element)geometryNode).getAttribute("srsName"));
	    } else if (isCalledAs("getGeometryType")) {
	      result = new StringValue(geometry.getGeometryType());
	    } else if (isCalledAs("isClosed")) {
	      result = new BooleanValue(!geometry.isEmpty());
	    } else if (isCalledAs("isSimple")) {
	      result = new BooleanValue(geometry.isSimple());
	    } else if (isCalledAs("isValid")) {
	      result = new BooleanValue(geometry.isValid());
	    } else {
	      throw new XPathException("Unknown spatial property: " + mySignature.getName().getLocalName());
	    }
	  }
	  } catch (SpatialIndexException e) {
	    throw new XPathException(e);
	  }
        }
	return result;
      }</pre>
            <p>The design is very much the same: we build an early result if empty sequences are
                involved, we get a <ph>AbstractGMLJDBCIndex</ph>, then we set a
                <ph>propertyName</ph>, which is actually a SQL field name, depending on
                the function's name.</p>
            <p>An attempt to retrieve the field content from the DB is made and, if unsuccessful,
                we try to get the node's <ph>Geometry</ph> from the index.</p>
            <p>Then, if we still haven't got this <ph>Geometry</ph>, either because the
                node isn't indexed or because it is an in-memory node, we stream it to a
                <ph>Geometry</ph> and we transform this into an EPSG:4326
                <ph>Geometry</ph> if the function's name requires to do so.</p>
            <p>We then <i>dynamically</i> build the property to be returned.</p>
            <note type="note">
                    <p>This mechanism if far from being efficient compared to the index lookup, but
                    it shows how easy it would be to return a property which is not available in a
                    spatial index.</p>
                </note>
            <p>The third functions set,
                <ph>org.exist.xquery.modules.spatial.FunGMLProducers</ph>, uses the same
                design:</p>
            <pre>public Sequence eval(Sequence[] args, Sequence contextSequence)
        throws XPathException {
	Sequence result = null; 
	try {
	  AbstractGMLJDBCIndexWorker indexWorker = (AbstractGMLJDBCIndexWorker)
	  context.getBroker().getIndexController().getWorkerByIndexId(AbstractGMLJDBCIndex.ID);
	  if (indexWorker == null)
	    throw new XPathException("Unable to find a spatial index worker");
	  Geometry geometry = null;	        
	  String targetSRS = null;
	  if (isCalledAs("transform")) {
	    if (args[0].isEmpty())
	      result = Sequence.EMPTY_SEQUENCE;
	    else {
	      NodeValue geometryNode = (NodeValue) args[0].itemAt(0);
	      //Try to get the geometry from the index
	      String sourceSRS = null;
	      if (geometryNode.getImplementationType() == NodeValue.PERSISTENT_NODE) {
	        sourceSRS = indexWorker.getGeometricPropertyForNode(context.getBroker(), (NodeProxy)geometryNode, "SRS_NAME").getStringValue();
		geometry = indexWorker.getGeometryForNode(context.getBroker(), (NodeProxy)geometryNode, false);		        		
		hasUsedIndex = true;
	      }
	      //Otherwise, build it
	      if (geometry == null) {
	        sourceSRS = ((Element)geometryNode.getNode()).getAttribute("srsName").trim();
		geometry = indexWorker.streamNodeToGeometry(context, geometryNode);
	      }
	      if (geometry == null) 
	        throw new XPathException("Unable to get a geometry from the node");
		targetSRS = args[1].itemAt(0).getStringValue().trim();
      
		geometry = indexWorker.transformGeometry(geometry, sourceSRS, targetSRS);
	      }
	    } else if (isCalledAs("WKTtoGML")) {
	      if (args[0].isEmpty())
	        result = Sequence.EMPTY_SEQUENCE;
	      else {
	        String wkt = args[0].itemAt(0).getStringValue();
		WKTReader wktReader = new WKTReader();
		try {        	
		 geometry = wktReader.read(wkt);
		} catch (ParseException e) {
		throw new XPathException(e);	
	    }
	    if (geometry == null) 
	      throw new XPathException("Unable to get a geometry from the node");
	    targetSRS = args[1].itemAt(0).getStringValue().trim();
	  }
	} else if (isCalledAs("buffer")) {
	  if (args[0].isEmpty())
	    result = Sequence.EMPTY_SEQUENCE;
	  else {
	    NodeValue geometryNode = (NodeValue) args[0].itemAt(0);	        		        	
	    //Try to get the geometry from the index
	    if (geometryNode.getImplementationType() == NodeValue.PERSISTENT_NODE) {
	      targetSRS = indexWorker.getGeometricPropertyForNode(context.getBroker(), (NodeProxy)geometryNode, "SRS_NAME").getStringValue();
	      geometry = indexWorker.getGeometryForNode(context.getBroker(), (NodeProxy)geometryNode, false);		        		
	      hasUsedIndex = true;
	    }
	    //Otherwise, build it
	    if (geometry == null) {
	      targetSRS =  ((Element)geometryNode.getNode()).getAttribute("srsName").trim();
	      geometry = indexWorker.streamNodeToGeometry(context, geometryNode);
	    }
	    if (geometry == null) 
	      throw new XPathException("Unable to get a geometry from the node");
	    double distance = ((DoubleValue)args[1].itemAt(0)).getDouble();
	    int quadrantSegments = 8;	
	    int endCapStyle = BufferOp.CAP_ROUND;
	    if (getArgumentCount() &gt; 2 &amp;&amp; Type.subTypeOf(args[2].itemAt(0).getType(), Type.INTEGER))
	      quadrantSegments = ((IntegerValue)args[2].itemAt(0)).getInt();
	    if (getArgumentCount() &gt; 3 &amp;&amp; Type.subTypeOf(args[3].itemAt(0).getType(), Type.INTEGER))
	      endCapStyle = ((IntegerValue)args[3].itemAt(0)).getInt();
	    switch (endCapStyle) {
	      case BufferOp.CAP_ROUND:
	      case BufferOp.CAP_BUTT:
	      case BufferOp.CAP_SQUARE:
	        //OK
		break;
	      default:
	        throw new XPathException("Invalid line end style");	
	    }        	
	    geometry = geometry.buffer(distance, quadrantSegments, endCapStyle);
	  }
	} else if (isCalledAs("getBbox")) {
	  if (args[0].isEmpty())
	    result = Sequence.EMPTY_SEQUENCE;
	  else {
	    NodeValue geometryNode = (NodeValue) args[0].itemAt(0);
	    //Try to get the geometry from the index
	    if (geometryNode.getImplementationType() == NodeValue.PERSISTENT_NODE) {
	      targetSRS = indexWorker.getGeometricPropertyForNode(context.getBroker(), (NodeProxy)geometryNode, "SRS_NAME").getStringValue();
	      geometry = indexWorker.getGeometryForNode(context.getBroker(), (NodeProxy)geometryNode, false);		        		
	      hasUsedIndex = true;
	    }
	    //Otherwise, build it
	    if (geometry == null) {
	      targetSRS = ((Element)geometryNode.getNode()).getAttribute("srsName").trim();
	      geometry = indexWorker.streamNodeToGeometry(context, geometryNode);		            	
	    }
	    if (geometry == null) 
	      throw new XPathException("Unable to get a geometry from the node");
	      
	    geometry = geometry.getEnvelope();
	  }
	} else if (isCalledAs("convexHull")) {
	  if (args[0].isEmpty())
	    result = Sequence.EMPTY_SEQUENCE;
	  else {
	    NodeValue geometryNode = (NodeValue) args[0].itemAt(0);
	    //Try to get the geometry from the index
	    if (geometryNode.getImplementationType() == NodeValue.PERSISTENT_NODE) {
	      targetSRS = indexWorker.getGeometricPropertyForNode(context.getBroker(), (NodeProxy)geometryNode, "SRS_NAME").getStringValue();
	      geometry = indexWorker.getGeometryForNode(context.getBroker(), (NodeProxy)geometryNode, false);		        		
	      hasUsedIndex = true;
	    }
	    //Otherwise, build it
	    if (geometry == null) {
	      targetSRS = ((Element)geometryNode.getNode()).getAttribute("srsName").trim();
	      geometry = indexWorker.streamNodeToGeometry(context, geometryNode);
	    }
	    if (geometry == null) 
	      throw new XPathException("Unable to get a geometry from the node");
	      
	    geometry = geometry.convexHull();
	  }
	} else if (isCalledAs("boundary")) {
	  if (args[0].isEmpty())
	    result = Sequence.EMPTY_SEQUENCE;
	  else {
	    NodeValue geometryNode = (NodeValue) args[0].itemAt(0);
	    //Try to get the geometry from the index
	    if (geometryNode.getImplementationType() == NodeValue.PERSISTENT_NODE) {
	      targetSRS = indexWorker.getGeometricPropertyForNode(context.getBroker(), (NodeProxy)geometryNode, "SRS_NAME").getStringValue();
	      geometry = indexWorker.getGeometryForNode(context.getBroker(), (NodeProxy)geometryNode, false);		        		
	      hasUsedIndex = true;
	    }
	    //Otherwise, build it
	    if (geometry == null) {
	      targetSRS = ((Element)geometryNode.getNode()).getAttribute("srsName").trim();
	      geometry = indexWorker.streamNodeToGeometry(context, geometryNode);
	    }
	    if (geometry == null) 
	      throw new XPathException("Unable to get a geometry from the node");
	      
	    geometry = geometry.getBoundary();		        	
	  }
	} else {
	  Geometry geometry1 = null;
	  Geometry geometry2 = null;
	  if (args[0].isEmpty() &amp;&amp; args[1].isEmpty())
	    result = Sequence.EMPTY_SEQUENCE;
	  else if (!args[0].isEmpty() &amp;&amp; args[1].isEmpty())
	    result = args[0].itemAt(0).toSequence();
	  else if (args[0].isEmpty() &amp;&amp; !args[1].isEmpty())
	    result = args[1].itemAt(0).toSequence();
	  else {
	    NodeValue geometryNode1 = (NodeValue) args[0].itemAt(0);
	    NodeValue geometryNode2 = (NodeValue) args[1].itemAt(0);
	    String srsName1 = null;
	    String srsName2 = null;
	    //Try to get the geometries from the index
	    if (geometryNode1.getImplementationType() == NodeValue.PERSISTENT_NODE) {
	      srsName1 = indexWorker.getGeometricPropertyForNode(context.getBroker(), (NodeProxy)geometryNode1, "SRS_NAME").getStringValue();
	      geometry1 = indexWorker.getGeometryForNode(context.getBroker(), (NodeProxy)geometryNode1, false);		        		
	      hasUsedIndex = true;
	    }
	    if (geometryNode2.getImplementationType() == NodeValue.PERSISTENT_NODE) {
	      srsName2 = indexWorker.getGeometricPropertyForNode(context.getBroker(), (NodeProxy)geometryNode2, "SRS_NAME").getStringValue();
	      geometry2 = indexWorker.getGeometryForNode(context.getBroker(), (NodeProxy)geometryNode2, false);		        		
	      hasUsedIndex = true;
	    }
	    //Otherwise build them
	    if (geometry1 == null) {
	      srsName1 = ((Element)geometryNode1.getNode()).getAttribute("srsName").trim();
	      geometry1 = indexWorker.streamNodeToGeometry(context, geometryNode1);
	    }
	    if (geometry2 == null) {
	      srsName2 = ((Element)geometryNode2.getNode()).getAttribute("srsName").trim();
	      geometry2 = indexWorker.streamNodeToGeometry(context, geometryNode2);	            	
	    }
      
	    if (geometry1 == null) 
	      throw new XPathException("Unable to get a geometry from the first node");
	    if (geometry2 == null) 
	      throw new XPathException("Unable to get a geometry from the second node");		        	
	      
	    //Transform the second geometry in the SRS of the first one if necessary
	    if (!srsName1.equalsIgnoreCase(srsName2)) {
	      geometry2 = indexWorker.transformGeometry(geometry2, srsName1, srsName2);      	
	    }				
	    
	    if (isCalledAs("intersection")) {
	      geometry = geometry1.intersection(geometry2);
	    } else if (isCalledAs("union")) {
	      geometry = geometry1.union(geometry2);
	    } else if (isCalledAs("difference")) {	
	      geometry = geometry1.difference(geometry2);
	    } else if (isCalledAs("symetricDifference")) {
	      geometry = geometry1.symDifference(geometry2);
	    }	
      
	    targetSRS = srsName1;
	  }
	}
      
	if (result == null) {	        
	  String gmlPrefix = context.getPrefixForURI(AbstractGMLJDBCIndexWorker.GML_NS);
	  if (gmlPrefix == null) 
	    throw new XPathException("'" + AbstractGMLJDBCIndexWorker.GML_NS + "' namespace is not defined");	
	    
	  context.pushDocumentContext();
	  try {
	    MemTreeBuilder builder = context.getDocumentBuilder();
	    DocumentBuilderReceiver receiver = new DocumentBuilderReceiver(builder);
	    result = (NodeValue)indexWorker.streamGeometryToElement(geometry, targetSRS, receiver);
	  } finally {
	    context.popDocumentContext();
	  }
	}
	} catch (SpatialIndexException e) {
	  throw new XPathException(e);
        }        
	return result;
      }</pre>
            <p>It looks more complicated because of the multiple possible argument counts.
                However, the pinciple remains the same: early result when empty sequences are
                involved, fetching of the <ph>Geometry</ph>ies (and of its/their SRS) from
                the DB, streaming if nothing can be fetched, then geometric computations after a
                transformation of the second <ph>Geometry</ph> if relevant.</p>
            <p>The final process streams the resulting <ph>Geometry</ph> as the result,
                in the SRS specified by the relevant argument or in the SRS of the first
                <ph>Geometry</ph>, depending on the function's name.</p>
        </section>

        

        <section id="play-spatial">
                <title>Playing with the spatial index</title>
            

            <p>Now that we have described the spatial index, it is time to play with it. Only a
                few of its features will be demonstrated, but we will explain again what happens
                under the hood.</p>
            <p>The first step is to make sure to have a recent enough release version of eXist:
                4.7.0 or later, and Java 8. </p>
            <p>Our demonstration file is taken from the <xref href="http://www.ordnancesurvey.co.uk/oswebsite/" scope="external">Ordnance Survey of
                Great-Britain's WWW site</xref> which offers <xref href="http://www.ordnancesurvey.co.uk/oswebsite/products/try-now/sample-data.html" scope="external">sample data</xref>.</p>
            <p>The chosen topography layer is of Port-Talbot, which is available as <xref href="http://www.ordnancesurvey.co.uk/products/osmastermap/layers/topography/sampledata/2182-SS7886-2c1.gz" scope="external">2182-SS7886-2c1.gz</xref>. Download this file, gunzip it, and give to the resulting
                file a <ph>.gml</ph> extension (<ph>port-talbot.gml</ph>) this
                will allow eXist to recognise it as an XML file.</p>
            <note type="note">
                    <p>If you have previously executed <ph>mvn test</ph>, the file should
                    have been downloaded and gunzipped for you in
                    <ph>$EXIST_HOME/extensions/indexes/spatial/target/test-classes</ph>.</p>
                </note>
            <p>Since this file refers to an OSGB-hosted schema, we will need to bypass validation
                in <ph>${EXIST_HOME}/etc/conf.xml</ph>.</p>
            <p>Make sure the mode value is set like this:</p>
            <pre>&lt;validation mode="no"&gt;</pre>
            <p>We are now ready to start the demonstration and we will use the interactive client
                for that. Run either <ph>${EXIST_HOME}/bin/client.bat</ph> or
                <ph>${EXIST_HOME}/bin/client.sh</ph> from the command line (please read
                elsewhere if you do not know how to start it).</p>
            <p>Let's start by creating a collection named <ph>spatial</ph> in the
                <ph>/db</ph> collection. The menus might be localised, but in english it
                is <ph>File/Create a collection...</ph>.</p>
            <p>Then, we will configure this collection by creating a configuration
                collection.</p>
            <p>Let's navigate to <ph>/db/system/config</ph> </p>
            <p>If required, let's create a general configuration collection: <ph>File/Create
                a collection...</ph> name it <ph>db</ph> and get into it.</p>
            <p>Then let's create a configuration collection for <ph>/db/spatial</ph>:
                <ph>File/Create a collection...</ph> name it <ph>spatial</ph>
                and get into it.</p>
            <p>We are now in <ph>/db/system/config/db/spatial</ph>.</p>
            <p>Let's now create a configuration file for this collection: <ph>File/Create an
                empty document...</ph> name it <ph>collection.xconf</ph>.</p>
            <p>Double-click on this document and let's replace its auto-generated content: <ph>template/</ph>
                </p>
            <p>with this one:</p>
            <pre outputclass="xml">&lt;collection xmlns="http://exist-db.org/collection-config/1.0"&gt;
  &lt;index&gt;
    &lt;gml flushAfter="200"/&gt;
  &lt;/index&gt;
&lt;/collection&gt;</pre>
            <note type="note">
                    <p>Do not forget to save the document before closing the window.</p>
                </note>
            <p>The <ph>/db/system/config/db/spatial</ph> collection is now configured
                to index GML geometries when they are uploaded. The in-memory index entries will be
                flushed to the HSQLDB every 200 geometries and will wait at most 100 seconds, the
                default value, to establish a connection to the HSQL db.</p>
            <p>Let's navigate to <ph>/db/spatial</ph>.</p>
            <p>Let's upload <ph>port-talbot.gml</ph>: File/Upload
                files/directories...</p>
            <p>On my computer, the operation on this 23.6 Mb file is performed in about 100
                seconds, including some default fulltext indexing. Let's close the upload window and
                quit the interactive client.</p>
            <p>Let's look our our GML file looks like on GML Viewer, a free viewer provided by
                <xref href="https://wiki.snowflakesoftware.com/display/GV50DOC" scope="external">Snowflake
                software</xref>:</p>
            <fig>
                    <image href="assets/port-talbot.jpg"/>
                </fig>
            <p>If you want to have a look at the spatial index HSQLDB, which, if you are using
                the default data-dir, is in <ph>${EXIST_HOME}/data/spatial_index.*</ph>
                there is a dedicated script file in
                <ph>${EXIST_HOME}/extensions/indexes/spatial/</ph>. to launch HSQL's GUI
                client: Use either <ph>hsql.bat</ph> or <ph>hsql.sh
                [data-dir]</ph> (you only need to supply data-dir if it is not the default
                one).</p>
            <p>If the SQL command <ph>SELECT * FROM SPATIAL_INDEX_V1;</ph> is executed,
                the result window shows that <ph>21961</ph> geometries have been
                indexed.</p>
            <p>Let's get back to the interactive client and open the query window (the one we get
                when clicking on the binocular button in the toolbar).</p>
            <p>This query:</p>
            <pre>declare namespace gml = "http://www.opengis.net/gml";
      spatial:transform(
        &lt;gml:Polygon srsName="osgb:BNG" xmlns:gml='http://www.opengis.net/gml'&gt;
        &lt;gml:outerBoundaryIs&gt;&lt;gml:LinearRing&gt;&lt;gml:coordinates&gt;
          278200,187600 278400,187600 278400,188000 278200,188000 278200,187600
         &lt;/gml:coordinates&gt;&lt;/gml:LinearRing&gt;&lt;/gml:outerBoundaryIs&gt;
        &lt;/gml:Polygon&gt;, 
        "epsg:4326")</pre>
            <p>Computes in a little bit less than 2 seconds. That could seem high, but there is a
                cost for the Geotools transformation factories initialization. Subsequent requests
                will be much faster, although there will always be a small cost for the streaming of
                the in-memory node to a <ph>Geometry</ph> object.</p>
            <p>The result is:</p>
            <pre outputclass="xml">&lt;gml:Polygon xmlns:gml="http://www.opengis.net/gml"&gt;
  &lt;gml:outerBoundaryIs&gt;
    &lt;gml:LinearRing&gt;
      &lt;gml:coordinates decimal="." cs="," ts=" "&gt;
        -3.7578,51.5743 -3.7579,51.5779 -3.755,51.5779 -3.7549,51.5743 -3.7578,51.5743
      &lt;/gml:coordinates&gt;
    &lt;/gml:LinearRing&gt;
  &lt;/gml:outerBoundaryIs&gt;
&lt;/gml:Polygon&gt;</pre>
            <note type="note">
                    <p>Due to the current Geotools limitations, there is no
                    <ph>srsName</ph> attribute on <ph>gml:Polygon</ph> ! See
                    above.</p>
                </note>
            <p>It might be more convenient to perform this query:</p>
            <pre>declare namespace gml = "http://www.opengis.net/gml";
      spatial:getEPSG4326WKT(
        &lt;gml:Polygon srsName="osgb:BNG" xmlns:gml='http://www.opengis.net/gml'&gt;
          &lt;gml:outerBoundaryIs&gt;&lt;gml:LinearRing&gt;
	    &lt;gml:coordinates&gt;
	      278200,187600 278400,187600 278400,188000 278200,188000 278200,187600
	    &lt;/gml:coordinates&gt;
	  &lt;/gml:LinearRing&gt;
	&lt;/gml:outerBoundaryIs&gt;
      &lt;/gml:Polygon&gt;)</pre>
            <p>Which results in:</p>
            <pre>POLYGON ((-3.7577853800140995 51.57430250509819, -3.7579241102503356 
      51.57789774692169, -3.7550389942943365 51.57794093512567, -3.754900491457913 51.57434568777196, 
      -3.7577853800140995 51.57430250509819)</pre>
            <p>So, 3 degrees West, 51 deegrees North... we must be indeed northern of Brittany,
                i.e. in south-western Great Britain.</p>
            <p>Let's see what our polygon looks like:</p>
            <fig>
                    <image href="assets/rectangle.jpg"/>
                </fig>
            <p>Now, we continue doing something more practical:</p>
            <pre>declare namespace gml = "http://www.opengis.net/gml";
      spatial:intersects(//gml:Polygon,
        &lt;gml:Polygon srsName="osgb:BNG" xmlns:gml='http://www.opengis.net/gml'&gt;
          &lt;gml:outerBoundaryIs&gt;
	    &lt;gml:LinearRing&gt;
	      &lt;gml:coordinates&gt;
	        278200,187600 278400,187600 278400,188000 278200,188000 278200,187600
	      &lt;/gml:coordinates&gt;
	    &lt;/gml:LinearRing&gt;
	 &lt;/gml:outerBoundaryIs&gt;
      &lt;/gml:Polygon&gt;
      )</pre>
            <p>This query returns 756 <ph>gml:Polygon</ph>s in about 15 seconds. A subsequent
                call returns in just about 450 ms, not having the cost for initializations (in
                particular the first connection to the HSQLDB). A slighly modified query, in order
                to show the performance without utilising eXist's performant cache:</p>
            <pre>declare namespace gml = "http://www.opengis.net/gml";
      spatial:intersects(//gml:Polygon,
        &lt;gml:Polygon srsName="osgb:BNG" xmlns:gml='http://www.opengis.net/gml'&gt;
          &lt;gml:outerBoundaryIs&gt;
	    &lt;gml:LinearRing&gt;
	      &lt;gml:coordinates&gt;
	        278201,187600 278401,187600 278401,188000 278201,188000 278201,187600
	      &lt;/gml:coordinates&gt;
	    &lt;/gml:LinearRing&gt;
	  &lt;/gml:outerBoundaryIs&gt;
        &lt;/gml:Polygon&gt;
      )</pre>
            <p>... retuns 755 <ph>gml:Polygon</ph> (one less) in just about 470 ms.</p>
            <p>The result of our first intersection query looks like this:</p>
            <fig>
                    <image href="assets/intersection.jpg"/>
                </fig>
            <p>Let's try another type of spatial query:</p>
            <pre>declare namespace gml = "http://www.opengis.net/gml";
      spatial:within(//gml:Polygon,
        &lt;gml:Polygon srsName="osgb:BNG" xmlns:gml='http://www.opengis.net/gml'&gt;
          &lt;gml:outerBoundaryIs&gt;
	    &lt;gml:LinearRing&gt;
	      &lt;gml:coordinates&gt;
	        278200,187600 278400,187600 278400,188000 278200,188000 278200,187600
	      &lt;/gml:coordinates&gt;
	    &lt;/gml:LinearRing&gt;
	  &lt;/gml:outerBoundaryIs&gt;
        &lt;/gml:Polygon&gt;
      )</pre>
            <p>It returns 598 <ph>gml:Polygon</ph>s in just a little bit more than 400 ms. Here
                is what they look like:</p>
            <fig>
                    <image href="assets/within.jpg"/>
                </fig>
            <p>The last query of this session is just to demonstrate some interesting
                capabilities of the spatial functions:</p>
            <pre>declare namespace gml = "http://www.opengis.net/gml";
      spatial:buffer(
        &lt;gml:Polygon srsName="osgb:BNG" xmlns:gml='http://www.opengis.net/gml'&gt;
          &lt;gml:outerBoundaryIs&gt;
	    &lt;gml:LinearRing&gt;
	      &lt;gml:coordinates&gt;
	        278200,187600 278400,187600 278400,188000 278200,188000 278200,187600
	      &lt;/gml:coordinates&gt;
	    &lt;/gml:LinearRing&gt;
          &lt;/gml:outerBoundaryIs&gt;
       &lt;/gml:Polygon&gt;,
       500
      )</pre>
            <p>See the (not so) rounded corners of our 500 metres buffer over Port-Talbot:</p>
            <fig>
                    <image href="assets/buffer.jpg"/>
                </fig>
        </section>

        

        <section id="facts">
                <title>Facts and thoughts</title>
            

            <p>The spatial index is in working condition. It provides an interesting set of
                functionalities and its performance is satisfactory given the lightweight, non
                spatially-enabled, database engine that stores the <ph>Geometry</ph>
                objects and their properties.</p>
            <note type="note">
                    <p>The Spatial Index does not currently support XQuery Update or XUpdate. Using
                    either update facility on GML documents will likely lead to incorrect query
                    results.</p>
                </note>
            <p>Here are still some tentative improvements.</p>
            <p>The first improvement is to plug in the <ph>getGeometriesForNodes()</ph>
                and <ph>getGeometricPropertyForNodes()</ph> (in
                <ph>org.exist.indexing.spatial.AbstractGMLJDBCIndexWorker</ph>) to allow
                sequence/set optimization.</p>
            <p>Indeed, a query like this one on our test file:</p>
            <pre>declare namespace gml = "http://www.opengis.net/gml";
for $pol in //gml:Polygon
return spatial:getArea($pol)</pre>
            <p>Which results in <ph>5339</ph> items through as many calls to the DB in
                51 seconds on an initialized index! Intercepting the
                <ph>SINGLE_STEP_EXECUTION</ph> flag when the expression is analyzed would
                allow to call the 2 above methods rather than their "individual" counterparts,
                namely <ph>getGeometryForNode()</ph> and
                <ph>getGeometricPropertyForNode()</ph>. The expected performance
                improvement would be interesting.</p>
            <p>A second improvement could be to refine the queries on the HSQLDB. Currently,
                <ph>search()</ph> (in
                <ph>org.exist.indexing.spatial.GMLHSQLIndexWorker</ph>) filters the
                records on the BBox of the searched <ph>Geometry</ph>. It would also be
                nice to refine the query on the context nodes and, in particular, on their involved
                collections and/or documents. The like applies for the HSQL implementation of
                <ph>getGeometryForNode()</ph> and
                <ph>getGeometricPropertyForNode()</ph> too.</p>
            <p>However, we have to be aware that writing such a SQL statement and passing it to
                the DB server might become counter-productive. The idea is then to define some
                (configurable) threshold values that would refine the query on the documents if
                there are fewer than, say, 10 documents in the context nodeset, and if there are
                more than 10 documents in it, but less than, say, 15 collections, refine the query
                on the collection.</p>
            <p>It would be quite easy to determine those threshold values above which writing a
                long SQL statement and passing it to the DB server takes more time than filtering
                the fectched data.</p>
            <p>We might also consider the field in which the document's URI is stored
                (<ph>DOCUMENT_URI</ph>) and possibly split it into two fields, one for the
                collection and the second one for the document. Of course, having indexed integer
                values here would probably be interesting.</p>
            <p>Having some better algorithms to prefilter the <ph>Geometry</ph>ies
                could probably help as well and, more generally, everything a DB server could bring
                (caching for instance) should be considered.</p>
            <p>An other improvement would be to introduce some error margins for
                <ph>Geometry</ph>ies computations or BBox ones. The <xref href="http://www.vividsolutions.com/jts/jtshome.htm" scope="external">Java Topology
                Suite</xref>, widely used by Geotools, has all the necessary material for
                this.</p>
            <p>Another interesting improvement would be to compute and return simplified
                <ph>Geometry</ph>ies depending of a "hint". Applications might want to
                return simplified polygons and even points at wider scales. Depending on the hint's
                value, passed in the function's parameters, the index could use the right DB fields
                in order to work on <i>precomputed</i> simpler (or, more generally,
                different) entries.</p>
            <p>This is how a hint configuration could look like:</p>
            <pre outputclass="xml">&lt;gml flushAfter="200"&gt;
  &lt;!-- divide the number of vertices by 2 --&gt;
  &lt;hint value="simplify1" method="simplify" parameter="2"/&gt;
  &lt;!-- divide the number of vertices by 4 --&gt;
  &lt;hint value="simplify2" method="simplify" parameter="4"/&gt;
  &lt;!-- reduce to a point --&gt;
  &lt;hint value="simplify3" method="point"/&gt;
&lt;/gml&gt;</pre>
            <p>We should also discuss with the other developers about the opportunity to have a
                <ph>org.exist.indexing.Index</ph> interface in the modularized indexes
                hierarchy. The abstract class <ph>org.exist.indexing.AbstractIndex</ph>
                provides some nice general-purpose methods and allows <i>static</i>
                members that are nearly mandatory (like <ph>ID</ph>). The like for
                <ph>org.exist.indexing.StreamListener</ph> versus
                <ph>org.exist.indexing.AbstractStreamListener</ph>.</p>
            <p>More tests should also be driven. The spatial index has only been tested on one
                file until now although this file is sizeable. It might be interesting to see how it
                behaves with unusual geometries like rectangles, multi-geometries and collections.
                It might also be interesting to know more about the error margin when geometries in
                different SRSes are involved. The accuracy of the referencing libraries available in
                the CLASSPATH would play an important role here.</p>
            <p>As always, the code could be written in a more efficient way. There are probably
                too many </p>
            <pre>if (...) ... else if(...) ... else ...</pre>
            <p> constructs in the code for instance. Also, we will have to follow Geotools
                progress to get rid of some of the more or less elegant workarounds we've had to
                implement.</p>
        </section>
    </section>
    </body>
</topic>